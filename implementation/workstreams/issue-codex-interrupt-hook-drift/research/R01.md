# Research — R01 codex interrupt hook drift

Research ID: codex-interrupt-hook-drift-r1
Status: active
Origin role: other
Origin subject: intake:issue-codex-interrupt-hook-drift
Return target: project_definition:issue-codex-interrupt-hook-drift
Research question: Determine which exact Codex, launcher, migration, UI, or other operation persisted `enabled = false` onto the journal-owned Interrupt hook; establish whether that write is intentional current Codex behavior or accidental drift; and identify the smallest safe compatibility fix that prevents a dead active route after restart without weakening codex-chatgpt-web ownership/fail-closed protection.
Return reconciliation: pending
Return reconciliation result: none

## Known evidence

- Incident and bounded recovery are summarized in `implementation/workstreams/issue-codex-interrupt-hook-drift/INTAKE.md`.
- The persisted hook retained the exact journal-owned command, state key, trusted hash, type, and timeout; the only semantic drift inside the managed block was `enabled = false`.
- The earliest retained mismatch predates the 2026-09-21 v5.0.14 update.
- Both retained v5.0.13 and v5.0.14 refused to overwrite the changed hook.
- Removing only `enabled = false` restored a journal-exact managed block; the next same-image launcher restart completed runtime migration/start and restored ordinary Sol routing.

## Investigation boundary

Research only for now. Do not implement or relax hook-ownership safety until the exact writer/behavior and intended compatibility rule are established from source/runtime evidence.
