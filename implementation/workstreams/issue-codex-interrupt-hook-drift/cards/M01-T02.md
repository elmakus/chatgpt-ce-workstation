# M01-T02 — Integrate and publish the corrected fork release

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/CODEX_INTERRUPT_HOOK_DRIFT_PLAN.md#milestone-m01--bounded-fork-compatibility-repair-and-release`
- Requirements: `requirements/CODEX_INTERRUPT_HOOK_DRIFT.md` — CIH-001..006
- Accepted decisions: `docs/DECISIONS.md#d11--codex-web-gpt-included-our-fork-is-the-default-package-source`; `docs/DECISIONS.md#d25--workstation-updates-resolve-latest-stable-identities-before-build`; `docs/DECISIONS.md#d29--native-interrupt-hook-disable-state-is-narrowly-repairable-while-ownership-drift-remains-fail-closed`
- Relevant OpenSpec: none
- Accepted dependency results: terminal GREEN result of `M01-T01`

### Must preserve

- Publish through the existing fork release workflow; do not invent a second release/config path.
- Release identity must bind accepted source commit, tag/version, Linux x64 AppImage and checksum.
- Do not publish a release from an implementation subject whose REQUIRED/RECOMMENDED Card review is not GREEN.
- Preserve v5.0.14 as the prior rollback artifact until the corrected release is accepted.

## Dependencies

- `M01-T01` terminal GREEN, including independent review.

## Outcome

The accepted fork repair is integrated and published as the next stable Linux release with exact source/tag/asset/checksum identity suitable for workstation D25 consumption.

## Scope

### Included

- Merge/integrate the independently accepted fork implementation through normal GitHub flow.
- Advance the fork package version as required by the existing release workflow.
- Use the existing `.github/workflows/fork-linux-release.yml` release path.
- Verify release tag/commit, Linux x64 AppImage, `checksums.txt`, and digest identity after publication.
- Persist exact release identity/evidence for M02.

### Excluded

- Changes to workstation resolver solely to pin the new release.
- Workstation candidate/runtime recurrence verification.
- Live workstation deployment.

## Acceptance

- Fork `main` contains the independently accepted repair.
- A non-draft/non-prerelease stable release for the new version exists and its tag resolves to the accepted source/release commit.
- The expected Linux x64 AppImage and `checksums.txt` are present and the checksum entry matches the published AppImage.
- Exact release identity is durable for M02 consumption.
- Prior known-working release remains available as rollback.

## Required tests / checks

- Required fork CI/release workflow checks on the release subject.
- GitHub release readback for tag, target commit, assets, and checksums.
- Verify published checksum matches the Linux x64 AppImage identity exposed to the workstation resolver.

## Optional execution hints

- Priority: `HIGH`
- Complexity: `LOW`
- Phase: `fork integration/release`

## External write/readback needs

GitHub merge/release writes in `elmakus/codex-chatgpt-web`; read back fork `main`, tag/commit, release metadata, asset list and checksums after publication.

## Independent review

`none` — the behavioral implementation subject is independently reviewed in M01-T01; this Card is deterministic integration/publication of that exact accepted subject and existing release mechanism.

## Contract overrides

None.
