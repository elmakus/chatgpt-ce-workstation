# M00-T02 — Align fork release candidate with canonical private release lineage

- Milestone: `M00`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m00--codex-web-gpt-fork-release-readiness`
- Requirements: `requirements/SMART_UPSTREAM_UPDATES.md#R4`, `#R5`, `#R9`, `#R13`
- Accepted decisions: `docs/DECISIONS.md#D5`, `#D10`, `#D11`, `#D25`
- Relevant OpenSpec: none
- Accepted dependency results:
  - `M00-T01` result `elmakus/codex-chatgpt-web@0dcf8ed9dbd229cf908cc68b31f4126c0af2414d`
  - `implementation/workstreams/change-codex-web-upstream-v6-sync/evidence/final-integration-review.md`
  - current workflow-main `workflow/common/FORK_RELEASE_VERSIONING.md`

### Must preserve

- T01's accepted upstream-v6.1.0 behavior and all Workstation-required fork routing/auth/isolation behavior.
- Upstream lineage baseline remains `miuuyy/codex-chatgpt-web v6.1.0`; fork-local release identity must use the canonical first-lane version `v6.1.0-private.1`.
- `private.N` is lineage metadata, not prerelease quality; this M00 publication target is the accepted stable fork release.
- Fork release provenance/checksum behavior and image-managed updater-disable behavior remain intact.
- No Workstation production image/container rebuild, recreate or promotion belongs to this Card.

### Must not / rationale that must travel

- Do not rewrite/delete legacy `v5.0.x` releases or tags.
- Do not advance the upstream baseline version to manufacture a fork version.
- Do not publish a release/tag or merge the fork to `main` before the corrected exact subject receives the required workstream-level fresh independent review.
- Do not change runtime behavior merely to satisfy release-lineage metadata.

## Dependencies

- `M00-T01` done and its exact independent final-integration verdict GREEN.

## Outcome

Produce one exact corrected Codex Web GPT fork candidate that preserves the reviewed T01 behavior while making the canonical stable downstream release `v6.1.0-private.1` internally version-consistent and publishable by the repository release workflow.

## Scope

### Included

- Reconcile package/launcher/runtime version metadata to the canonical `6.1.0-private.1` fork release identity.
- Reconcile release-workflow tag/version validation for that exact canonical identity.
- Ensure canonical `-private.N` tags are not automatically classified as GitHub prereleases solely because they contain a hyphen.
- Add/refine focused release-version contract tests/checks when useful.
- Preserve the upstream launcher asset baseline `6.1.0` where it intentionally identifies upstream assets rather than the fork release.

### Excluded

- Runtime feature changes.
- A new upstream sync.
- Workstation production deployment.
- Fork-main merge, tag creation or GitHub Release publication before the fresh final-integration review gate is GREEN.
- Rewriting historical releases/tags.

## Acceptance

- Root package, launcher package and runtime-reported fork version are synchronized to `6.1.0-private.1`.
- The release workflow accepts tag `v6.1.0-private.1` as matching the package version.
- The release workflow treats canonical `vX.Y.Z-private.N` as stable release lineage, not prerelease merely due to the hyphen; ordinary non-canonical hyphenated prerelease tags remain non-stable.
- Existing upstream launcher download references remain tied to upstream `6.1.0` where intended.
- T01 runtime/routing/auth/isolation behavior is unchanged by the correction.
- Focused version/release checks and materially affected CI/build checks are GREEN on the exact corrected candidate.
- The exact corrected candidate is frozen for a new RECOMMENDED workstream final-integration review before fork-main merge/release publication.

## Required tests / checks

- `bun run check-version`.
- Focused source assertions/tests for canonical private-tag stable classification and non-canonical prerelease classification.
- Typecheck/build or repository CI materially affected by the release metadata/workflow change.
- Exact Git diff inspection proving no unintended runtime-behavior change from T01.

## External write/readback needs

Write only to `elmakus/codex-chatgpt-web:work/upstream-v6.1-sync` during implementation, then read back the exact branch HEAD. Fork `main`, tags/releases and Workstation production remain untouched until later gates.

## Independent review

`none` for the Card itself because the selected workstream's existing RECOMMENDED final-integration gate will be re-frozen on the exact corrected release-ready subject and independently reviewed before publication.

## Contract overrides

None.
