# M00-T03 — Repair exact-tree health privacy regression for private release lineage

- Milestone: `M00`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m00--codex-web-gpt-fork-release-readiness`
- Requirements: `requirements/SMART_UPSTREAM_UPDATES.md#R4`, `#R5`, `#R9`, `#R13`
- Accepted decisions: `docs/DECISIONS.md#D5`, `#D10`, `#D11`, `#D25`
- Workflow release-lineage contract: current `workflow/common/FORK_RELEASE_VERSIONING.md`
- Accepted dependency result: `M00-T02` subject `elmakus/codex-chatgpt-web@7dac256e2607cf6cb46ce0441f17a123fb50bbda`
- Corrective origin: `implementation/workstreams/change-codex-web-upstream-v6-sync/evidence/final-integration-review-7dac256e-red.md`

### Must preserve

- Canonical fork identity remains `v6.1.0-private.1`; do not revert or disguise the accepted `private.N` lineage.
- T01/T02 runtime, routing, authentication, Muse/Codex-LB isolation, interrupt-hook, proxy, checksum/provenance and updater-disable behavior remains unchanged.
- Health/runtime metadata may expose the public fork version required for inspectability; actual credentials, upstream private response detail and bearer tokens must remain absent.
- No fork release/tag/main publication and no Workstation production rebuild/recreate/promotion belongs to this Card.

## Dependencies

- `M00-T02` done.
- Final-integration review of `7dac256e2607cf6cb46ce0441f17a123fb50bbda` RED with the bounded failing assertion identified above.

## Outcome

Restore the exact-tree runtime suite by making the health privacy regression test distinguish public canonical release metadata from actual sensitive fixture material, without changing runtime behavior.

## Scope

### Included

- Replace the over-broad health assertion that rejects the word `private` with explicit assertions that the concrete sensitive proxy error detail, upstream response detail and bearer token are absent from health output.
- Keep the correction test-only unless exact evidence proves a runtime change is required.
- Re-run the full M00 exact-candidate verification required for the corrected immutable subject.

### Excluded

- Runtime routing/auth behavior changes.
- Release-version or release-workflow changes.
- New upstream synchronization.
- Fork-main merge, tag/release publication or Workstation production deployment.

## Acceptance

- The health test still proves that the concrete sensitive transport error text, upstream denial body and bearer token do not appear in health output.
- Public `6.1.0-private.1` version metadata no longer triggers a false privacy failure.
- No production/runtime source behavior changes from T02.
- Full applicable runtime tests, launcher tests, typecheck/build and representative release/package smoke are GREEN on the exact corrected tree.
- The corrected exact candidate is frozen for a new RECOMMENDED workstream final-integration review before publication.

## Required tests / checks

- Focused `tests/server-lifecycle.test.ts`.
- `bun run check-version`.
- Full `bun test ./tests`.
- Full Node 24 launcher suite.
- Root + launcher typecheck/build.
- Representative Linux release/package smoke sufficient to preserve the M00 package/release acceptance on the exact corrected tree.
- Exact Git diff inspection proving the corrective commit is test-only.

## External write/readback needs

Write only to `elmakus/codex-chatgpt-web:work/upstream-v6.1-sync`, then read back the exact branch HEAD. Publication surfaces remain untouched.

## Independent review

`none` for this Card itself. The selected workstream's RECOMMENDED final-integration gate must be re-frozen on the corrected exact release-ready subject and reviewed by a fresh independent chat.

## Contract overrides

None.
