# M01-T14 — Omit Muse-unsupported `web_search.search_content_types` only on Muse-bound Responses requests

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R8 / M01 planned work 0–2
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R6, R7
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
- Accepted research evidence: `implementation/workstreams/change-muse-native-upstream/research/R2.md`
- Production blocker evidence: `implementation/workstreams/change-muse-native-upstream/evidence/M01-T13-muse-activation-blocker.md`
- Accepted fork baseline: `elmakus/codex-chatgpt-web@3a6d1d28c28dbe1be885077ae53fe4bba62b9673` / v5.0.15
- Relevant OpenSpec: none

### Must preserve

- Provider-specific request normalization belongs in `elmakus/codex-chatgpt-web`; Workstation MUST NOT gain request-body filtering/provider-routing logic.
- Apply the new normalization only for native `responses` requests whose validated model ID is `muse-*`.
- Preserve the existing accepted exact Gmail namespace omission for Muse-bound Responses requests.
- For a tool whose exact `type` is `web_search`, preserve the tool entry and every other property while omitting only `search_content_types`.
- Preserve relative tool ordering and every unrelated tool entry/property.
- Do not strip `search_content_types` from `web_search_preview` shapes merely for symmetry.
- When neither the Gmail omission nor the new web-search property omission nor an existing bridge scrub requires rewriting, preserve the ordinary native request body bytes.
- Preserve existing zstd/decode/re-serialization behavior and remove stale `content-encoding` only when an actual rewrite occurs.
- Ordinary native/Codex-LB requests retain their current Gmail and `web_search` shapes unchanged.
- Browser-backed `chatgpt-web/*` behavior remains unchanged.
- Preserve existing native/Muse routing, catalog filtering, authentication isolation, bridge-artifact scrubbing and failure semantics.
- No credential value may enter source, fixtures, logs or evidence.
- Production Muse remains unset during this Card; no Workstation deployment or CLIProxyAPI production mutation is part of this Card.

### Must not / rationale that must travel

- Do not remove or disable the complete `web_search` tool: R2 proves the tool succeeds when only `search_content_types` is absent.
- Do not rename `web_search` to `web_search_preview`.
- Do not strip another `web_search` property or another non-Gmail tool without separate verified evidence and authority.
- Do not patch CLIProxyAPI/Meta production or add a generic provider-normalization framework.
- Do not change Workstation Compose/runtime configuration.
- Do not combine unrelated current related-fork behavior into the immutable correction subject unless required by exact current fork build/test compatibility; any such need must route through normal workflow classification before broadening scope.

## Dependencies

- `M01-T10`
- approved R8 planning result
- consumed R2 research establishing the exact unsupported property

## Outcome

The related fork deterministically preserves Muse web search while removing only the provider-unsupported `search_content_types` property from exact `type: "web_search"` tool entries on Muse-bound native Responses requests, composing safely with the existing Muse-only Gmail omission and leaving ordinary native/Codex-LB and browser-backed behavior unchanged.

## Scope

### Included

- Extend the existing decoded Muse Responses-body normalization in `src/native-passthrough.ts` (or an equivalently narrow adjacent helper) rather than introducing a second routing layer.
- Exact `muse-*` + `responses` guard.
- Property-level normalization for exact `type: "web_search"`.
- Composition with the existing exact Gmail namespace omission.
- Correct identity/zstd body and header handling when either Muse-specific rewrite occurs.
- Focused regression tests proving exact field omission, preservation of the rest of the `web_search` tool, representative unrelated tools/properties/order, Gmail composition, Muse no-op when no rewrite is needed, `web_search_preview` non-overreach, and ordinary-native controls.
- Existing fork verification and applicable package/CI checks for the exact behavioral correction subject.
- Dedicated related-fork branch/PR and immutable result subject suitable for independent review.

### Excluded

- Version bump or release publication.
- Integrating an unreviewed behavioral subject into a release.
- Workstation image rebuild/update/promotion.
- Production Muse activation.
- CLIProxyAPI source/configuration/runtime changes.
- New credentials or credential migration.
- Any broader provider/tool compatibility normalization.

## Acceptance

- A validated `muse-*` native Responses request containing `{"type":"web_search","search_content_types":["text","image"]}` forwards the same `web_search` tool with only `search_content_types` absent.
- Other properties on that `web_search` entry and all unrelated tools remain unchanged and in the same relative order.
- Existing exact Gmail namespace omission still applies and composes with the new property-level normalization in the same Muse request.
- `web_search_preview` shapes are not altered by the new rule.
- A Muse request requiring none of the bounded rewrites remains a no-op for this compatibility path.
- A non-Muse native request retains Gmail and the current `web_search.search_content_types` shape unchanged.
- Browser-backed `chatgpt-web/*` code path is not behaviorally changed.
- Existing bridge-artifact/native routing and Muse catalog/routing behavior remain GREEN.
- `bun run verify` and applicable related-fork CI/package checks are GREEN.
- No secret value is added or emitted.
- Exact branch/commit/PR result is durable and frozen for independent review.

## Required tests / checks

- Focused exact Muse `web_search.search_content_types` omission test.
- Focused preservation test for remaining `web_search` properties plus representative unrelated tools/order.
- Gmail + web-search normalization composition test.
- Muse no-op test when neither compatibility rewrite is needed.
- `web_search_preview` non-overreach test.
- Non-Muse native Gmail + `web_search` shape-preservation control.
- zstd-compressed request regression coverage when a rewrite occurs.
- Existing native passthrough/network routing tests.
- Existing Muse catalog/routing tests.
- `bun run verify`.
- Related-fork PR CI/package matrix before behavioral acceptance.

## External write/readback needs

Repository writes only to a dedicated branch/PR in `elmakus/codex-chatgpt-web`. No Workstation production/live-system or CLIProxyAPI write is authorized or needed in this Card. Read back the exact branch/commit/PR subject and CI results.

## Independent review

`RECOMMENDED` — this changes the exact forwarded Muse tool payload and will become the basis for a production fork release.

## Contract overrides

none
