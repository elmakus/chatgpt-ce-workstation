# M01-T13 — Activate Muse upstream and verify R7 production routing

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R7 / M01 planned work 4–5
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R1–R7
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
  - `docs/DECISIONS.md#d25--workstation-updates-resolve-latest-stable-identities-before-build`
- Accepted predecessor results:
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T08-independent-review.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T09-independent-review.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T10-fork-release.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T11-d25-update.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T12-independent-review.md`

### Must preserve

- Production starts this Card on the exact GREEN M01-T11 image whose embedded `codex_web_gpt` is `v5.0.15@3a6d1d28c28dbe1be885077ae53fe4bba62b9673`, AppImage SHA-256 `bdad25547ae79f29ee27a029bed5ec6c7762f98a92e7578762de40a113b9b49d`, `override: false`.
- Set only the non-secret endpoint `CODEX_CHATGPT_WEB_MUSE_UPSTREAM=http://192.168.2.104:8317/v1` in target-host local `.env`.
- Keep `CODEX_CHATGPT_WEB_NATIVE_UPSTREAM=http://192.168.2.104:2455/backend-api/codex` unchanged.
- Reuse the existing persistent `~/.config/codex-web-gpt/muse-proxy-api-key`; verify only metadata/readability and never print, commit or persist its content.
- Recreate the same promoted Workstation image through repository-owned Compose with build inputs re-rendered from the running image's exact embedded frozen resolution; do not rebuild or re-resolve upstreams.
- Only `muse-*` models may be imported/routed from CLIProxyAPI; its non-Muse rows must not appear in the Workstation catalog.
- A normal Muse-bound Codex Responses request must omit exactly the `mcp__codex_apps__gmail` namespace while preserving representative non-Gmail tools.
- An ordinary native/Codex-LB request must retain Gmail unchanged; browser-backed `chatgpt-web/*` remains unchanged.
- Workstation must not gain provider-routing/request-body filtering logic.
- If activation or acceptance fails, unset Muse upstream and recreate the same frozen image/resolution, then verify the pre-activation healthy route/catalog baseline.

## Dependencies

- `M01-T11`
- `M01-T12`

## Outcome

Production Workstation exposes usable `muse-*` models through CLIProxyAPI on the exact v5.0.15 image. A normal Muse turn succeeds with Gmail intentionally omitted and non-Gmail tools retained, while ordinary native/Codex-LB and `chatgpt-web/*` remain GREEN.

## Scope

### Included

- Re-read running image, embedded frozen resolution, v5.0.15 identity, health, native upstream and Muse-unset baseline before mutation.
- Recheck Muse key file presence/owner/group/mode/readability without content disclosure.
- Make one secret-safe authenticated direct CLIProxyAPI `GET /v1/models` readback, recording only response shape/counts/public model IDs.
- Persist only the non-secret Muse endpoint in local untracked `.env`.
- Render exact frozen Compose inputs from the running image's embedded resolution and recreate the same image through repository-owned Compose.
- Verify post-recreate image identity, health, native/Muse environment and key metadata.
- Verify Workstation catalog includes the available `muse-*` rows, excludes CLIProxyAPI non-Muse rows, and retains ordinary native plus `chatgpt-web/*` rows.
- Run a normal native request smoke through Codex-LB.
- Run a normal Codex client Muse turn with the real tool surface and capture only secret-safe request metadata/tool names sufficient to prove Gmail omission and representative non-Gmail preservation.
- Run a native control proving the Gmail namespace is still present unchanged for ordinary native/Codex-LB requests.
- Run a browser-backed `chatgpt-web/*` smoke.
- Verify route status/health, `bash scripts/validate-source.sh`, `bash scripts/wait-healthy.sh`, and `bash scripts/verify-runtime.sh` with exact frozen inputs.
- Persist secret-safe production evidence and rollback readiness.

### Excluded

- Fork/source/release changes.
- Workstation image rebuild or upstream re-resolution.
- CLIProxyAPI provider/configuration mutation.
- Any credential-content readback or evidence.
- Restoring Gmail capability for Muse.
- Additional provider/tool compatibility exceptions.
- Unrelated runtime/configuration changes.

## Acceptance

- Pre-activation baseline matches M01-T11 exact image/frozen v5.0.15 identity and is healthy with native upstream unchanged and Muse unset.
- Muse key file exists, has restrictive ownership/mode, and is readable by the runtime user without content disclosure.
- Direct authenticated CLIProxyAPI catalog is HTTP 200/OpenAI-compatible; available public `muse-*` IDs and non-Muse row count are recorded without credential output.
- Post-recreate running image is unchanged from M01-T11, native upstream is unchanged, and Muse endpoint is exactly `http://192.168.2.104:8317/v1`.
- Workstation model discovery imports the available `muse-*` IDs, imports no CLIProxyAPI non-Muse IDs, and retains ordinary `gpt-5.6-sol` plus `chatgpt-web/*`.
- Ordinary native request succeeds through Codex-LB.
- A normal Codex client turn using an available `muse-*` model succeeds. Secret-safe forwarded-request evidence proves `mcp__codex_apps__gmail` is absent while representative non-Gmail tools remain.
- Native control proves `mcp__codex_apps__gmail` remains present unchanged on an ordinary native/Codex-LB request.
- Browser-backed `chatgpt-web/*` smoke succeeds.
- Source validation, route status, health and `WORKSTATION_RUNTIME_GREEN` are GREEN.
- No secret value is emitted or committed.

## Required tests / checks

- Exact running image/frozen-resolution/v5.0.15 identity readback.
- Secret-safe key metadata/readability check.
- Authenticated CLIProxyAPI catalog shape/filtering readback.
- Frozen-resolution Compose recreate without rebuild.
- Post-recreate environment/image/health readback.
- Workstation catalog filtering/discovery check.
- Native, normal-client Muse, and browser-backed route smokes.
- Secret-safe Muse forwarded-tool-name capture and native Gmail-preservation control.
- `bash scripts/validate-source.sh`.
- `bash scripts/wait-healthy.sh` and `bash scripts/verify-runtime.sh` with frozen rendered inputs.
- Git clean/secret-hygiene readback.

## External write/readback needs

Already authorized by approved R7: write the non-secret Muse endpoint to target-host local `.env`, recreate the existing production Workstation with the exact frozen resolution/image, and perform secret-safe runtime/API verification. The persistent ingress credential may be consumed only in-memory by the existing runtime/request path and must never be printed or persisted in evidence.

## Independent review

`none` — approved R7 keeps the remaining RECOMMENDED independent gate at integrated M01 after production activation/verification evidence is durable.

## Contract overrides

None.
