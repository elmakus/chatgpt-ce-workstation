# M01-T03 — Correct CLIProxyAPI Muse catalog compatibility in the related fork

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md#milestone-m01--make-the-optional-muse-upstream-production-ready-and-deploy-it` (approved R5)
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R4, R6
- Accepted decisions: `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
- Relevant OpenSpec: none
- Accepted dependency results:
  - `M01-T01` repository wiring result
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T02-live-activation-blocker.md`

### Must preserve

- Provider/catalog adaptation remains owned by `elmakus/codex-chatgpt-web`; do not add parallel provider-routing logic to Workstation.
- The actual CLIProxyAPI `GET /v1/models` contract is OpenAI-compatible: top-level `data` array, public model identity in `id`.
- Only public IDs beginning with `muse-` may enter the Codex model catalog; all CLIProxyAPI non-Muse rows remain excluded.
- Muse catalog/request authentication stays separate from Codex-LB authentication.
- Ordinary native/Codex-LB routing, `chatgpt-web/*` placement, Muse request fail-closed behavior, optional Muse-catalog failure isolation, and the v5.0.13 configured-upstream proxy-resolution correction must remain intact.
- Normalized Muse rows must satisfy the current Codex catalog surface sufficiently to be discoverable/selectable for the routed Muse request path; do not merely rename `id` to `slug` if required catalog metadata would still be absent.
- Preserve the currently-tested Codex-shaped Muse catalog form (`models[]` / `slug`) unless exact implementation evidence proves that compatibility must be deliberately superseded.
- No credential or live secret value may enter source, test fixtures, logs, or evidence.

### Must not / rationale that must travel

- Do not change Workstation production configuration in this Card.
- Do not broaden the fork into a generic CLIProxyAPI provider importer.
- Do not import or route non-`muse-*` models.
- Do not weaken failure isolation by letting an invalid/unavailable Muse catalog fail the primary native or `chatgpt-web/*` catalog.

## Dependencies

- `M01-T01`
- accepted M01-T02 blocker evidence establishing the live catalog shape

## Outcome

The related fork accepts the observed CLIProxyAPI OpenAI-compatible Muse catalog, emits only usable `muse-*` rows into the Codex catalog, and preserves the existing primary native, browser-backed, authentication, routing and failure-isolation contracts.

## Scope

### Included

- Focused catalog normalization/merge changes in `elmakus/codex-chatgpt-web`.
- Regression tests using the observed `{ object, data: [...] }` / `id` shape, including non-Muse filtering.
- Regression coverage for optional Muse-catalog failure isolation and existing route ownership.
- The fork's normal verify/CI/package checks for the exact correction subject.

### Excluded

- Version bump/release publication.
- Workstation image rebuild/promotion.
- Production Muse activation.
- CLIProxyAPI provider configuration changes.

## Acceptance

- A fixture matching the observed live CLIProxyAPI response shape produces expected `muse-*` Codex catalog rows and no non-Muse CLIProxyAPI rows.
- Produced Muse rows satisfy the existing Codex catalog contract needed for discovery/selection and preserve the exact public Muse IDs used for routing.
- Existing Codex-shaped Muse catalog compatibility remains GREEN unless deliberately superseded by exact evidence.
- Invalid/unavailable Muse catalog input remains isolated: primary native and `chatgpt-web/*` catalog rows are still returned.
- Ordinary native requests stay on Codex-LB, Muse requests stay on the Muse upstream, and `chatgpt-web/*` behavior is unchanged.
- Fork verification and applicable CI/package checks are GREEN.
- No secret value is added or emitted.

## Required tests / checks

- Focused model-catalog/server-model tests for live CLIProxyAPI `data/id` normalization and Muse-only filtering.
- Existing Muse merge/failure-isolation tests.
- Existing native/Muse route tests.
- `bun run verify`.
- Fork pull-request CI/package matrix before release.

## Optional execution hints

- Priority: HIGH
- Complexity: MEDIUM
- Phase: related-fork compatibility correction
- Expected/relevant code locations:
  - `src/model-catalog.ts`
  - `src/server.ts` only if normalization boundary requires it
  - `tests/model-catalog.test.ts`
  - `tests/server-models.test.ts`

## External write/readback needs

Repository writes only to a dedicated branch/PR in `elmakus/codex-chatgpt-web`; no production/live-system write is authorized or needed in this Card. Read back the exact branch/PR subject and CI results.

## Independent review

`RECOMMENDED` — this focused behavioral correction changes the catalog presented to Codex and will become the release basis for production activation.

## Contract overrides

none
