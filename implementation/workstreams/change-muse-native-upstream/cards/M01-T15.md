# M01-T15 — Prepare immutable v5.0.16 release candidate from the GREEN Muse web-search correction

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R8 / M01 planned work 2.
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R6, R7.
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
- Accepted dependency result:
  - M01-T14 exact independently GREEN behavioral subject `elmakus/codex-chatgpt-web@acc95f7a5447144bdfc5cd40ff50ce7e63dd3e95`
  - M01-T14 independent review evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T14-independent-review.md`
- Current published fork baseline: `v5.0.15@3a6d1d28c28dbe1be885077ae53fe4bba62b9673`.
- Current related-fork `main@0f77eb00203f93617f7c38a24ad4e1c6e1cc9d3b` has unrelated interrupt-hook/integration drift and MUST NOT be folded into this release subject.
- Existing stale `release-prep/v5.0.16@68142a3f1725e052a606f488ac2c525db650a661` diverges from the reviewed M01-T14 subject and MUST NOT be force-reset/reused as the candidate branch.
- Relevant OpenSpec: none.

### Must preserve

- The release candidate must contain the exact independently GREEN M01-T14 behavioral tree plus only the version-coupled metadata required for v5.0.16 publication.
- Base the new candidate branch directly on exact reviewed subject `acc95f7a5447144bdfc5cd40ff50ce7e63dd3e95`; do not merge/rebase unrelated current `main` drift into it.
- Synchronize only `package.json`, `launcher/package.json`, and `src/version.ts` to `5.0.16`; keep `upstreamLauncherVersion` unchanged.
- Prepare on a non-release-triggering branch/PR so the exact candidate can receive independent review before publication.
- Preserve the existing release workflow and checks; do not bypass version-sync, CI/package, AppImage ABI or app-smoke gates.
- Preserve the reviewed Muse-only Gmail omission and exact `web_search.search_content_types` normalization unchanged.
- No credentials, API keys, bearer tokens or operator runtime values may enter Git or evidence.

### Must not / rationale that must travel

- Do not create/push `release/v5.0.16`, create tag `v5.0.16`, or publish/edit a GitHub release in this Card.
- Do not reuse or force-reset the pre-existing divergent `release-prep/v5.0.16` branch.
- Do not merge PR #17 or the candidate into unrelated current `main` as part of this Card.
- Do not alter M01-T14 behavioral source/tests beyond the exact reviewed subject.
- Do not mutate Workstation production, CLIProxyAPI or Muse activation state.

## Dependencies

- `M01-T14`

## Outcome

A narrowly scoped non-publishing v5.0.16 release candidate exists from exact independently GREEN M01-T14 subject, with only synchronized version metadata added, GREEN required CI/package checks, and an immutable SHA/PR frozen for independent review.

## Scope

### Included

- Re-read M01-T14 exact subject and confirm its GREEN review evidence.
- Create a fresh non-publishing candidate branch from exact subject `acc95f7a5447144bdfc5cd40ff50ce7e63dd3e95`, using a name that does not overwrite the divergent stale `release-prep/v5.0.16`.
- Change only `package.json`, `launcher/package.json`, and `src/version.ts` from `5.0.15` to `5.0.16`; keep `upstreamLauncherVersion` unchanged.
- Open a draft release-candidate PR against `release/v5.0.15` so the candidate delta is the reviewed correction plus version metadata, without unrelated `main` drift.
- Verify exact compare/diff and required related-fork CI/package gates.
- Persist exact candidate SHA/PR evidence and freeze that SHA as the independent-review subject.

### Excluded

- Release trigger branch/tag/publication.
- Related-fork `main` integration.
- Any new compatibility behavior.
- Workstation image resolution/build/promotion.
- Workstation production activation/runtime mutation.
- CLIProxyAPI mutation.

## Acceptance

- Candidate branch starts from exact reviewed M01-T14 subject and adds only synchronized v5.0.16 metadata in the three expected files.
- Compare against `release/v5.0.15@3a6d1d28c28dbe1be885077ae53fe4bba62b9673` contains exactly the already-reviewed M01-T14 two-file behavioral correction plus the three version files, with no unrelated current-`main` drift.
- `package.json.version`, `launcher/package.json.version`, and `src/version.ts` identify `5.0.16`; `upstreamLauncherVersion` remains unchanged.
- Candidate PR CI succeeds for required verify/package/platform matrix.
- No release/tag/release-triggering branch is published.
- Exact candidate SHA/PR and checks are durable evidence for independent review.

## Required tests / checks

- Exact candidate ancestry/readback from M01-T14 subject.
- Exact compare/diff/stat against published v5.0.15 baseline.
- `scripts/check-version.ts` through `bun run verify`.
- Candidate PR CI: actionlint plus macOS/Windows/Ubuntu `bun run verify`, package/app smoke and Linux AppImage ABI validation.
- Readback proving the stale divergent `release-prep/v5.0.16` branch was not mutated.

## External write/readback needs

Repository writes only in `elmakus/codex-chatgpt-web`: create a fresh non-publishing candidate branch, one version-only candidate commit, and a draft PR. No release publication or Workstation/live-system write is authorized in this Card.

## Independent review

`RECOMMENDED` — this is the last reversible checkpoint before v5.0.16 publication and must prove the immutable candidate equals the independently GREEN M01-T14 behavior plus only required version metadata.

## Contract overrides

none
