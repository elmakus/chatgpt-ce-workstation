# M01-T10 — Publish the exact independently GREEN v5.0.15 fork release

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R7 / M01 planned work 2
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R6, R7
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
- Relevant OpenSpec: none
- Accepted dependency results:
  - M01-T09 exact independently GREEN release candidate `elmakus/codex-chatgpt-web@3a6d1d28c28dbe1be885077ae53fe4bba62b9673`
  - M01-T09 independent review evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T09-independent-review.md`
  - existing fork publication contract `.github/workflows/fork-linux-release.yml` at the reviewed candidate
  - prior accepted publication pattern from M01-T05, except its post-publication main fast-forward is not reusable because current `main` has independently diverged from the v5.0.15 candidate base

### Must preserve

- Publish the exact independently GREEN M01-T09 candidate without changing its tree or version metadata.
- Use the existing fork publication path: create `release/v5.0.15` pointing directly at exact reviewed SHA `3a6d1d28c28dbe1be885077ae53fe4bba62b9673`; do not add or edit release machinery.
- The published `v5.0.15` tag/release must resolve to that exact reviewed SHA.
- Preserve the existing Fork Linux Release verify/package/Linux AppImage ABI/app-smoke/checksum gates.
- Preserve the accepted Muse Gmail behavior exactly as already reviewed; no new source behavior may be introduced during publication.
- Current related-fork `main` is independently at `0f77eb00203f93617f7c38a24ad4e1c6e1cc9d3b` with unrelated interrupt/integration changes after candidate base `d752b2609d8a173e0b6f516f3dcbae4b509640d1`. Do not rebase/merge those commits into the release candidate and do not force/reset `main`.
- No credentials, API keys, bearer tokens, or operator runtime values may enter Git or evidence.

### Must not / rationale that must travel

- Do not create a replacement release commit, merge commit or rebased candidate after independent review; that would change the immutable publication subject.
- Do not repeat M01-T05's fast-forward of `main`; it is no longer legal because `main` and the candidate have diverged.
- Do not merge PR #15 into the advanced `main` as part of release publication, because that would create a different integrated tree containing unrelated post-freeze changes.
- Do not rebuild or mutate the Workstation, CLIProxyAPI or production Muse endpoint in this Card.

## Dependencies

- `M01-T09`

## Outcome

`elmakus/codex-chatgpt-web` release `v5.0.15` is published from the exact independently GREEN candidate, the existing release checks are GREEN, the release/tag/assets/checksums are read back, and unrelated current-`main` drift remains outside the immutable v5.0.15 publication subject.

## Scope

### Included

- Re-read `release-prep/v5.0.15` / PR #15 and verify its head still equals exact reviewed candidate `3a6d1d28c28dbe1be885077ae53fe4bba62b9673`.
- Re-read absence of an existing `v5.0.15` tag and `release/v5.0.15` trigger branch.
- Read back current related-fork `main` and record its pre-publication SHA without mutating it.
- Create `release/v5.0.15` directly from exact reviewed candidate SHA.
- Let existing `Fork Linux Release` workflow execute its required gates and publish `v5.0.15`.
- Verify the published tag resolves to the exact reviewed candidate and the release includes the Linux x64 AppImage plus `checksums.txt`.
- Verify published checksum evidence is consistent with the release asset/readback available from GitHub.
- Verify related-fork `main` remains unchanged from the pre-publication SHA.
- After successful publication/readback, close draft PR #15 without merging it so the stale release-prep branch cannot later be mistaken for an integration request into the advanced `main`.

### Excluded

- Any source/behavior/version change beyond the already-reviewed candidate.
- New release workflow/mechanism changes.
- Rebase/merge/cherry-pick of current `main` drift into v5.0.15.
- Force/reset/fast-forward of diverged related-fork `main`.
- Workstation source/image update.
- Production container or CLIProxyAPI mutation.
- Muse endpoint activation.

## Acceptance

- `release/v5.0.15` is created from exact reviewed candidate `3a6d1d28c28dbe1be885077ae53fe4bba62b9673`.
- Fork Linux Release publication gates complete GREEN.
- Published `v5.0.15` tag/release resolves to the exact reviewed candidate SHA.
- Release contains the expected Linux x64 AppImage and `checksums.txt`, with checksum readback consistent.
- Related-fork `main` remains at its exact pre-publication SHA; no unrelated main drift enters the release.
- PR #15 is closed without merge after publication succeeds.
- No Workstation/live runtime mutation occurs.
- Exact release/tag/checksum evidence is durable for the next JIT D25 consumption Card.

## Required tests / checks

- Candidate branch/PR exact-SHA identity readback immediately before publication.
- Existing `.github/workflows/fork-linux-release.yml` verify/package/Linux AppImage ABI/app-smoke gates.
- GitHub tag/release target readback.
- Release assets/checksums readback.
- Pre/post related-fork `main` SHA equality.
- PR #15 final closed/unmerged readback.

## External write/readback needs

Authorized repository/release writes under approved R7: create exact `release/v5.0.15` branch, allow the existing GitHub release workflow to publish `v5.0.15`, and close PR #15 without merge after GREEN publication. Read back trigger branch, workflow result, tag/release target, assets/checksums, unchanged `main`, and final PR state.

## Independent review

`none` — M01-T09 already placed independent RECOMMENDED review at the last reversible checkpoint and returned GREEN for the exact immutable subject that this Card publishes unchanged. A new review is required only if the publication subject changes.

## Contract overrides

None.
