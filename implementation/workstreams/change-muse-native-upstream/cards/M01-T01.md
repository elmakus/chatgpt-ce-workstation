# M01-T01 — Wire optional Muse upstream in repository configuration

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md#milestone-m01--wire-and-deploy-the-optional-muse-upstream`
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R1, R2, R3, R5
- Accepted decisions: `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`; D10; D16
- Relevant OpenSpec: none
- Accepted dependency results: none

### Must preserve

- Workstation only wires the existing fork capability; do not duplicate provider/model-routing logic.
- `CODEX_CHATGPT_WEB_MUSE_UPSTREAM` remains optional and an unset value preserves current behavior.
- The CLIProxyAPI ingress key must not be committed or stored in Workstation `.env`.
- Existing `CODEX_CHATGPT_WEB_NATIVE_UPSTREAM` behavior and value are not changed by this Card.
- Repository-owned Compose remains the runtime source of truth.

### Must not / rationale that must travel

- Do not add the Muse API key to `compose.yaml`, `.env.example`, tracked scripts or any other repository file.
- Do not alter `codex-chatgpt-web` routing/filtering code from this Workstation repository.

## Dependencies

- none

## Outcome

The Workstation repository reproducibly exposes the optional Muse upstream environment variable to `codex-web-gpt` and documents the non-secret setting without changing existing routes or storing credentials.

## Scope

### Included

- Add `CODEX_CHATGPT_WEB_MUSE_UPSTREAM: ${CODEX_CHATGPT_WEB_MUSE_UPSTREAM:-}` beside the existing native-upstream environment wiring in `compose.yaml`.
- Add the optional non-secret Muse upstream setting to `.env.example`.
- Run repository/static validation appropriate to the changed Compose/example configuration and verify no secret material was introduced.

### Excluded

- Production endpoint activation.
- Reading or writing the CLIProxyAPI ingress key.
- Recreating/restarting the production Workstation.
- Any change to `codex-chatgpt-web` source.

## Acceptance

- `compose.yaml` passes the optional Muse upstream to the Workstation service.
- `.env.example` documents the setting without a credential.
- Unset Muse upstream keeps Compose behavior compatible with the existing configuration.
- Existing native-upstream wiring is unchanged.
- Repository validation is GREEN and the diff contains no secret value.

## Required tests / checks

- `bash scripts/validate-source.sh`
- Compose rendering/configuration validation through the repository-supported path.
- Diff review confirming only the intended non-secret configuration wiring changed.

## Optional execution hints

- Priority: HIGH
- Complexity: LOW
- Phase: repository wiring
- Expected/relevant code locations:
  - `compose.yaml`
  - `.env.example`

## External write/readback needs

Repository writes only. No live production write in this Card.

## Independent review

`none` — the integrated M01 milestone carries the independent review gate after production verification.

## Contract overrides

none
