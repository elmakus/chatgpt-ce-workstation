# M01-T07 — Activate Muse upstream and verify production routing

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` R5 / M01 planned work 4–5
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R1–R6
- Accepted decision: `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
- Accepted predecessor evidence:
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T02-live-activation-blocker.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T03-independent-review.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T04-independent-review.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T05-fork-release.md`
  - `implementation/workstreams/change-muse-native-upstream/evidence/M01-T06-d25-update.md`

### Must preserve

- Production activation is legal only because the running Workstation now embeds exact corrected release `v5.0.14` with package SHA-256 `2c68166425e049241c61ae76eafd42afdde37343e1e887ef977fd43bf636cbf2`.
- Set only the non-secret endpoint `CODEX_CHATGPT_WEB_MUSE_UPSTREAM=http://192.168.2.104:8317/v1` in Workstation local `.env`.
- Keep `CODEX_CHATGPT_WEB_NATIVE_UPSTREAM` unchanged.
- Reuse the already-provisioned persistent `~/.config/codex-web-gpt/muse-proxy-api-key`; verify only metadata/readability and never emit its content.
- Recreate through repository-owned Compose with build inputs rendered from the running image's exact frozen embedded resolution.
- CLIProxyAPI catalog evidence may expose public model IDs/shape but no credential value.
- Preserve ordinary native/Codex-LB and `chatgpt-web/*` behavior while adding only eligible `muse-*` rows.
- Use exact corrected-release source/automated evidence for Muse-catalog failure isolation; do not intentionally break production.
- If activation fails acceptance, unset Muse upstream and recreate the same frozen image/resolution, then verify rollback health/catalog behavior.

### Must not / rationale that must travel

- Do not copy the CLIProxyAPI ingress key into Git, Workstation `.env`, command output, logs, evidence or chat.
- Do not move Codex-LB behind CLIProxyAPI.
- Do not import CLIProxyAPI non-`muse-*` rows.
- Do not add another provider/catalog adapter in Workstation.
- Do not rebuild the image or change the corrected fork in this Card.
- Do not deliberately inject a production failure.

## Dependencies

- `M01-T06`

## Outcome

Production Workstation has the optional Muse endpoint activated against the exact corrected fork release, discovers only available `muse-*` CLIProxyAPI rows alongside ordinary native and browser-backed rows, successfully routes one native request, one Muse request and one `chatgpt-web/*` request through their intended paths, and remains healthy.

## Scope

### Included

- Re-read exact running image/frozen Codex Web GPT identity before activation.
- Recheck Muse key file presence, owner/group, mode and readability without reading it into output.
- Make one secret-safe authenticated direct CLIProxyAPI `GET /v1/models` call, recording response shape, counts and public model IDs only.
- Persist only the non-secret Muse endpoint in local untracked `.env`.
- Render exact frozen Compose environment from the running image's embedded `/opt/workstation/upstream-resolution.json`.
- Recreate the Workstation on the same image through repository-owned Compose.
- Verify post-recreate image identity, health, native/Muse environment readback and key metadata.
- Verify Workstation-side model discovery includes expected available `muse-*` rows, excludes CLIProxyAPI non-Muse rows and retains ordinary native plus `chatgpt-web/*` rows.
- Run one secret-safe ordinary native request smoke, one available Muse request smoke and one browser-backed `chatgpt-web/*` request smoke through the existing Codex Web GPT API surface.
- Bind failure-isolation acceptance to the already independently reviewed exact-release automated/source evidence unless a non-production isolated check is actually needed.
- Run repository runtime verification with the exact frozen resolution environment.
- Persist secret-safe production evidence and rollback readiness.

### Excluded

- Fork/source/release changes.
- Workstation image rebuild.
- CLIProxyAPI provider configuration changes.
- Any credential-content readback.
- Deliberate production failure injection.
- Unrelated runtime/configuration changes.

## Acceptance

- Pre-activation readback proves running image is the GREEN M01-T06 image and embedded fork identity is exact `v5.0.14` with expected checksum.
- Key file exists, is readable by the runtime user and retains restrictive ownership/mode without content disclosure.
- Direct authenticated CLIProxyAPI catalog returns HTTP 200 with OpenAI-compatible top-level `data` rows and public `id` values; available `muse-*` IDs are recorded and non-Muse rows are counted.
- Post-recreate production environment has native upstream unchanged and exact Muse endpoint present.
- Workstation-side model discovery includes expected Muse IDs, excludes CLIProxyAPI non-Muse IDs and retains ordinary native plus `chatgpt-web/*` rows.
- One ordinary native route smoke succeeds through existing Codex-LB.
- One available `muse-*` route smoke succeeds through CLIProxyAPI.
- One browser-backed `chatgpt-web/*` route smoke succeeds.
- Exact corrected-release evidence covers optional-catalog failure isolation without production degradation.
- Workstation remains healthy and `scripts/verify-runtime.sh` returns `WORKSTATION_RUNTIME_GREEN`.
- No secret value is emitted or committed.

## Required tests / checks

- Running image/frozen-resolution identity readback.
- Secret-safe key metadata/readability check.
- Authenticated CLIProxyAPI catalog shape/filtering readback.
- Repository source validation as appropriate for the exact branch state.
- Frozen-resolution Compose recreate.
- Post-recreate environment/image/health readback.
- Workstation model-catalog filtering/discovery check.
- Native, Muse and browser-backed route smokes.
- Exact-release failure-isolation evidence review.
- `scripts/wait-healthy.sh` and `scripts/verify-runtime.sh` using frozen rendered inputs.
- Git secret-hygiene/clean tracked-source readback.

## External write/readback needs

Already authorized by R5: write the non-secret endpoint to target-host local `.env`, recreate the existing production Workstation with the exact frozen resolution, and perform secret-safe runtime/API verification. Read the existing key only as an in-memory credential for authenticated requests; never emit or persist its value.

## Independent review

`none` — M01 carries the RECOMMENDED integrated milestone review after this production-verification evidence is durable.

## Contract overrides

None.
