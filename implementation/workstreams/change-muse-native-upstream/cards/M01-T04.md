# M01-T04 — Integrate the reviewed fork correction and prepare v5.0.14 release candidate

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board resolved by the ChatGPT-only workstream contract. Historical root/default `implementation/TASK_BOARD.yaml` is recovery/migration input only.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` R5 / M01 steps 1–2
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R4, R6
- Accepted decisions: `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
- Relevant OpenSpec: none
- Accepted dependency results:
  - M01-T03 implementation subject `elmakus/codex-chatgpt-web@9ec01b939a08a191082b551bc2d6dc738a0159e2`
  - M01-T03 independent GREEN evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T03-independent-review.md`
  - latest published related-fork release baseline `v5.0.13`

### Must preserve

- The exact M01-T03 reviewed correction must be the behavior integrated to related-fork `main`; do not amend or substitute a different correction subject while merging it.
- Release preparation is publication-only apart from the already-reviewed correction: synchronize the related fork's package/runtime/launcher version to `5.0.14` without changing routing, catalog, auth, proxy, launcher, or other runtime behavior.
- Keep `upstreamLauncherVersion` unchanged unless existing release tooling itself proves otherwise.
- Prepare on a non-release-triggering branch first so the exact candidate can receive independent review before publication.
- Preserve the repository's normal release workflow and checks; do not bypass version-sync, CI/package, AppImage ABI, or release checksum gates.
- No credentials, API keys, bearer tokens, or operator runtime values may enter Git or evidence.

### Must not / rationale that must travel

- Do not publish a GitHub release, create a `v5.0.14` tag, or create/push a `release/v5.0.14` trigger branch in this Card. Publication is deliberately deferred until the exact release candidate is independently GREEN.
- Do not fold unrelated cleanup or formatting churn into the release candidate; exact diff reviewability is part of acceptance.

## Dependencies

- `M01-T03`

## Outcome

The independently GREEN Muse catalog correction is integrated into related-fork `main`, and a narrowly scoped `v5.0.14` release candidate exists on a non-publishing branch/PR with synchronized version metadata and GREEN CI, ready for independent review before release publication.

## Scope

### Included

- Move related-fork PR #11 out of draft if needed and merge it through the normal PR path only while its head remains the reviewed `9ec01b939a08a191082b551bc2d6dc738a0159e2`.
- Verify post-merge `main` contains that reviewed correction.
- Create `release-prep/v5.0.14` from the exact post-merge `main`.
- Change only `package.json`, `launcher/package.json`, and `src/version.ts` from `5.0.13` to `5.0.14`; keep `upstreamLauncherVersion` unchanged.
- Open the release-candidate PR to `main`.
- Verify the candidate diff is version-only and required related-fork CI/package gates are GREEN.
- Persist exact candidate SHA/PR evidence and freeze that SHA as the independent-review subject.

### Excluded

- Creating/pushing `release/v5.0.14`, creating tag `v5.0.14`, or publishing/editing a GitHub release.
- Workstation image resolution/build/promotion.
- Workstation production activation or runtime mutation.
- Any new Muse catalog/routing/auth implementation.

## Acceptance

- PR #11 is merged and its exact reviewed head is contained in related-fork `main`.
- `release-prep/v5.0.14` is based on that post-merge main.
- Candidate diff against main is exactly the synchronized version bump in the three expected files and nothing else.
- `package.json.version`, `launcher/package.json.version`, and `src/version.ts` all identify `5.0.14`; `upstreamLauncherVersion` remains unchanged.
- Candidate PR CI succeeds for the required verify/package/platform matrix.
- No release/tag/release-triggering branch is published by this Card.
- Exact release-candidate SHA and PR are durable evidence for independent review.

## Required tests / checks

- Related-fork PR #11 exact-head readback before merge and post-merge ancestry/readback.
- Exact release-candidate diff/stat readback.
- `scripts/check-version.ts` as part of `bun run verify`.
- Candidate PR CI: actionlint plus macOS/Windows/Ubuntu `bun run verify`, package/app smoke, and Linux AppImage ABI validation.

## Optional execution hints

- Priority: `HIGH`
- Complexity: `MEDIUM`
- Phase: `release candidate`
- Expected/relevant code locations:
  - `package.json`
  - `launcher/package.json`
  - `src/version.ts`
  - `.github/workflows/ci.yml`
  - `.github/workflows/fork-linux-release.yml`

## External write/readback needs

Related-fork PR #11 merge, branch creation and release-candidate PR creation are authorized repository writes under approved R5. Read back exact merged main SHA, candidate SHA/PR, candidate diff and CI. Publication writes are excluded from this Card.

## Independent review

`RECOMMENDED` — this is the last reversible release-candidate checkpoint before the later GitHub release publication gate; review must cover the exact candidate SHA and prove it is only the accepted correction plus synchronized version metadata.

## Contract overrides

None.
