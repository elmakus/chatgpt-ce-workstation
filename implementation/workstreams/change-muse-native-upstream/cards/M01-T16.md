# M01-T16 — Publish the exact independently GREEN v5.0.16 fork release

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R8 / M01 planned work 2.
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R6, R7.
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
- Relevant OpenSpec: none.
- Accepted dependency results:
  - M01-T15 exact independently GREEN release candidate `elmakus/codex-chatgpt-web@d7c9db70cf1d54029c061490d73335146a23ed28`.
  - M01-T15 independent review evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T15-independent-review.md`.
  - existing fork publication contract `.github/workflows/fork-linux-release.yml` at the reviewed candidate.
  - prior accepted v5.0.15 publication pattern from M01-T10.

### Must preserve

- Publish the exact independently GREEN M01-T15 candidate without changing its tree or version metadata.
- Use the existing fork publication path: create `release/v5.0.16` pointing directly at exact reviewed SHA `d7c9db70cf1d54029c061490d73335146a23ed28`; do not add or edit release machinery.
- The published `v5.0.16` tag/release must resolve to that exact reviewed SHA.
- Preserve the existing Fork Linux Release verify/package/Linux AppImage ABI/app-smoke/checksum gates.
- Preserve the reviewed Muse Gmail and web-search normalization exactly; no new source behavior may be introduced during publication.
- Related-fork `main` is independently at `0f77eb00203f93617f7c38a24ad4e1c6e1cc9d3b`; do not rebase/merge its unrelated drift into the release candidate and do not force/reset `main`.
- Preserve the divergent stale `release-prep/v5.0.16@68142a3f1725e052a606f488ac2c525db650a661` untouched.
- No credentials, API keys, bearer tokens or operator runtime values may enter Git or evidence.

### Must not / rationale that must travel

- Do not create a replacement release commit, merge commit or rebased candidate after independent review.
- Do not merge PR #18 into advanced `main` as part of release publication.
- Do not mutate Workstation source/image/runtime, CLIProxyAPI or production Muse endpoint in this Card.
- Do not change the release workflow.

## Dependencies

- `M01-T15`

## Outcome

`elmakus/codex-chatgpt-web` release `v5.0.16` is published from the exact independently GREEN candidate, the existing release checks are GREEN, the release/tag/assets/checksums are read back, and unrelated current-`main` drift remains outside the immutable v5.0.16 publication subject.

## Scope

### Included

- Re-read `release-prep/muse-v5.0.16` / PR #18 and verify its head still equals exact reviewed candidate `d7c9db70cf1d54029c061490d73335146a23ed28`.
- Re-read absence of an existing `v5.0.16` tag and `release/v5.0.16` trigger branch.
- Read back current related-fork `main` and stale `release-prep/v5.0.16` SHA before publication without mutating either.
- Create `release/v5.0.16` directly from exact reviewed candidate SHA.
- Let existing `Fork Linux Release` workflow execute its required gates and publish `v5.0.16`.
- Verify the published tag resolves to the exact reviewed candidate and the release includes the Linux x64 AppImage plus `checksums.txt`.
- Verify published checksum evidence is consistent with the release asset/readback available from GitHub.
- Verify related-fork `main` and stale divergent prep branch remain unchanged.
- After successful publication/readback, close draft PR #18 without merging it.

### Excluded

- Any source/behavior/version change beyond the already-reviewed candidate.
- New release workflow/mechanism changes.
- Rebase/merge/cherry-pick of current `main` drift into v5.0.16.
- Force/reset/fast-forward of related-fork `main`.
- Mutation of stale `release-prep/v5.0.16`.
- Workstation source/image update.
- Production container or CLIProxyAPI mutation.
- Muse endpoint activation.

## Acceptance

- `release/v5.0.16` is created from exact reviewed candidate `d7c9db70cf1d54029c061490d73335146a23ed28`.
- Fork Linux Release publication gates complete GREEN.
- Published `v5.0.16` tag/release resolves to the exact reviewed candidate SHA.
- Release contains the expected Linux x64 AppImage and `checksums.txt`, with checksum readback consistent.
- Related-fork `main` remains at its exact pre-publication SHA and stale `release-prep/v5.0.16` remains unchanged.
- PR #18 is closed without merge after publication succeeds.
- No Workstation/live runtime mutation occurs.
- Exact release/tag/checksum evidence is durable for the next JIT D25 consumption Card.

## Required tests / checks

- Candidate branch/PR exact-SHA identity readback immediately before publication.
- Existing `.github/workflows/fork-linux-release.yml` verify/package/Linux AppImage ABI/app-smoke gates.
- GitHub tag/release target readback.
- Release assets/checksums readback.
- Pre/post related-fork `main` and stale-prep SHA equality.
- PR #18 final closed/unmerged readback.

## External write/readback needs

Authorized repository/release writes under approved R8: create exact `release/v5.0.16` branch, allow the existing GitHub release workflow to publish `v5.0.16`, and close PR #18 without merge after GREEN publication. Read back trigger branch, workflow result, tag/release target, assets/checksums, unchanged `main`/stale prep branch, and final PR state.

## Independent review

`none` — M01-T15 already placed independent RECOMMENDED review at the last reversible checkpoint and returned GREEN for the exact immutable subject this Card publishes unchanged. A new review is required only if the publication subject changes.

## Contract overrides

None.
