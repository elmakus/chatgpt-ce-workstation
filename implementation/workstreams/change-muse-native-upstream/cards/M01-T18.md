# M01-T18 — Activate Muse and verify the complete optional route

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R8 / M01 planned work 4-5.
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R1-R7.
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
- Relevant OpenSpec: none.
- Accepted dependency results:
  - M01-T14 independently GREEN bounded Muse-only web-search normalization.
  - M01-T15/M01-T16 exact reviewed/published fork release `v5.0.16@d7c9db70cf1d54029c061490d73335146a23ed28`.
  - M01-T17 GREEN D25 consumption evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T17-d25-update.md`.
  - promoted Workstation image `sha256:9ede8f1f522a710707acac32d01fd8c4d7671b91912791e299d1c635d964b893`.

### Must preserve

- Activate only `CODEX_CHATGPT_WEB_MUSE_UPSTREAM=http://192.168.2.104:8317/v1`.
- Preserve the existing `CODEX_CHATGPT_WEB_NATIVE_UPSTREAM` unchanged.
- Reuse the already-provisioned persistent Muse key; verify only metadata/readability and never emit its value.
- Recreate Workstation only through repository-owned Compose using the current D25-promoted v5.0.16 image/resolution.
- Preserve the two accepted Muse-only compatibility rules: omit the exact Gmail namespace and omit only `search_content_types` from exact `type: "web_search"` tools while preserving the tool and all other non-Gmail tools/properties.
- Preserve ordinary native/Codex-LB request behavior and browser-backed `chatgpt-web/*` behavior.
- Keep rollback deterministic: on failed final acceptance, unset Muse upstream, recreate the same frozen image/resolution, and verify the known-good Muse-disabled state.

### Must not / rationale that must travel

- Do not mutate CLIProxyAPI production, provider-routing/catalog code, fork source/release state, native upstream, or Muse credential content.
- Do not consume a newer fork release or rebuild a different Workstation image.
- Do not manually delete Codex model cache as normal rollback; R8 requires normal catalog reconciliation.
- Do not widen provider compatibility beyond the two independently evidenced Muse-only exceptions.

## Dependencies

- `M01-T17`

## Outcome

Production Workstation exposes usable `muse-*` models through CLIProxyAPI while ordinary native/Codex-LB and browser-backed routes remain GREEN, with the real Muse turn succeeding under the two accepted Muse-only compatibility normalizations.

## Scope

### Included

- Target-host refresh/readback of clean branch/HEAD, running image, health, current native upstream and Muse-disabled baseline.
- Metadata/readability-only verification of the persistent Muse key.
- Set only `CODEX_CHATGPT_WEB_MUSE_UPSTREAM=http://192.168.2.104:8317/v1` in the repository-owned runtime configuration surface.
- Recreate Workstation using repository-owned Compose without rebuilding/replacing the promoted v5.0.16 image.
- Verify direct CLIProxyAPI catalog health.
- Verify Workstation imports exactly available `muse-*` rows and no CLIProxyAPI non-Muse rows.
- Verify ordinary `gpt-5.6-sol` remains GREEN through Codex-LB.
- Verify a normal Codex client `muse-*` turn succeeds with the real tool surface.
- Capture secret-safe forwarded-request evidence proving Gmail omission, `web_search` preservation, only `search_content_types` omission, and representative other non-Gmail-tool preservation.
- Verify ordinary-native control preserves Gmail and current `web_search` shape.
- Verify browser-backed `chatgpt-web/*`, route status, health, source validation and `WORKSTATION_RUNTIME_GREEN`.
- Roll back to Muse-disabled state if any final acceptance criterion fails.

### Excluded

- CLIProxyAPI mutation.
- Fork source/version/release changes.
- Native-upstream changes.
- Credential rotation or content inspection.
- New provider-compatibility exceptions.
- Unrelated Workstation changes.

## Acceptance

- Workstation runs the exact already-promoted v5.0.16 image and is healthy after activation.
- Muse upstream is exactly `http://192.168.2.104:8317/v1`; native upstream is unchanged.
- Direct CLIProxyAPI catalog is healthy.
- Workstation catalog contains the available `muse-*` rows and excludes CLIProxyAPI non-Muse rows.
- Ordinary `gpt-5.6-sol` remains GREEN through Codex-LB.
- A normal Codex client `muse-*` turn succeeds with the real tool surface.
- Forwarded Muse request evidence is secret-safe and proves: Gmail namespace absent; exact `web_search` tool present; only its `search_content_types` property absent; representative other non-Gmail tools preserved.
- Ordinary-native control proves Gmail and current `web_search` shape remain unchanged.
- Browser-backed `chatgpt-web/*` remains GREEN.
- Source validation, route status, container health and `WORKSTATION_RUNTIME_GREEN` are GREEN.
- No credential value is emitted or committed.
- If acceptance fails, rollback leaves Muse unset, native upstream unchanged, normal catalog reconciliation complete, ordinary native + browser-backed routes GREEN and runtime GREEN.

## Required tests / checks

- Target-host Git clean/branch/HEAD and running-image readback.
- Muse key path metadata/readability check without content.
- `bash scripts/validate-source.sh`.
- Repository-owned Compose config/recreate and post-recreate health check.
- Direct CLIProxyAPI catalog check.
- Workstation catalog route/filter verification.
- Ordinary native `gpt-5.6-sol` functional control.
- Normal Codex client Muse functional turn.
- Secret-safe forwarded-request inspection for Muse normalization and ordinary-native control.
- Browser-backed route smoke.
- `bash scripts/verify-runtime.sh`.
- Post-activation persisted environment/image/health readback.
- Rollback verification if any acceptance criterion fails.

## External write/readback needs

Already authorized by approved R8: set the single Muse upstream endpoint in the repository-owned runtime configuration and recreate Workstation using the existing promoted image. Read back the persisted non-secret endpoint values, running image, route/catalog state, health and functional results. If acceptance fails, unset only the Muse upstream and recreate the same image, then verify rollback.

## Independent review

`none` — the constituent behavioral corrections and release subjects already received independent review; integrated M01 retains the remaining RECOMMENDED milestone review gate after live activation/verification.

## Contract overrides

None.
