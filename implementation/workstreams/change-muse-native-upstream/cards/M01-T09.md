# M01-T09 — Integrate the reviewed Gmail filter and prepare v5.0.15 release candidate

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R7 / M01 planned work 2
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R6, R7
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
- Relevant OpenSpec: none
- Accepted dependency results:
  - M01-T08 implementation subject `elmakus/codex-chatgpt-web@2bd8a74a6f467767c7a640806720e672f7342471`
  - M01-T08 independent GREEN evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T08-independent-review.md`
  - current published related-fork baseline `v5.0.14`
  - related-fork `main@2686d2a616864a1bc9cd975901773ab16a54ba47` before integrating PR #13

### Must preserve

- The exact M01-T08 reviewed Gmail-filter correction must be the behavior integrated to related-fork `main`; do not amend or substitute another correction subject while merging it.
- Release preparation is publication-only apart from the already-reviewed correction: synchronize the related fork's package/runtime/launcher version to `5.0.15` without changing routing, tool filtering, catalog, auth, proxy, launcher, or other runtime behavior.
- Keep `upstreamLauncherVersion` unchanged.
- Prepare on a non-release-triggering branch first so the exact candidate can receive independent review before publication.
- Preserve the repository's normal release workflow and checks; do not bypass version-sync, CI/package, AppImage ABI, or release checksum gates.
- Preserve the accepted Muse-only Gmail exception exactly: only the exact Gmail namespace is omitted on validated `muse-*` Responses requests; non-Gmail Muse tools, ordinary native Gmail and browser-backed behavior remain unchanged.
- No credentials, API keys, bearer tokens, or operator runtime values may enter Git or evidence.

### Must not / rationale that must travel

- Do not publish a GitHub release, create a `v5.0.15` tag, or create/push a `release/v5.0.15` trigger branch in this Card. Publication is deliberately deferred until the exact candidate is independently GREEN.
- Do not fold unrelated cleanup or formatting churn into the release candidate.
- Do not mutate Workstation production, CLIProxyAPI, or Muse activation state in this Card.

## Dependencies

- `M01-T08`

## Outcome

The independently GREEN Gmail-filter correction is integrated into related-fork `main`, and a narrowly scoped `v5.0.15` release candidate exists on a non-publishing branch/PR with synchronized version metadata and GREEN CI, ready for independent review before publication.

## Scope

### Included

- Re-read PR #13 and merge it through the normal PR path only while its head remains exact reviewed subject `2bd8a74a6f467767c7a640806720e672f7342471`.
- Verify post-merge `main` contains that exact reviewed correction.
- Create `release-prep/v5.0.15` from the exact post-merge `main`.
- Change only `package.json`, `launcher/package.json`, and `src/version.ts` from `5.0.14` to `5.0.15`; keep `upstreamLauncherVersion` unchanged.
- Open a release-candidate PR to `main`.
- Verify the candidate diff is version-only and required related-fork CI/package gates are GREEN.
- Persist exact candidate SHA/PR evidence and freeze that SHA as the independent-review subject.

### Excluded

- Creating/pushing `release/v5.0.15`, creating tag `v5.0.15`, or publishing/editing a GitHub release.
- Workstation image resolution/build/promotion.
- Workstation production activation or runtime mutation.
- CLIProxyAPI mutation.
- Any new Muse compatibility behavior beyond the already-reviewed Gmail filter.

## Acceptance

- PR #13 is merged only from exact reviewed head `2bd8a74a6f467767c7a640806720e672f7342471` and the resulting related-fork `main` contains that subject.
- `release-prep/v5.0.15` is based on that post-merge `main`.
- Candidate diff against main is exactly the synchronized version bump in the three expected files and nothing else.
- `package.json.version`, `launcher/package.json.version`, and `src/version.ts` all identify `5.0.15`; `upstreamLauncherVersion` remains unchanged.
- Candidate PR CI succeeds for the required verify/package/platform matrix.
- No release/tag/release-triggering branch is published by this Card.
- Exact release-candidate SHA and PR are durable evidence for independent review.

## Required tests / checks

- Related-fork PR #13 exact-head readback before merge and post-merge ancestry/readback.
- Exact release-candidate diff/stat readback.
- `scripts/check-version.ts` as part of `bun run verify`.
- Candidate PR CI: actionlint plus macOS/Windows/Ubuntu `bun run verify`, package/app smoke, and Linux AppImage ABI validation.

## External write/readback needs

Related-fork PR #13 merge, branch creation and release-candidate PR creation are authorized repository writes under approved R7. Read back exact merged main SHA, candidate SHA/PR, candidate diff and CI. Publication writes are excluded from this Card.

## Independent review

`RECOMMENDED` — this is the last reversible release-candidate checkpoint before the later GitHub release publication gate; review must cover the exact candidate SHA and prove it contains only the accepted reviewed correction plus synchronized version metadata.

## Contract overrides

None.
