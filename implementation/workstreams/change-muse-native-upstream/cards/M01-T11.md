# M01-T11 — Consume v5.0.15 through D25 and promote the Workstation image

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` approved R7 / M01 planned work 3
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R1, R2, R4, R5, R6, R7
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
  - `docs/DECISIONS.md#d30--muse-bound-codex-turns-intentionally-omit-gmail-tools`
  - `docs/DECISIONS.md#d25--workstation-updates-resolve-latest-stable-identities-before-build`
- Accepted dependency results:
  - M01-T10 exact published release evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T10-fork-release.md`
  - exact corrected release `v5.0.15@3a6d1d28c28dbe1be885077ae53fe4bba62b9673`
  - published Linux AppImage SHA-256 `bdad25547ae79f29ee27a029bed5ec6c7762f98a92e7578762de40a113b9b49d`
  - M01-T06 established D25 production baseline and repository-owned update pattern
- Repository-owned update path: `scripts/update.sh`, `scripts/resolve-upstreams.py`, `scripts/render-build-env.py`, `scripts/build.sh`

### Must preserve

- The corrected fork release consumed by the candidate must resolve to exact `v5.0.15@3a6d1d28c28dbe1be885077ae53fe4bba62b9673` with published Linux AppImage SHA-256 `bdad25547ae79f29ee27a029bed5ec6c7762f98a92e7578762de40a113b9b49d`.
- At preparation time `v5.0.15` is the current latest stable related-fork release. Use normal D25 latest-stable resolution with `override: false`; do not force an exact-version override while latest still resolves to the reviewed release.
- If latest-stable changes before the frozen resolution is produced, do not silently consume the newer release and do not promote a candidate that is not bound to exact v5.0.15. Stop affected execution for normal workflow classification rather than weakening the reviewed lineage.
- Use the repository-owned D25 `resolve -> freeze -> build -> validate -> promote` lifecycle; do not manually install or replace Codex Web GPT inside the running container.
- `CODEX_CHATGPT_WEB_MUSE_UPSTREAM` must remain unset throughout this Card.
- Existing `CODEX_CHATGPT_WEB_NATIVE_UPSTREAM` must remain unchanged.
- The already-provisioned Muse key file remains dormant; do not read or emit its content.
- Candidate promotion must retain the normal prior-image rollback baseline and execute repository runtime verification.
- No provider-routing or catalog code is changed in Workstation.

### Must not / rationale that must travel

- Do not activate Muse merely because the corrected image is available; activation and three-route verification remain a later predecessor-dependent obligation.
- Do not bypass the frozen resolution by a raw Compose rebuild/recreate.
- Do not treat a version string alone as capability proof: bind the frozen release identity/checksum to M01-T10 exact release evidence and retain M01-T08/M01-T09 independent source/release-candidate evidence.
- Do not consume the unrelated v5.0.16 candidate or another later release without a separate legal authority/review path.

## Dependencies

- `M01-T10`

## Outcome

Production Workstation runs a D25-validated candidate whose frozen upstream resolution consumes exact fork release `v5.0.15`, while Muse remains disabled and the existing production runtime remains GREEN.

## Scope

### Included

- Read back target-host repository branch/HEAD, clean-tree state, current container/image/health, current native upstream and absence of Muse upstream before mutation.
- Fast-forward the target-host workstream checkout to the current durable branch HEAD when clean.
- Run repository source validation/preflight required by `scripts/update.sh`.
- Resolve and freeze one D25 update candidate.
- Verify the frozen resolution's `codex_web_gpt` component is version `5.0.15`, exact package SHA-256 `bdad25547ae79f29ee27a029bed5ec6c7762f98a92e7578762de40a113b9b49d`, and `override: false`.
- Build, validate and promote through `bash scripts/update.sh`, including normal candidate provenance, health, runtime verification, persistence recreate and rollback-baseline retention.
- Read back resulting update evidence and running image.
- Verify installed Codex Web GPT reports/contains `5.0.15`.
- Verify native upstream remains unchanged and Muse upstream remains unset.
- Keep Workstation health/runtime verification GREEN.

### Excluded

- Setting `CODEX_CHATGPT_WEB_MUSE_UPSTREAM`.
- Functional Muse request.
- CLIProxyAPI catalog/request verification.
- Reading/changing Muse ingress-key content.
- Related-fork source or release changes.
- Consuming v5.0.16 or another later release.
- Any unrelated Workstation update correction unless a concrete bounded failure requires normal corrective routing.

## Acceptance

- Pre-update readback proves clean expected workstream checkout and GREEN production baseline.
- Frozen D25 resolution records Codex Web GPT `5.0.15`, exact published AppImage SHA-256 from M01-T10, and `override: false`.
- `scripts/update.sh` completes with success evidence and normal rollback baseline retained.
- Running container uses the promoted candidate image and is healthy.
- `scripts/verify-runtime.sh` remains GREEN after promotion/persistence recreate.
- Installed Codex Web GPT identity is `5.0.15`.
- Native upstream is unchanged from the pre-update value.
- Muse upstream remains unset.
- No secret value is emitted into durable evidence.

## Required tests / checks

- Target-host Git clean/branch/HEAD readback.
- `bash scripts/validate-source.sh` through the D25 update path.
- Exact frozen-resolution inspection for `codex_web_gpt`.
- D25 candidate provenance/readback gates.
- `bash scripts/wait-healthy.sh`.
- `bash scripts/verify-runtime.sh` including the persistence recreate performed by `scripts/update.sh`.
- Post-update image/version/environment readback without secret content.
- Persisted `.workstation-update/last-update.json` readback.

## External write/readback needs

Already authorized by approved R7: target-host workstream fast-forward when clean, D25 build/promotion/recreate, and associated image/cache retention. Read back Git state, frozen resolution identity, update evidence, container/image health, installed fork version, and only non-secret environment values.

## Independent review

`none` — this Card consumes the exact independently GREEN/released fork subject through the already-accepted D25 deployment lifecycle. Integrated M01 carries the remaining independent review gate after production activation/verification.

## Contract overrides

None.
