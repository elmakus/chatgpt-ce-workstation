# M01-T05 — Publish the independently GREEN v5.0.14 fork release

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MUSE_NATIVE_UPSTREAM_PLAN.md` R5 / M01 planned work 2
- Requirements: `requirements/MUSE_NATIVE_UPSTREAM.md` R2, R4, R6
- Accepted decisions:
  - `docs/DECISIONS.md#d29--muse-is-an-optional-parallel-native-upstream-through-cliproxyapi`
- Relevant OpenSpec: none
- Accepted dependency results:
  - M01-T04 exact independently GREEN release candidate `elmakus/codex-chatgpt-web@2686d2a616864a1bc9cd975901773ab16a54ba47`
  - M01-T04 independent review evidence `implementation/workstreams/change-muse-native-upstream/evidence/M01-T04-independent-review.md`
  - existing fork publication contract `.github/workflows/fork-linux-release.yml`
  - prior accepted fork-publication pattern recorded by historical `implementation/cards/M02-T01.md`

### Must preserve

- Publish the exact independently GREEN M01-T04 candidate without changing its tree or version metadata.
- Use the existing fork publication path: a `release/v5.0.14` branch pointing at the exact reviewed candidate; do not add or edit release machinery.
- The published `v5.0.14` tag/release must resolve to that exact candidate SHA.
- Do not change routing, catalog, auth, proxy, launcher behavior, CI/workflow code, or version metadata during publication.
- Preserve the existing Fork Linux Release verify/package/Linux AppImage ABI/app-smoke/checksum gates.
- Fast-forward fork `main` only after publication is GREEN and only to the exact reviewed candidate SHA.
- No credentials, API keys, bearer tokens, or operator runtime values may enter Git or evidence.

### Must not / rationale that must travel

- Do not create a replacement release commit or merge commit after independent review; doing so would needlessly change the immutable publication subject.
- Do not rebuild or mutate the Workstation in this Card. Workstation D25 consumption is a later JIT obligation whose exact input depends on this release's durable tag/checksum evidence.
- Do not activate `CODEX_CHATGPT_WEB_MUSE_UPSTREAM` in production.

## Dependencies

- `M01-T04`

## Outcome

`elmakus/codex-chatgpt-web` release `v5.0.14` is published from the exact independently GREEN candidate, the fork's release checks are GREEN, the release/tag/assets/checksums are read back, and fork `main` is synchronized to that exact candidate.

## Scope

### Included

- Re-read the exact candidate and verify `release-prep/v5.0.14` still equals `2686d2a616864a1bc9cd975901773ab16a54ba47`.
- Re-read absence of a pre-existing `v5.0.14` tag/release trigger before publication.
- Create `release/v5.0.14` from exact candidate SHA `2686d2a616864a1bc9cd975901773ab16a54ba47`.
- Let the existing `Fork Linux Release` workflow execute its required gates and publish `v5.0.14`.
- Verify the published tag resolves to the exact candidate SHA and the release includes the Linux x64 AppImage plus `checksums.txt`.
- Verify published checksum evidence is consistent with the release asset digest/readback available from GitHub.
- Fast-forward related-fork `main` to the exact candidate SHA only after release publication succeeds.
- Verify final `main` equals the reviewed candidate.
- Verify PR #12 no longer represents an unintegrated delta; if GitHub leaves it open after the exact fast-forward, close it without changing source.

### Excluded

- Any source/behavior/version changes beyond the already-reviewed candidate.
- New release workflow/mechanism changes.
- Workstation source/image update.
- Production container or CLIProxyAPI mutation.
- Muse endpoint activation.

## Acceptance

- `release/v5.0.14` was created from exact reviewed candidate `2686d2a616864a1bc9cd975901773ab16a54ba47`.
- Fork Linux Release publication gates complete GREEN.
- Published `v5.0.14` tag/release resolves to exact candidate SHA.
- Release contains the expected Linux x64 AppImage and `checksums.txt`, with checksum readback consistent.
- Related-fork `main` resolves to exact candidate SHA after publication.
- No source, routing, catalog, auth, proxy, launcher, CI/workflow or version metadata changed during publication.
- No Workstation/live runtime mutation occurred.
- Exact release/tag/checksum evidence is durable for the next JIT D25 consumption Card.

## Required tests / checks

- Candidate branch/SHA identity readback immediately before publication.
- Existing `.github/workflows/fork-linux-release.yml` verify/package/Linux AppImage ABI/app-smoke gates.
- GitHub tag/release target readback.
- Release assets/checksums readback.
- Final related-fork `main` SHA readback.

## External write/readback needs

Authorized repository/release writes under approved R5: create the exact `release/v5.0.14` branch, allow the existing GitHub release workflow to publish `v5.0.14`, then fast-forward fork `main` to the exact reviewed candidate after GREEN publication. Read back branch, workflow result, tag/release target, assets/checksums, final `main`, and PR #12 integration state.

## Independent review

`none` — M01-T04 already placed independent RECOMMENDED review at the last reversible checkpoint and returned GREEN for the exact immutable subject that this Card publishes unchanged. A new review is required only if the publication subject changes.

## Contract overrides

None.
