# Research R1 — Muse request-schema compatibility with native Codex

Research ID: `change-muse-native-upstream-R1`
Status: `active`
Origin role: `execution_resolution`
Origin subject: `M01-T07@4e3b676b6030ece4b332fa5f89a8363e9bd2cc98`
Return target: `execution_resolution:M01-T07@4e3b676b6030ece4b332fa5f89a8363e9bd2cc98`
Research question: `Why do normal native Codex client requests to advertised muse-* models fail with "Recursive JSON schemas are not currently supported" while a minimal Responses request through the same local proxy succeeds, and which component inside the accepted D29 boundary should adapt or reject that request shape?`
Return reconciliation: `pending`
Return reconciliation result: `none`

## Scope

Establish source/runtime evidence only. Do not change CLIProxyAPI provider configuration, fork routing behavior, Workstation image contents, accepted D29 ownership, or production secrets while this Research record is active.

## Known evidence

- Production Workstation remains on exact promoted image `sha256:ce07c414c1442bae243689a17fb32310eed457cb38c52e89394899b9a0289b42`.
- Authorized journal-owned Interrupt hook repair is GREEN; persistent runtime migrated to `codex-chatgpt-web` v5.0.14 and route status has no errors.
- Muse endpoint is activated at `http://192.168.2.104:8317/v1`; native upstream remains unchanged.
- Direct authenticated CLIProxyAPI catalog and Workstation-local model catalog are GREEN. At the latest readback CLIProxyAPI advertised four `muse-*` rows plus 12 non-Muse rows; the local catalog imported exactly the same four Muse rows, retained 12 ordinary native rows and three `chatgpt-web/*` rows, and imported none of the 12 non-Muse CLIProxyAPI IDs.
- Actual native Codex client smoke for `gpt-5.6-sol` is GREEN.
- Actual native Codex client smoke for `chatgpt-web/light` is GREEN.
- Minimal tool-free local Responses request for `muse-spark-1.2` is HTTP 200/completed and returned the expected marker, proving the local proxy -> CLIProxyAPI Muse route and credential path work.
- Normal `codex exec` requests for both `muse-spark-1.3` and `muse-spark-1.2` fail after routing with `invalid_request_error`: `Recursive JSON schemas are not currently supported` on `parameters`.
- A transient `503 backend temporarily overloaded` was observed for one minimal `muse-spark-1.3` request; another advertised Muse model succeeded immediately and this is separate from the deterministic native-client schema failure.
- No credential value was emitted.

## Questions

1. Which exact tool/schema in a normal Codex Responses request is recursive and triggers the Muse backend rejection?
2. Does CLIProxyAPI own/offer an OpenAI-Responses-to-Muse schema transformation that can safely normalize recursive JSON schemas, or is the rejection a provider/backend limitation?
3. Can `codex-chatgpt-web` legally and safely adapt only Muse-bound requests under D29 without degrading Codex-LB or `chatgpt-web/*`, and what semantics would be lost by stripping/flattening unsupported tool schemas?
4. Is there an already-supported Codex request/configuration mode that avoids the incompatible schema while retaining useful Muse operation, rather than adding source changes?
5. Based on the evidence, is the final owner bounded Execution, Strategic Planning, Project Definition, or another Research obligation?

## Exit criteria

Research is complete when evidence identifies the failure mechanism and the smallest correct ownership/strategy for making a normal Codex Muse turn usable, or proves that accepted requirements/plan must explicitly narrow the supported Muse behavior.
