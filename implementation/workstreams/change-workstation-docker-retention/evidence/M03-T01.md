# M03-T01 — Multi-cycle retention lifecycle evidence\n\nIntegrated implementation subject: `e4b0570adc975b6cd18d9e1ccf49bc74cb0a4662`\nM01 checkpoint: `a3f15c251c4a31787249b6bd5fc69a7e97e4a285`\nM02 checkpoint: `a6a580fced3573b3e4a9912a02430ab62c04d153`\n\n## Result before independent review\n\nImplementation/tests: **GREEN**\nIndependent review: **RECOMMENDED / pending**\n\n## Integrated lifecycle proof\n\n`scripts/test-retention-multicycle.sh` sources the real `scripts/update.sh` and replaces only external Docker/BuildKit/runtime operations with deterministic stateful fakes.\n\nThe harness proves:\n- three consecutive successful updates with changing candidate/current/rollback identities;\n- after each of the first three successful cycles, the Workstation lifecycle set is bounded to exactly the current candidate ref plus one immediate rollback ref;\n- older candidate/rollback refs become removable only after the newer candidate has passed runtime verification;\n- a subsequent runtime-failing candidate restores the exact immediately previous production image and runs zero image/cache retention cleanup;\n- a later verified-success cycle with an injected stale-image removal failure remains production-successful and records `image_cleanup.status=warning`;\n- a later verified-success cycle with an injected cache-GC failure remains production-successful and records `build_cache_cleanup.status=warning`;\n- after the cleanup-warning recovery sequence, lifecycle refs return to exactly current + one rollback;\n- a same-repository non-lifecycle ref and a foreign-repository candidate-like ref remain untouched;\n- real updater `write_evidence` JSON is read back for success, rollback and cleanup-warning states;\n- cache evidence records builder `chatgpt-ce-workstation`, driver contract `docker-container`, `max_used_space=24gb` and `reserved_space=8gb`;\n- normal build source remains cache-enabled and explicitly selects the dedicated builder.\n\n## Exact verification\n\nVerification ran on Tower in an isolated temporary checkout at exact branch head `e4b0570adc975b6cd18d9e1ccf49bc74cb0a4662`.\n\nGREEN:\n- shell syntax for the M03 harness and all M01/M02 changed shell files;\n- `bash scripts/test-retention-multicycle.sh` -> `RETENTION_MULTICYCLE_TESTS_GREEN`;\n- `bash scripts/test-buildkit-cache.sh` -> `BUILDKIT_CACHE_TESTS_GREEN`;\n- `bash scripts/test-update-orchestration.sh` -> `UPDATE_ORCHESTRATION_TESTS_GREEN`;\n- `bash scripts/validate-source.sh` -> `SOURCE_VALIDATION_GREEN`;\n- source validation also reran resolver/render fixtures and the existing updater regression suite;\n- `git diff --check a6a580fced3573b3e4a9912a02430ab62c04d153...HEAD` -> GREEN;\n- static scan found no `docker system prune`, `docker builder prune`, `docker buildx use`, or `--no-cache` in the mutation-capable Workstation build/update source.\n\nThe `FAIL:` lines emitted by `test-buildkit-cache.sh` are expected diagnostics from fail-closed negative fixtures; the test itself terminates GREEN.\n\n## Live-write boundary\n\nNo real Workstation builder was created.\nNo candidate image was built.\nNo BuildKit cache was pruned.\nNo image/tag was removed from the live daemon.\nNo production container was recreated or restarted.\nNo live updater invocation was run.\n\nM04 remains blocked behind applicable independent review GREEN plus explicit user live-write/deployment authorization.\n\n## Review subject\n\nThe independent reviewer must review exact integrated subject `e4b0570adc975b6cd18d9e1ccf49bc74cb0a4662` against M03-T01, the approved `workstation-docker-retention-R1` plan, R11-R12/R16-R24, D25/D27, and the M01/M02 accepted evidence. Later evidence/Task Board commits are workflow-state only and must not silently replace the frozen implementation subject.\n