# M04-T01 evidence — current-main integration refresh

Status: implementation GREEN; REQUIRED independent review pending.

Exact implementation subject: `f19c906ee92dbdf966bd1eff3bb125853b876ac8`

Integration target baseline used for refresh: `main@bcd74082aec6e1888d244c6aa647ad76e1d35605`.

## Refresh result

The retention workstream was refreshed against current main after the issue #12 keyring/rollback workstream completed its review, live retry, merge and target-side closure.

The merge had two true content conflicts:
- `docs/DECISIONS.md`: current main already owns D27 for passwordless noVNC. The unchanged retention decision was mechanically renumbered from D27 to D28, and retention-owned authority references were reconciled to D28. Main's D27 remains unchanged.
- `scripts/test-update-orchestration.sh`: retention cleanup assertions/tests were combined with current-main persistence-recreate and keyring rollback tests so neither acceptance surface was dropped.

Current-main changes to `scripts/update.sh` and `scripts/validate-source.sh` merged without unresolved conflict. The resulting updater keeps strict candidate verification plus the issue #12 passwordless-persistence recreate/verification before keyring finalization, then performs workstation-scoped image retention and dedicated-builder cache retention only after the verified success gate.

The M03 multi-cycle harness required one compatibility adapter for the new issue #12 rollback contract: it now fakes `verify_rollback_runtime(expected_image_id)` and asserts the exact restored image identity instead of accidentally invoking the real Compose-based rollback verifier from the deterministic fixture.

## Verification

All verification ran against the combined merge tree before the subject was committed/pushed. No live Docker, BuildKit, workstation container or keyring mutation occurred.

GREEN:
- `bash -n` for the affected updater/build/cache/test/runtime shell surface.
- `scripts/test-retention-multicycle.sh` → `RETENTION_MULTICYCLE_TESTS_GREEN`.
- `scripts/test-buildkit-cache.sh` → `BUILDKIT_CACHE_TESTS_GREEN` (expected negative-fixture diagnostics were exercised).
- `scripts/test-update-orchestration.sh` → `UPDATE_ORCHESTRATION_TESTS_GREEN` (expected fail-closed keyring rollback diagnostics were exercised).
- `scripts/validate-source.sh` → `SOURCE_VALIDATION_GREEN`, including current keyring migration/session/runtime-lock regressions, updater/rollback coverage, Compose validation and noVNC boundary checks.
- Forbidden scan: no global/system/default-builder prune, no global `buildx use`, and no `--no-cache` on the normal workstation build path.
- Refresh diff check against current `origin/main` → GREEN.

The first unscoped merge-index `git diff --check` also reported pre-existing trailing whitespace in `implementation/workstreams/issue-chatgpt-session-auto-refresh/INTAKE.md`, imported unchanged from current main. The accepted refresh check was therefore scoped against `origin/main`, which excludes unchanged target-baseline artifacts and is GREEN. No unrelated main artifact was rewritten to hide that baseline condition.

## Readback

The pushed branch readback matched the exact frozen implementation subject:
`f19c906ee92dbdf966bd1eff3bb125853b876ac8`.

## Review boundary

M04-T01 is REQUIRED independent review because this combined subject composes destructive post-success retention cleanup with the production keyring migration/rollback updater path. M04-T02 live mutation remains blocked until this exact subject receives GREEN independent review.
