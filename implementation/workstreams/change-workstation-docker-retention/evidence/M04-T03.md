# M04-T03 evidence — passwordless keyring preflight correction

Status: implementation GREEN; REQUIRED independent review pending.

Exact implementation subject:
`9c2523a0bd0ae18c43a3a559a05e57e8275aaa77`.

Origin blocker:
`implementation/workstreams/change-workstation-docker-retention/evidence/M04-T02.md`.

## Correction

The M04-T02 live attempt proved that the D26 passwordless steady state is blocked by
`scripts/preflight-host.sh` requiring `keyring-password` to be non-empty.

The correction changes only that stale readiness predicate:
- the migration placeholder must still exist as a regular file;
- empty content is accepted as the intended fresh/post-migration D26 state;
- non-empty legacy migration credentials remain accepted;
- a missing placeholder still fails host preflight.

A deterministic regression fixture,
`scripts/test-preflight-keyring-state.sh`, runs the real preflight script with fake
host commands and temporary paths and proves all three states. Source validation now
runs that fixture and guards against restoring the stale `-s` requirement.

The first implementation write accidentally encoded literal `\\n` sequences in
the preflight block. The new missing-placeholder fixture immediately caught that
the guard had become commented out. That intermediate state was corrected before
the subject was frozen; the final exact subject above contains executable separate
lines and passed the full test set.

## Verification

GREEN against exact subject `9c2523a0bd0ae18c43a3a559a05e57e8275aaa77` on Tower:
- `bash -n scripts/preflight-host.sh scripts/test-preflight-keyring-state.sh scripts/validate-source.sh`;
- `scripts/test-preflight-keyring-state.sh` -> `PREFLIGHT_KEYRING_STATE_TESTS_GREEN`;
- `scripts/test-update-orchestration.sh` -> `UPDATE_ORCHESTRATION_TESTS_GREEN`;
- `scripts/test-retention-multicycle.sh` -> `RETENTION_MULTICYCLE_TESTS_GREEN`;
- `scripts/test-buildkit-cache.sh` -> `BUILDKIT_CACHE_TESTS_GREEN`;
- `scripts/validate-source.sh` -> `SOURCE_VALIDATION_GREEN`;
- `git diff --check f19c906ee92dbdf966bd1eff3bb125853b876ac8..HEAD` -> GREEN;
- static guard confirms `preflight-host.sh` contains the `-f` placeholder check and no longer contains the stale `-s` check.

Read-only target-host validation:
- current frozen resolution was copied from the running workstation image;
- the corrected real `scripts/preflight-host.sh` was executed against the live empty
  `/mnt/user/appdata/chatgpt-ce-workstation/secrets/keyring-password` placeholder;
- result: `HOST_PREFLIGHT_GREEN`.

No production mutation occurred during M04-T03. Post-test readback remained:
- workstation image:
  `sha256:dcf23c733b28b39827b2b6161cea880d3b45151b698ae5a6427bdd397ea1dd5a`;
- container: running, healthy;
- dedicated `chatgpt-ce-workstation` Buildx builder: absent;
- workstation lifecycle candidate/rollback refs: 9, unchanged from the pre-attempt state.

M04-T02 remains blocked until this exact correction subject receives REQUIRED independent review GREEN.
