# M02-T02 — Scoped BuildKit retention implementation evidence\n\nImplementation subject: `a6a580fced3573b3e4a9912a02430ab62c04d153`\nDependency evidence: `implementation/workstreams/change-workstation-docker-retention/evidence/M02-T01.md`\n\n## Result\n\n**GREEN**\n\nM02 now gives Workstation a dedicated Buildx cache namespace and a bounded, builder-scoped post-success cache GC path.\n\nImplemented behavior:\n- new `scripts/buildkit-cache.sh` owns builder/cache policy helpers;\n- default builder name is `chatgpt-ce-workstation`;\n- required builder driver is `docker-container`;\n- builder creation is explicit and does not use `docker buildx use`;\n- an existing same-name builder with any other driver fails closed;\n- `scripts/build.sh` explicitly invokes `docker compose build --builder <exact-name> --pull workstation`;\n- default cache bound is `max-used-space=24gb`, `reserved-space=8gb`;\n- cache GC is exactly builder-scoped and includes internal/frontend records: `docker buildx prune --builder <exact-name> --all --max-used-space <bound> --reserved-space <reserve> --force`;\n- cache GC runs only after promoted-image identity, health and runtime verification, after the existing image/tag retention step;\n- cache GC failure becomes a retention warning and does not trigger rollback or change the verified update result;\n- update evidence records cache-cleanup status, builder, driver contract and finite policy values;\n- `.env.example` documents non-secret overrides for builder name and cache bounds;\n- existing shared `default/docker` cache is never cleaned by this implementation.\n\n## Verification\n\nVerification ran on Tower in an isolated temporary checkout at exact branch head `a6a580fced3573b3e4a9912a02430ab62c04d153`.\n\nGREEN:\n- `bash -n scripts/buildkit-cache.sh`\n- `bash -n scripts/test-buildkit-cache.sh`\n- `bash -n scripts/build.sh`\n- `bash -n scripts/update.sh`\n- `bash -n scripts/test-update-orchestration.sh`\n- `bash scripts/test-buildkit-cache.sh` -> `BUILDKIT_CACHE_TESTS_GREEN`\n- `bash scripts/test-update-orchestration.sh` -> `UPDATE_ORCHESTRATION_TESTS_GREEN`\n- `bash scripts/validate-source.sh` -> `SOURCE_VALIDATION_GREEN`\n- `git diff --check b9d26667232e4455277e9065c0cf0fe75eee2b17...HEAD` -> GREEN\n- prohibited cleanup scan for `docker system prune`, `docker builder prune`, and `docker buildx use` -> GREEN\n\nThe helper tests intentionally emit `FAIL:` diagnostics for negative fixtures (conflicting driver and invalid inputs); the harness terminates `BUILDKIT_CACHE_TESTS_GREEN`, confirming those fail-closed cases behaved as expected.\n\n`shellcheck` was not installed on Tower and was not part of the M02-T02 required test contract.\n\n## Live-write boundary\n\nNo real Workstation builder was created.\nNo candidate build was run.\nNo BuildKit cache was pruned.\nNo image was removed.\nNo production container was recreated or restarted.\nNo production updater invocation was run.\n\nAll Docker/BuildKit operations used for M02-T01 discovery remained read-only; M02-T02 verification used deterministic fakes/static validation for the new mutation-capable paths. The explicit live mutation gate remains M04.\n