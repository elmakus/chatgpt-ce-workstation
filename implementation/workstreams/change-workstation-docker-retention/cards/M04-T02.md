# M04-T02 — Live Tower retention verification

- Milestone: M04
- Dependencies: M04-T01

## Authority slice

- Master Plan: planning/WORKSTATION_DOCKER_RETENTION_MASTER_PLAN.md#Milestone-M04--Live-Unraid-verification
- Requirements: requirements/SMART_UPSTREAM_UPDATES.md#R11, #R12, #R16-#R24
- Accepted decisions: docs/DECISIONS.md#D25, #D26, #D28
- Dependency result: exact M04-T01 combined source subject with GREEN REQUIRED independent review
- User authorization: explicit continuation authorization in the current operator interaction after the M04 live-write/deployment gate was described

## Must preserve

- Production workstation must remain healthy and verifiably recoverable.
- Existing keyring/CE authentication and persistent home/projects/secrets data must remain intact.
- Exactly current plus one immediate workstation rollback lifecycle set is protected after successful promotion.
- Cleanup remains workstation-scoped/reference-safe; unrelated Unraid Docker artifacts are untouched.
- BuildKit retention remains dedicated-builder scoped and preserves useful cache reuse.
- Any cleanup failure is reported separately from a successful update.

## Outcome

Run the accepted combined updater on Tower and prove the M04 live image/cache-retention acceptance surface with write-readback evidence while leaving unrelated Unraid workloads and persistent workstation data unchanged.

## Included scope

- Pre-mutation read-only Docker/BuildKit/workstation state capture.
- One normal successful workstation update using the reviewed combined source.
- Runtime/image/rollback/evidence readback.
- Workstation-scoped stale lifecycle cleanup verification.
- Dedicated workstation BuildKit bounded-GC verification.
- Repeated unchanged-input build/cache-reuse verification.
- Persistent-data invariants and unrelated-artifact readback.

## Excluded scope

- Destructive synthetic failure against production or unrelated workloads.
- Global Docker/image/cache prune.
- Keyring reset/item deletion or CE relogin.
- New behavior changes; any required source correction returns to implementation/review before retry.

## Acceptance

1. Live production is healthy on the exact promoted image and reaches WORKSTATION_RUNTIME_GREEN.
2. Exact previous known-working rollback image remains available.
3. Older workstation-only candidate/rollback lifecycle artifacts are bounded as specified.
4. BuildKit cache policy is active, bounded, and scoped to the dedicated workstation builder.
5. An unchanged-input repeat build still demonstrates useful cache reuse.
6. Unrelated Unraid Docker artifacts are unchanged.
7. Persistent home/projects/secrets/keyring state remains intact.
8. Durable update evidence records protected current/rollback identities plus image/cache cleanup results.

## Required tests/checks

- Pre/post Docker container/image/reference inventory limited to the workstation and comparison set needed to prove unrelated refs unchanged.
- Pre/post dedicated builder/cache readback.
- Normal scripts/update.sh execution.
- scripts/verify-runtime.sh and exact image readback.
- Repeated/no-change build cache-reuse evidence.
- Durable updater evidence readback and persistent-data invariant checks.

## External writes/readback

AUTHORIZED for this Card by the current user instruction after the explicit M04 gate was presented. Every material live write must be followed by persisted-state readback. If source changes become necessary, stop live execution and return to implementation/review.

## Independent review

none — M04-T01 REQUIRED review gates the exact source subject used here; this Card performs deployment/verification only and may not change source behavior.
