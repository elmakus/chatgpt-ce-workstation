# M04-T03 — Reconcile passwordless keyring host preflight

- Milestone: M04
- Dependencies: M04-T01
- Trigger: M04-T02 live attempt 1 blocker evidence

## Authority slice

- Master Plan: planning/WORKSTATION_DOCKER_RETENTION_MASTER_PLAN.md#Milestone-M04--Live-Unraid-verification
- Requirements: requirements/SMART_UPSTREAM_UPDATES.md#R11, #R12, #R16-#R24
- Accepted decisions: docs/DECISIONS.md#D25, #D26, #D28
- Blocker evidence: implementation/workstreams/change-workstation-docker-retention/evidence/M04-T02.md
- Reviewed predecessor source: M04-T01 subject `f19c906ee92dbdf966bd1eff3bb125853b876ac8`

## Must preserve

- D26 empty `keyring-password` placeholder is a valid fresh/post-migration passwordless state.
- A missing keyring migration placeholder remains a host-preflight failure.
- A non-empty legacy migration credential remains accepted for installations that still need one-time migration.
- Runtime migration remains fail-closed and rollback-compatible; do not weaken keyring runtime verification, backup/restore or exact-image rollback.
- No production Docker/BuildKit/container/keyring mutation in this correction Card.
- No retention semantics from M01-M03 are changed.

## Outcome

Remove the stale preflight contradiction that requires the D26 migration placeholder to be non-empty, and add deterministic regression coverage proving empty-present and non-empty-present states pass while a missing placeholder fails.

## Included scope

- `scripts/preflight-host.sh` keyring placeholder readiness check/message.
- A bounded deterministic preflight regression fixture using fake host commands and temporary paths.
- `scripts/validate-source.sh` wiring for that regression.
- Required source tests for the combined updater/retention/keyring surface.

## Excluded scope

- Live workstation update/recreate or retention cleanup.
- Keyring content migration/reset.
- Changes to D26 target behavior.
- Changes to Docker image/cache retention policy.
- Any ad-hoc creation/population of a live keyring password.

## Acceptance

1. Existing empty regular `keyring-password` placeholder passes the keyring host-preflight requirement.
2. Existing non-empty legacy credential also passes.
3. Missing placeholder fails host preflight before mutation.
4. Regression is exercised deterministically by source validation.
5. Existing updater, keyring/rollback, retention and BuildKit tests remain GREEN.
6. No live production state is mutated by this Card.

## Required tests/checks

- `bash -n scripts/preflight-host.sh scripts/test-preflight-keyring-state.sh scripts/validate-source.sh`.
- `scripts/test-preflight-keyring-state.sh`.
- `scripts/test-update-orchestration.sh`.
- `scripts/test-retention-multicycle.sh`.
- `scripts/test-buildkit-cache.sh`.
- `scripts/validate-source.sh`.
- `git diff --check`.
- Static guard that preflight no longer uses `-s` for the D26 migration placeholder.

## External writes/readback

Repository branch writes only. No live Docker/BuildKit/container/keyring mutation.

## Independent review

REQUIRED — this correction changes the host gate immediately before the production updater and must be independently reviewed before M04-T02 live retry.
