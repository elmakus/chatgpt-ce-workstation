# M04-T01 — Refresh retention workstream against current main

- Milestone: M04
- Dependencies: M03-T01

## Authority slice

- Master Plan: planning/WORKSTATION_DOCKER_RETENTION_MASTER_PLAN.md#Milestone-M04--Live-Unraid-verification
- Requirements: requirements/SMART_UPSTREAM_UPDATES.md#R11, #R12, #R16-#R24
- Accepted decisions: docs/DECISIONS.md#D25, #D27
- Integration dependency: current main at refresh time, including the completed issue-keyring-live-migration-rollback workstream and D26 behavior
- Predecessor: M03 checkpoint/evidence and GREEN independent review

## Must preserve

- All reviewed M01-M03 workstation-only image/tag retention and dedicated BuildKit-cache semantics.
- Exact candidate verification, exact-image rollback, failure ordering, and cleanup-only-after-success behavior.
- The integrated issue #12 passwordless-keyring migration, rollback-version compatibility, strict candidate runtime verification, and live-retry fixes now present on main.
- No global/default Docker or BuildKit prune and no --no-cache behavior.
- No live Docker/BuildKit/container/keyring mutation in this Card.

## Outcome

Refresh the retention workstream onto current main, resolve overlapping updater/test/validation changes so both the reviewed retention behavior and the integrated keyring/rollback behavior are preserved, and prove the combined source deterministically before any live M04 mutation.

## Included scope

- Merge current main into this workstream without rewriting accepted history.
- Resolve only integration conflicts required by the accepted M04 refresh.
- Re-run affected updater, retention, BuildKit, keyring/rollback and source-validation tests.
- Freeze the exact combined source subject and evidence for independent review.

## Excluded scope

- Live workstation update/recreate.
- Live Docker image/cache pruning.
- Live keyring mutation.
- New retention/security behavior beyond accepted D25/D26/D27 authority.

## Acceptance

1. Workstream contains current main and the complete retention implementation.
2. Conflict resolution preserves both retention and issue #12 semantics without weakening either.
3. Required deterministic tests and source validation are GREEN.
4. Static checks confirm no global/default prune, no buildx-use side effect, and no --no-cache regression.
5. Exact combined source subject is frozen with durable evidence.
6. No production Docker/BuildKit/keyring state is mutated.

## Required tests/checks

- bash -n for affected shell scripts.
- scripts/test-retention-multicycle.sh.
- scripts/test-buildkit-cache.sh.
- scripts/test-update-orchestration.sh.
- keyring migration/runtime/session/rollback regression tests exercised by current validate-source.
- scripts/validate-source.sh.
- git diff --check across refresh.
- forbidden global/default prune/buildx-use/no-cache scans.

## External writes/readback

Repository branch writes only. Verify exact branch HEAD after each durable integration-state write. No live runtime mutation.

## Independent review

REQUIRED — the refresh composes retention cleanup with the production keyring/rollback updater path; the exact combined subject must be independently reviewed before M04-T02 may mutate production.
