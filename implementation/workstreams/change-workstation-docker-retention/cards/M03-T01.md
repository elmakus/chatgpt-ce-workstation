# M03-T01 — Prove multi-cycle retention lifecycle and freeze pre-live subject\n\n- Milestone: `M03`\n\n> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.\n\n## Authority slice\n\n- Master Plan: `planning/WORKSTATION_DOCKER_RETENTION_MASTER_PLAN.md#Milestone-M03--Multi-cycle-orchestration-and-retention-evidence`\n- Requirements: `requirements/SMART_UPSTREAM_UPDATES.md#R11`, `#R12`, and `#R16` through `#R24`\n- Decisions: `docs/DECISIONS.md#D25`, `docs/DECISIONS.md#D27`\n- M01 checkpoint/handoff: `a3f15c251c4a31787249b6bd5fc69a7e97e4a285`, `implementation/workstreams/change-workstation-docker-retention/handoffs/M01_HANDOFF.md`\n- M02 checkpoint/handoff: `a6a580fced3573b3e4a9912a02430ab62c04d153`, `implementation/workstreams/change-workstation-docker-retention/handoffs/M02_HANDOFF.md`\n\n### Must preserve\n\n- Use the actual integrated updater functions with deterministic external-operation fakes; do not replace the retention logic under test with a parallel model.\n- Model at least three consecutive successful update invocations with changing candidate/current/rollback identities in one stateful harness.\n- After each verified successful cycle, exactly the current candidate ref and one immediate rollback ref are the protected Workstation lifecycle set; older candidate/rollback refs become removable only after the new candidate verifies GREEN.\n- On any pre-promotion/promotion/image-mismatch/health/runtime failure, no image or cache retention cleanup may run.\n- A failed candidate must roll back to the exact retained previous image and verify it GREEN.\n- Image-cleanup and cache-cleanup warning paths occur only after production verification and remain distinct from update failure/rollback.\n- Durable evidence must read back current, rollback, image-cleanup and cache-cleanup state without secrets.\n- Existing identity-driven BuildKit reuse remains enabled; no `--no-cache`/timestamp invalidation and no global/default-builder prune may appear.\n- No live Docker/BuildKit mutation on Tower. M04 remains the explicit live-write gate.\n\n## Dependencies\n\n- M01 and M02 terminal GREEN.\n\n## Outcome\n\nAn integrated deterministic harness proves the accepted retention lifecycle across repeated success, failure/rollback and cleanup-warning paths, and freezes the exact M01-M03 subject for independent pre-live review.\n\n## Scope\n\n### Included\n\n- Add a stateful multi-cycle orchestration test using `scripts/update.sh` with deterministic fakes for Docker/BuildKit/runtime operations.\n- Run at least three sequential successful cycles and inspect lifecycle refs after every cycle.\n- Add a failed-candidate rollback cycle after successful history and prove cleanup is not invoked.\n- Exercise post-success image-cleanup warning and cache-cleanup warning in the integrated lifecycle.\n- Read back real `write_evidence` JSON produced by the updater for the modeled cycles.\n- Add source-validation wiring for the new harness.\n- Run existing resolver/build-input/updater/source regressions and diff/static scope checks.\n\n### Excluded\n\n- Production source redesign unless the integrated harness exposes a correctness defect inside existing accepted authority.\n- Real builder creation, build, prune, image deletion, production recreation, restart or updater invocation on Tower.\n- M04 live verification/deployment.\n\n## Acceptance\n\n- At least three sequential successful cycles complete with `status=success`.\n- After each success the Workstation lifecycle inventory is bounded to exactly current candidate + one rollback ref.\n- Cycle N+1 does not remove N's rollback prerequisite before N+1 health/runtime verification is GREEN.\n- A later candidate runtime/health failure invokes exact rollback to the immediately previous production image and runs zero retention cleanup.\n- A post-success image cleanup failure leaves production successful and evidence reports image warning separately.\n- A post-success cache cleanup failure leaves production successful and evidence reports cache warning separately.\n- Evidence readback contains exact current/previous image identities, rollback ref, bounded image cleanup data and builder-scoped cache policy/result.\n- External/unrelated repo refs and non-lifecycle refs remain untouched in the fake inventory.\n- Existing updater tests and full source validation remain GREEN.\n- Source contains no global image/system/default-builder prune, no global `buildx use`, and no `--no-cache` in the normal candidate path.\n\n## Required tests / checks\n\n- `bash -n` for new/changed shell test files.\n- New multi-cycle harness GREEN.\n- `bash scripts/test-buildkit-cache.sh` GREEN.\n- `bash scripts/test-update-orchestration.sh` GREEN.\n- `bash scripts/validate-source.sh` GREEN.\n- Existing resolver/render fixture tests remain GREEN through source validation.\n- `git diff --check` GREEN.\n- Static forbidden-cleanup/cache-invalidation scan GREEN.\n\n## External write/readback needs\n\nRepository writes only. Test execution may use an isolated temporary checkout on Tower but must fake all mutation-capable Docker/BuildKit/updater operations.\n\n## Independent review\n\nRECOMMENDED — this Card freezes the exact integrated M01-M03 implementation subject that introduces destructive cleanup behavior adjacent to rollback-critical Docker state. The implementing chat must persist `review_state: pending` and stop for a fresh normal ChatGPT review before M04.\n\n## Contract overrides\n\nNone.\n