# M04-T01-C01 — Make Codex Web GPT interrupt-hook ownership compatible with native enabled normalization

- Milestone: `M04`
- Corrective parent: `M04-T01`

> This file is a stable corrective Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m04--production-activation-and-live-fault-injection-verification`
- Requirements: R1, R9, R12, R13, R16
- Accepted decisions: D10, D15, D16, D25
- Live regression evidence: `implementation/workstreams/feature-smart-upstream-updates/evidence/M04-T01-live-regression.md`
- Related source repository: `elmakus/codex-chatgpt-web`

### Must preserve

- Keep the interrupt-hook ownership check fail-closed.
- Accept only a semantically enabled native normalization (`enabled = true`) on the exact managed command hook.
- Continue rejecting `enabled = false`, changed command, timeout, trusted hash/state identity, duplicate managed hooks, reordered ownership and unexpected owned fields.
- Restore/disconnect must remove the complete managed hook, including the tolerated native field, without deleting foreign config.
- Do not manually normalize persistent workstation config as the durable fix.
- The corrected artifact must flow through the existing managed-upstream resolution/build path.

## Dependencies

- `M03-T02`
- live diagnosis from blocked `M04-T01`

## Outcome

Codex Web GPT safely recognizes the current Codex-native `enabled = true` normalization as equivalent managed state, retains strict ownership protection for real mutation, and has an immutable reviewed source subject ready for normal release/update consumption.

## Scope

### Included

- Patch the related fork's interrupt-hook locate/verify/restore logic.
- Add deterministic regression tests for native `enabled = true` normalization.
- Add negative coverage proving `enabled = false` and unexpected owned mutations remain rejected.
- Run the related repository test/typecheck/verification surface relevant to the correction.
- Freeze the exact corrected related-repository source subject for independent review.
- After review GREEN, publish/integrate the corrected artifact through the existing release mechanism as required by the workstation resolver, then return to M04-T01 for production revalidation.

### Excluded

- Broad redesign of hook ownership/journaling.
- Manual persistent-config edits as the accepted fix.
- Changes to unrelated Codex Web GPT routing/tunnel/browser semantics.
- M04 rollback/fault injection before M04-T01 is GREEN.

## Acceptance

- Existing managed-hook tests remain GREEN.
- `enabled = true` inserted by native Codex is accepted by verify and fully removed by restore.
- `enabled = false` remains rejected.
- command/timeout/trust/duplicate/unexpected-owned-state protections remain GREEN.
- Exact corrected source subject is persisted and independently reviewed.
- No production write is performed by this Card before that review.

## Required tests / checks

- targeted `tests/codex-interrupt-hook.test.ts`;
- full related-repository test suite where practical;
- typecheck/verify required by the related repository's own release gate;
- exact source diff review.

## External writes / authorization

Repository branch/PR writes are allowed. Publishing a corrected release and any subsequent production recreate happen only after the corrected source subject passes independent review; the user's existing M04 live-write authorization remains valid for the later workstation revalidation.

## Independent review

`REQUIRED` — this correction changes a fail-closed ownership guard around an executable interrupt hook. The implementing chat must freeze the exact corrected source subject and stop for a fresh independent reviewer before release/publication or production consumption.
