# M04-T01-C02 — Accept native multi_agent normalization without weakening ownership

- Milestone: `M04`

> This file is a stable corrective Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Parent Card: `implementation/workstreams/feature-smart-upstream-updates/cards/M04-T01.md`
- Live finding: `implementation/workstreams/feature-smart-upstream-updates/evidence/M04-T01-live-regression-2.md`
- Master Plan: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m04--production-activation-and-live-fault-injection-verification`
- Requirements: R11-R13, R16
- Accepted decisions: D4, D5, D10, D15, D16, D25
- Related source repository: `elmakus/codex-chatgpt-web`

### Must preserve

- Codex Web GPT must not silently take ownership of a real user edit.
- Compatibility V1 owns the `multi_agent=true` assignment only according to its exact version-10 journal baseline.
- Native normalization may be tolerated only when semantics are unchanged and the resulting assignment is unambiguous.
- Disconnect/uninstall/replacement must remain reversible from the journal baseline.
- No persistent production config may be manually edited as the durable correction.
- Production revalidation remains under the already-granted M04 live-write authorization; it starts only after this corrective subject is independently GREEN.

## Outcome

Codex Web GPT accepts the observed Codex-native comment stripping of its owned Compatibility V1 `multi_agent = true` line while keeping strict ownership/conflict detection and exact restore semantics.

## Included

- Bounded verifier/restore handling for literal `multi_agent = true` as the native-normalized equivalent of the exact managed Compatibility V1 line.
- Deterministic fixtures matching the live journal/config shape.
- Positive verification plus deactivate/uninstall restore coverage.
- Negative coverage for `multi_agent = false`, unexpected commented/changed forms, ambiguity/duplicate ownership, and existing relevant conflict behavior.
- Full related-repository tests/typecheck/verify.

## Excluded

- Broad relaxation of generic managed-feature ownership.
- Changes to Compatibility V1 product semantics.
- Manual mutation of the live persistent Codex config.
- M04 rollback exercise or no-change cache proof.
- Any unrelated Codex Web GPT behavior.

## Acceptance

- Exact live normalization `multi_agent = true` is accepted only for the managed Compatibility V1 path.
- Existing exact managed line remains accepted.
- Restore/deactivate/uninstall removes the normalized owned line when the journal says the previous assignment was absent.
- Restore returns the exact prior user line when the journal says it existed.
- `false`, semantic changes and ambiguous/unexpected ownership still fail closed.
- Related-repository `bun test`, typecheck and `bun run verify` are GREEN.
- Exact immutable implementation subject and evidence are persisted before publication.

## External writes / authorization

Repository and CI writes only until REQUIRED independent review is GREEN. Do not publish a corrected release or re-run production deployment before that review.

## Independent review

`REQUIRED` — the correction changes ownership/restore acceptance for persistent Codex configuration. The implementing chat must freeze the exact related-repository subject as `review_state: pending` and stop for a fresh normal ChatGPT reviewer.
