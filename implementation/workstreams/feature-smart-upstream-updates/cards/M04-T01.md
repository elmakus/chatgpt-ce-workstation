# M04-T01 — Activate accepted updater on production and run post-update regressions

- Milestone: `M04`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m04--production-activation-and-live-fault-injection-verification`
- Requirements: R1, R3, R4, R7, R9, R12, R13, R16
- Accepted decisions: D4, D5, D10, D15, D16, D25
- Dependency result: completed and independently GREEN M03 checkpoint at `ffe39dfcb6ac5584af9410feee5eab5f81b008cc`
- User deployment/live-write authorization: granted explicitly on 2026-09-20 for M04 production recreate/write and controlled rollback testing.

### Must preserve

- Refresh against the current integration target before production mutation.
- Preserve D15 ordering: source validation -> host preflight -> image build before recreate.
- Keep the prior production image identity retained for rollback.
- Do not weaken Compose/runtime isolation or persistence boundaries.
- Do not prune the previous image during M04.

## Dependencies

- `M03-T02`

## Outcome

The accepted smart updater completes one real production update GREEN and the promoted exact candidate passes the required post-recreate runtime/application regressions.

## Scope

### Included

- Target/branch refresh and exact candidate revalidation.
- Capture current production image identity and relevant persistence baseline.
- Run the normal `scripts/update.sh` lifecycle on the target workstation.
- Confirm exact resolved metadata and promoted image identity.
- Verify production health/runtime, mount/isolation invariants and Ubuntu 24.04 family.
- Verify native CE including Android Remote, Codex Web GPT configuration/model routing and Agent Workspace / Computer Use.
- Verify existing persistent home/projects/auth state remains present.

### Excluded

- Deliberate fault injection / rollback exercise.
- Final workstream integration to `main`.
- Any product/architecture change outside accepted M04 authority.

## Acceptance

- Real updater invocation returns GREEN using the accepted lifecycle.
- Exact promoted candidate matches frozen resolved metadata.
- Production health/runtime and isolation verification are GREEN.
- Required D15/D4 application regressions are GREEN.
- Persistence/auth state survives.
- Previous known-working image remains available for M04-T02 rollback testing.

## Required tests / checks

- `bash scripts/validate-source.sh`;
- `bash scripts/preflight-host.sh`;
- normal `bash scripts/update.sh`;
- `bash scripts/wait-healthy.sh`;
- `bash scripts/verify-runtime.sh`;
- targeted native CE/Android Remote, Codex Web GPT and Agent Workspace / Computer Use regression checks.

## External writes / authorization

Production image/container recreate is authorized by the user's explicit 2026-09-20 M04 live-write authorization. Every material write requires immediate readback/verification and durable evidence.

## Independent review

`none` — the production-protection implementation subject was independently reviewed GREEN before this live write in M03. M04-T01 validates the already-reviewed behavior against the real target and does not itself introduce a new implementation subject.
