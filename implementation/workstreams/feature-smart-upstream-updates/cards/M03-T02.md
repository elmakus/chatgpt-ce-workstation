# M03-T02 — Prove M03 failure safety and freeze the production-activation subject

- Milestone: `M03`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m03--safe-promoteverifyrollback-updater`
- Requirements: R1, R2, R9-R16
- Accepted decisions: D4, D5, D10, D15, D16, D25
- Dependency results: independently GREEN M02 checkpoint + completed M03-T01 updater implementation

### Must preserve

- All M03-T01 invariants and the exact M03 plan acceptance surface.
- Automated validation must exercise orchestration through mocks/isolated state only; no real workstation recreate or production image mutation.
- The exact integrated M03 implementation/test subject must be frozen before M04.
- M04 remains behind explicit deployment/live-write authorization even after this Card and its independent review are GREEN.

## Dependencies

- `M03-T01`

## Outcome

The exact integrated M03 updater subject has GREEN automated safety/rollback evidence and is frozen for independent review before any production activation.

## Scope

### Included

- Complete source assertions and isolated orchestration fixtures for M03.
- Prove pre-promotion failure does not call production mutation.
- Prove exact candidate promotion/readback.
- Prove post-promotion health/runtime failure restores and verifies the exact previous image while returning failure.
- Prove rollback failure is separately surfaced.
- Run repository CI/source validation.
- Freeze exact integrated M03 subject and evidence.

### Excluded

- Any real production recreate, live rollback exercise or M04 regression.
- Final workstream integration to `main`.

## Acceptance

- R1/R9-R16 M03-owned automated scenarios are GREEN.
- `scripts/update.sh` no longer uses timestamp/random/global-no-cache freshness.
- Source validation -> host preflight -> build ordering is enforced before mutation.
- Exact candidate and previous-image identities participate in promotion/rollback verification.
- Failure paths never falsely report success.
- Evidence is secret-free and recoverable.
- Production remains untouched.
- Exact integrated implementation/test subject is persisted.

## Required tests / checks

- full updater orchestration fixture suite;
- `bash scripts/validate-source.sh`;
- PR CI including shell/Dockerfile/secret checks.

## External writes / authorization

Repository/CI writes only. Do not execute a real workstation update.

## Independent review

`REQUIRED` — M03 controls an important production external-state protection and rollback boundary. The implementing chat must persist the exact integrated subject as `review_state: pending` and stop for a fresh normal ChatGPT reviewer before M04.
