# M01-T02 — Complete managed-upstream resolution and M01 acceptance

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m01--frozen-upstream-resolution-contract`
- Requirements: `requirements/SMART_UPSTREAM_UPDATES.md#r3--ubuntu-remains-on-the-2404-lts-family` through `#r7--real-upstream-identities-replace-timestamp-only-cache-invalidation`, plus `#r9--candidate-metadata-is-inspectable`, `#r14--expert-overrides-may-exist-but-must-be-explicit`, `#r15--networkupstream-failure-is-fail-closed`, `#r16--validation-covers-freshness-cache-semantics-and-rollback-behavior`
- Accepted decisions: `docs/DECISIONS.md#D5`, `#D6`, `#D10`, `#D11`, `#D15`, `#D16`, `#D25`
- Relevant OpenSpec: none
- Accepted dependency results: exact GREEN result of `M01-T01`

### Must preserve

- Preserve the M01-T01 canonical normalized resolution contract and trust/fail-closed semantics.
- Resolve every remaining in-scope moving upstream to the strongest practical immutable/vendor-verifiable identity before candidate build consumption: Ubuntu packages, Agent Workspace, s6-overlay, Codex Web GPT, Muse Code, Google Chrome stable and Rust stable.
- Preserve existing checksum/authenticity contracts, including Codex Web GPT release checksum verification and disabled runtime self-updaters.
- A later build step must not silently re-resolve a different moving state than the frozen resolution set.
- Static version pins may remain only as explicit recovery/debug/compatibility overrides, not normal latest-stable policy.
- Candidate-resolution metadata remains inspectable and secret-free.
- This Card must not recreate or otherwise mutate production.

### Must not / rationale that must travel

- Do not invent a workstation-specific trust substitute where an upstream already has a signed/checksummed authoritative path.
- Do not promise impossible Docker cache reuse after changed base/dependency inputs.
- If Muse, Ubuntu/Chrome package metadata or another channel cannot meet approved freeze/trust semantics with the available vendor-supported mechanism, open Research rather than silently accepting a weaker moving lookup.

## Dependencies

- `M01-T01`

## Outcome

The canonical resolver covers every in-scope upstream and M01 acceptance is reproducibly GREEN without production mutation.

## Scope

### Included

- Add remaining resolvers for Ubuntu package freshness, Agent Workspace, s6-overlay, Codex Web GPT, Muse Code, Chrome stable and Rust stable.
- Normalize all identities/provenance/overrides into the canonical M01 output.
- Preserve existing upstream checksum/signature paths while resolving exact versions first.
- Add tests proving unchanged state stability, independent component changes, explicit overrides, and fail-closed behavior.
- Verify M01 requirement coverage and source baseline invariants.

### Excluded

- Docker/Compose consumption of the full frozen set and cache-stage restructuring (M02).
- Production promotion/rollback (M03-M04).
- Live fault injection.

## Acceptance

- One resolution run represents all in-scope managed upstreams.
- Same upstream state produces byte-stable normalized content.
- A changed identity for each independently moving component changes only its relevant field(s), except documented shared metadata dependencies.
- Ubuntu remains constrained to 24.04.
- Existing checksum/signature/authenticity paths remain authoritative.
- Explicit overrides are visible.
- Required resolution failures are fail-closed.
- Normal resolution contains no arbitrary timestamp/nonce.
- Automated M01 tests and source validation are GREEN.
- Production has not been recreated or mutated.

## Required tests / checks

- Full resolver fixture/unit suite covering every managed upstream.
- Independent-change tests for each component family.
- Malformed/unavailable metadata and override tests.
- `bash scripts/validate-source.sh`.
- No live workstation recreate.

## Optional execution hints

- Priority: HIGH
- Complexity: HIGH
- Phase: resolver completion
- Expected/relevant code locations:
  - `scripts/`
  - `scripts/build/`
  - resolver fixtures/tests
  - `Dockerfile` / `compose.yaml` only where needed to preserve/validate resolver-facing contracts; actual build consumption belongs to M02

## External write/readback needs

Public upstream metadata reads only. No production/live workstation write is authorized.

## Independent review

`none` — the exact integrated M01-M02 resolver/build subject receives the planned independent review before production activation.

## Contract overrides

None.
