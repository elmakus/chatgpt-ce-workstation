# M04-T02 — Exercise bounded live rollback and prove stable no-change updates

- Milestone: `M04`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m04--production-activation-and-live-fault-injection-verification`
- Requirements: R1, R7, R11-R13, R16
- Accepted decisions: D4, D5, D10, D15, D16, D25
- Dependency result: M04-T01 successful production activation and regression evidence.
- User deployment/live-write authorization: granted explicitly on 2026-09-20 for M04 production recreate/write and controlled rollback testing.

### Must preserve

- Fault injection must be bounded and must not corrupt persistent home/projects/secrets.
- Prove the previous-image rollback path and reverify the restored state.
- Restore/confirm the intended accepted candidate after rollback testing and rerun required health/runtime/application checks.
- Keep timestamp-only invalidation absent and verify repeated/no-change update stability/cache behavior.
- A failed candidate is never accepted merely because rollback succeeds.

## Dependencies

- `M04-T01`

## Outcome

The real target proves deterministic bounded rollback, restoration of the accepted candidate and stable repeated/no-change smart-update behavior.

## Scope

### Included

- Record the exact current candidate and retained previous-image identities.
- Trigger one bounded post-promotion failure that exercises the real rollback path without damaging persistent data.
- Verify rollback reaches the exact prior image and returns required failure semantics.
- Restore/confirm the accepted candidate and rerun health/runtime plus D15/D4 regressions.
- Run a subsequent normal/no-change update/resolution cycle and verify no timestamp-only rebuild trigger and practical cache reuse/stability.
- Persist exact live evidence needed for M04 acceptance.

### Excluded

- Destructive persistent-data fault injection.
- Pruning rollback images before M04 acceptance.
- Final workstream integration to `main`.
- Any implementation change unless a live defect requires bounded correction through the normal workflow.

## Acceptance

- Controlled live failure invokes rollback and verifies the exact prior image.
- Persistent user state survives rollback and restoration.
- Intended accepted candidate is restored and fully reverified.
- Repeated/no-change resolution is stable and does not rebuild solely because time passed; cache behavior is evidenced.
- M04 acceptance surface is fully evidenced with no hidden live blocker.

## Required tests / checks

- bounded rollback/failure run with persisted evidence;
- post-rollback `wait-healthy.sh` + `verify-runtime.sh`;
- accepted-candidate restoration/readback;
- repeated D15/D4 application regression checks;
- subsequent no-change updater/resolution/cache verification.

## External writes / authorization

Controlled production fault injection, rollback recreate and accepted-candidate restoration are authorized by the user's explicit 2026-09-20 M04 live-write authorization. Use WRITE -> READBACK -> VERIFY -> EVIDENCE for every material transition.

## Independent review

`none` — this Card is live acceptance of the previously independently reviewed rollback implementation, not a new implementation subject. The distinct workstream final-integration review remains manifest-owned after M04.
