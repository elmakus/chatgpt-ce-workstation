# MF-T01 independent review — RED

Date: 2026-09-20
Workstream: `issue-keyring-live-migration-rollback`
Card: `MF-T01`
Reviewed immutable subject: `d89b2eb3ae4469349ed39d6bdb6d27e8236efc36`
Review type: REQUIRED independent Card-completion review

## Verdict

RED.

## Blocking finding

### R1 — migration artifacts are retired before the required restart/recreate persistence proof

Issue #12 acceptance requires a restart/recreate to prove that the migrated login collection remains usable without the legacy password before the legacy migration credential/backup are cleaned up.

On the reviewed subject, `scripts/update.sh` performs:

1. candidate recreate;
2. health check;
3. strict `scripts/verify-runtime.sh`;
4. immediate `finalize_keyring_passwordless_migration`, which zeroes the legacy keyring credential and deletes `keyrings.pre-passwordless-v2`.

There is no second restart/recreate between the successful migration/runtime check and cleanup.

The current strict runtime verifier proves only the live session state (v2 marker, converged aliases, unlocked collection, ownership and session isolation). It does not prove that the empty master password persists across a fresh Secret Service/container session without staging the legacy credential.

This leaves the exact persistence failure class that issue #12 requires the repair to rule out insufficiently covered before the rollback credential and backup are destroyed.

## Required correction

Within the existing MF-T01 authority, keep the v2 backup and legacy credential until the promoted exact candidate has been recreated once after successful migration, comes back healthy, and passes the strict current runtime verifier again without staging the legacy migration credential.

If that persistence-proof recreate/health/runtime verification fails, use the existing deterministic rollback path and restore the pre-attempt keyring backup before returning to the retained previous image.

Add deterministic updater-orchestration regression coverage for the successful second recreate and its failure paths. Do not weaken candidate verification or change D26.

## Other reviewed areas

The reviewed subject otherwise preserves the bounded issue #12 scope: v2 backup-before-ownership-repair, stale-v1 bypass, login/default alias convergence, explicit old-to-empty password change even for transiently unlocked collections, preservation of additional keyring files, unconditional candidate marker cleanup on rollback, and the separate exact-image rollback baseline verifier.

GitHub CI run 35505509221 / CI #260 is GREEN on the reviewed subject for source-validation, secret-scan and dockerfile-check. The RED verdict is an acceptance/cleanup-order defect not detected by those checks.

## Corrective route classification

Bounded L1/L2 implementation correction inside accepted MF-T01 / D8 / D14 / D26 / R11-R12 authority. No Definition or strategic-plan change is required.
