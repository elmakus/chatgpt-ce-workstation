# MF-T01 independent review attempt 2 — RED

Date: 2026-09-20
Workstream: `issue-keyring-live-migration-rollback`
Card: `MF-T01`
Reviewed immutable subject: `35d1b9dd91287c2953c56824f91716a77c59afd7`
Review type: REQUIRED independent Card-completion review

## Verdict

RED.

## What passed

The source correction for the prior RED finding is present on the exact reviewed subject:

- `scripts/update.sh` performs a second exact-candidate recreate after the first successful health + strict runtime verification and before `finalize_keyring_passwordless_migration`;
- the recreated container is read back against the exact candidate image, health-checked, and passed through the strict current runtime verifier again;
- any persistence-recreate/image/health/runtime failure enters the existing rollback path while the v2 backup and legacy migration credential are still retained;
- deterministic updater tests cover success and all four persistence-proof failure paths;
- the rollback-only verifier remains separate from strict candidate verification.

GitHub CI run `35506152500` / CI #267 is GREEN on the exact immutable subject for source-validation (including the updater orchestration regressions), ShellCheck, secret-scan, and dockerfile-check.

## Blocking finding

### R1 — issue #12 still requires real production-keyring-copy reproduction, but durable evidence says it was not performed

The MF-T01 authority slice explicitly binds `issue #12 acceptance`. Issue #12 acceptance requires:

- a real production-keyring copy to reproduce the migration failure before the fix; and
- the corrected migration to prove the passwordless state against that production-representative state without deleting items or forcing CE relogin.

Durable Research `implementation/workstreams/issue-keyring-live-migration-rollback/research/R1.md` states that the exact-byte disposable-copy command was **not executed** because the remote execution safeguard blocked the mutation-oriented probe. R1 instead established the failure mechanism from read-only production metadata/D-Bus evidence and required a deterministic synthetic fixture.

The reviewed subject provides those synthetic fixtures, but no later durable evidence demonstrates the acceptance-required reproduction against a disposable copy of the real production keyring. CI cannot satisfy that external-state evidence requirement.

Because the Task Card binds issue #12 acceptance and a Task Card cannot silently weaken its own bound authority, this exact subject cannot be accepted GREEN yet.

## Corrective-route classification

This is a **missing-evidence** gap, not a demonstrated source defect and not a Definition/plan change.

Create an implementation-owned Research obligation that:

1. uses a disposable copy of the real Tower keyring state without mutating the live keyring;
2. reproduces the pre-fix failure mechanism against that copy under an isolated HOME/D-Bus/session context, without exposing secret values;
3. exercises the corrected v2 migration against an equivalent disposable copy and verifies item preservation, login/default alias convergence, restart/recreate-persistent empty-master-password behavior, and no CE relogin/reset;
4. records only non-secret evidence sufficient to reconcile the exact MF-T01 acceptance surface.

Production update/retry remains prohibited until a later REQUIRED independent review is GREEN and the operator explicitly authorizes the live retry.
