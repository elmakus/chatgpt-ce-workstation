# MF-T01 independent review attempt 5 — GREEN

Date: 2026-09-20
Workstream: `issue-keyring-live-migration-rollback`
Card: `MF-T01`
Reviewed immutable subject: `8191cbbebe120b842f77b767979ee4f2202ed636`
Review type: REQUIRED independent Card-completion review

## Verdict

GREEN.

No blocking defect was found against the MF-T01 contract, issue #12 acceptance, smart-updater R11/R12, or accepted D8/D14/D26 authority.

## Exact subject and independence boundary

The reviewed implementation subject is exactly `8191cbbebe120b842f77b767979ee4f2202ed636`.

Post-subject commits through the review start changed only the selected Task Board and the fail-closed correction evidence; no behavioral/code file changed after the immutable subject. The review therefore judges the exact implementation/result recorded for MF-T01 rather than the later review-state bookkeeping commits.

## Prior RED correction

Review attempt 4 identified that `restore_keyring_migration_backup` could continue through safety-critical failures because it was called from an `if ! ...` condition while relying on shell errexit semantics, and it explicitly ignored workstation-stop failure.

The corrected subject now fails closed:

- a malformed non-directory rollback-backup path returns failure;
- when a v2 backup exists, `docker compose stop workstation` must succeed before persistent keyring mutation;
- candidate v1/v2 marker cleanup is checked explicitly;
- candidate keyring removal is checked explicitly;
- publication of the pre-migration backup into the active keyring path is checked explicitly;
- any restore failure returns non-zero to `rollback_after_failure`, which records `keyring_restore_failed:<reason>` recovery evidence and does not recreate the retained image as though restore had succeeded.

Deterministic regression coverage includes both stop failure and filesystem restore failure, with the pre-attempt backup left unconsumed and no retained-image recreate after a keyring-restore failure.

## Acceptance / authority review

The exact subject preserves the previously reviewed GREEN behavior:

- v2 preparation creates the first whole-keyring rollback snapshot through sibling staging and publishes it only after a complete copy, before ownership repair;
- interrupted/failed initial backup cannot become the canonical rollback snapshot and retry starts from still-unmutated active state;
- existing canonical backup is never overwritten;
- the root-owned legacy keyring becomes readable by the canonical codex desktop only after backup;
- migration selects the canonical/item-rich login collection, performs explicit old-to-empty master-password migration even when transiently unlocked, preserves additional collections, and proves natural `login` + writable `default` alias convergence;
- v2 success marker is written only after the migrated collection is unlocked and aliases converge;
- strict candidate verification still requires the v2 marker, alias convergence, unlocked canonical collection, codex-owned keyring files, and the existing fail-closed D-Bus/runtime isolation invariants;
- rollback clears candidate markers, restores the v2 backup before recreating the prior image, and uses the separate version-stable exact-image rollback verifier rather than weakening candidate verification;
- the persistence-proof second candidate recreate still occurs before legacy credential/backup cleanup.

The production-derived disposable-copy evidence remains GREEN and proved preservation of the production-shaped collection/item state without mutating the live production keyring.

## Verification evidence

Implementation evidence records GREEN checks on the corrected source, including:

- updater orchestration regression suite;
- keyring migration preparation regression suite;
- keyring helper tests: 6/6;
- full source validation;
- `git diff --check`;
- shell syntax checks on affected scripts;
- Dockerfile/buildx static check.

The previously missing exact-subject hosted CI requirement is now satisfied. GitHub Actions run `35509724696` / CI #282 is associated with exact commit `8191cbbebe120b842f77b767979ee4f2202ed636` and completed successfully. Its `source-validation`, `dockerfile-check`, and `secret-scan` jobs all concluded `success`, including ShellCheck and the noVNC desktop workarea test.

## External-state boundary

No production updater run, candidate promotion, live keyring mutation, credential cleanup, or container recreation was performed for this reviewed subject.

MF-T01 Acceptance 10 remains a later explicit gate: the Tower retry requires operator authorization after this GREEN review and must reach `WORKSTATION_RUNTIME_GREEN` before migration credential/backup cleanup is accepted.

The current PR target has moved and PR #13 is conflict-marked. That does not invalidate this Card-level verdict on the immutable implementation subject; the qualified micro-fix Close route must perform the required current-target integration refresh before final-integration coverage/reuse or merge.
