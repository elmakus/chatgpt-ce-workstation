# MF-T01 independent review attempt 6 — GREEN

Date: 2026-09-20
Workstream: `issue-keyring-live-migration-rollback`
Card: `MF-T01`
Reviewed immutable subject: `789fc358139a73a8bed8aaa38813157853df8b75`
Review type: REQUIRED independent Card-completion review

## Verdict

GREEN.

No blocking defect was found against the MF-T01 contract, issue #12 acceptance, smart-updater R11/R12, or accepted D8/D14/D26 authority for the reviewed source subject.

## Exact subject and independence boundary

The reviewed implementation subject is exactly `789fc358139a73a8bed8aaa38813157853df8b75`.

Branch commits after that immutable subject modify only selected Task Board state and durable evidence. No behavioral/code file changed after the subject, so later review bookkeeping does not alter the reviewed implementation.

The prior independently GREEN subject `8191cbbebe120b842f77b767979ee4f2202ed636` already covered the v2 migration, fail-closed rollback restoration and version-stable rollback verifier. The correction from that subject to `789fc358...` changes only the newly discovered per-session readiness boundary plus its regression/source-validation wiring; the underlying migration/rollback implementation remains unchanged.

## Readiness-race correction review

The production retry evidence shows that Docker health could become GREEN after Openbox/x11vnc/websockify were up but before the canonical GNOME Secret Service helper completed migration/unlock, allowing strict runtime verification to observe the login collection during a transient locked interval.

The corrected subject closes that race without weakening candidate verification:

- `desktop-session-inner.sh` clears the session readiness marker before starting the desktop substrate;
- it starts the canonical GNOME Secret Service and runs the existing passwordless helper;
- it removes the staged migration credential before readiness publication;
- it publishes `/run/workstation/keyring-session-ready` only after helper success and with mode 0600;
- `workstation-healthcheck` requires that marker in addition to the existing desktop/VNC substrate checks;
- because the marker is session-local and cleared at each desktop-service incarnation, a persistent v2 migration marker cannot incorrectly stand in for readiness of a later Secret Service session.

Failure semantics remain fail-closed: with `set -Eeuo pipefail`, helper failure, staged-credential removal failure or readiness publication failure prevents the marker from being established and therefore prevents Docker health from advancing to GREEN for the updater.

## Acceptance / authority review

The exact subject preserves the already-reviewed MF-T01 behavior required by D8/D14/D26 and R11/R12:

- persistent keyring state is backed up before ownership/content repair and existing collections/items are preserved;
- the canonical login collection is migrated explicitly to an empty master password and login/default aliases converge;
- stale v1 state cannot suppress v2 migration;
- strict candidate runtime verification still requires the v2 marker, alias convergence, an unlocked canonical collection, ownership and D-Bus/session isolation invariants;
- rollback clears candidate markers, restores the pre-attempt v2 snapshot before prior-image recreation, and verifies the retained older image with the version-stable exact-image baseline verifier rather than weakening candidate verification;
- the second candidate recreate/persistence proof remains before legacy migration credential/backup cleanup.

The readiness change is therefore a bounded implementation correction within the existing authority and does not introduce a new product/security decision.

## Regression and CI evidence

Independent readback of GitHub Actions run `35511110522` / CI #283 confirms success on the exact reviewed subject. Its `source-validation`, `dockerfile-check`, and `secret-scan` jobs all concluded successfully.

The source-validation log on the exact subject includes:

- Python helper suite: 6 tests, GREEN;
- `KEYRING_MIGRATION_PREP_TESTS_GREEN`;
- `KEYRING_SESSION_READINESS_TESTS_GREEN`;
- `UPDATE_ORCHESTRATION_TESTS_GREEN`;
- `SOURCE_VALIDATION_GREEN`;
- ShellCheck and noVNC desktop workarea checks GREEN.

The readiness regression specifically proves ordering of stale-marker clearing before substrate startup, helper completion before readiness publication, application launch after readiness publication, health failure without the session marker, and health success after the marker is published.

## External-state boundary

No second Tower candidate promotion or live keyring mutation occurred for `789fc358...`.

The previous explicitly authorized retry of `8191cbb...` failed strict candidate verification and then restored the exact prior image with `ROLLBACK_RUNTIME_GREEN`; that live failure is the evidence that motivated this bounded readiness correction.

MF-T01 Acceptance 10 remains an explicit post-review live-write gate. This GREEN verdict authorizes continuation to the normal execution route, but it does not itself authorize a new Tower retry. A new explicit operator authorization is still required, and the retry must reach `WORKSTATION_RUNTIME_GREEN` before legacy migration credential/backup cleanup is accepted.
