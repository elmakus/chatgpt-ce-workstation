# MF-T01 independent review attempt 7 — GREEN

Date: 2026-09-20
Workstream: `issue-keyring-live-migration-rollback`
Card: `MF-T01`
Reviewed immutable subject: `5b9afc564e6c2795f4fdd50df2453f3e47dc8b80`
Review type: REQUIRED independent Card-completion review

## Verdict

GREEN.

No blocking defect was found in the exact reviewed source subject against the MF-T01 contract, issue #12 acceptance surface, smart-updater R11/R12, or accepted D8/D14/D26 authority.

This verdict covers the reversible source checkpoint before the explicitly gated production retry. MF-T01 Acceptance 10 remains intentionally outstanding until a separately authorized Tower retry reaches `WORKSTATION_RUNTIME_GREEN`; this review does not authorize that live write and does not make the Card terminal by itself.

## Exact subject / freshness

The Task Board freezes `5b9afc564e6c2795f4fdd50df2453f3e47dc8b80` as the review subject.

Git comparison proves that the subject is one no-tree-change CI checkpoint above corrected behavior tree `ef677de83dd143da7d3d934f8c66426467313cd3` (zero changed files). Commits that advanced the workstream branch after the frozen subject changed only Task Board / Research / durable evidence state; they did not change reviewed code behavior.

## Authority and prior evidence checked

Reviewed against:
- `implementation/workstreams/issue-keyring-live-migration-rollback/cards/MF-T01.md`;
- issue #12 acceptance;
- `requirements/SMART_UPSTREAM_UPDATES.md#R11` and `#R12`;
- `docs/DECISIONS.md#D8`, `#D14`, `#D26`;
- Research R1/R2/R3 and the current correction evidence;
- exact source at the frozen subject.

R2's production-derived disposable-copy evidence is material: it reproduces the pre-fix failure class, then proves the corrected v2 migration preserves the four-item canonical login collection, preserves the three persistent collections, converges login/default aliases, remains unlocked/passwordless across a fresh isolated session without the legacy credential, creates an exact pre-attempt backup, and leaves live production state unchanged.

## Current correction review

R3 proved the two latest live failures were strict-verifier false negatives: Tower's `gdbus Properties.Get` representation for an unlocked collection is `(<false>,)`, while the verifier incorrectly grepped for `boolean false`.

The reviewed correction keeps the semantic requirement unchanged and fail-closed:
- `scripts/check-keyring-unlocked.sh` queries the exact `org.freedesktop.Secret.Collection.Locked` property using `dbus-send --session --print-reply`;
- it accepts only output containing typed `boolean false`;
- locked `boolean true`, malformed reply shape and invalid session-bus input fail;
- `scripts/verify-runtime.sh` delegates only the lock predicate to this helper and retains the existing D26 marker, alias-convergence, ownership and D-Bus/session-isolation checks.

No migration, rollback or security invariant is weakened by the parser correction.

## Migration / rollback invariants rechecked on the exact source

The frozen source retains the previously corrected MF-T01 behavior:
- v2 preparation publishes a complete rollback snapshot through a staging directory before ownership repair and never overwrites an existing published backup;
- root-owned legacy keyring state is made readable by the canonical codex desktop Secret Service only after backup;
- migration uses the retained legacy credential for explicit old-to-empty master-password change even when the collection is transiently unlocked;
- GNOME's natural reserved `login` alias is preserved/proved, writable `default` is converged to the same canonical collection, and both aliases are verified;
- the v2 completion marker is written only after the canonical collection is confirmed unlocked;
- failed promotion clears v1/v2 markers, restores the pre-attempt v2 snapshot before prior-image recreation, and fails closed if stop/remove/restore operations fail;
- rollback verification is bound to the exact retained previous image plus version-stable health/mount/isolation invariants and does not apply candidate-only D26 invariants to older images;
- promoted candidates still use the full strict current runtime verifier;
- successful migration cleanup occurs only after the second candidate recreate, health check and strict runtime verification, preserving the rollback backup and legacy credential until persistence has been proved.

## Exact verification evidence

GitHub Actions run `35512664624` / CI #284 is associated with exact subject `5b9afc564e6c2795f4fdd50df2453f3e47dc8b80` and concluded `success`.

Its jobs are GREEN:
- `source-validation`;
- `dockerfile-check`;
- `secret-scan`.

Independent job-log readback confirms:
- Python passwordless helper suite: 6 tests, GREEN;
- `KEYRING_MIGRATION_PREP_TESTS_GREEN`;
- `KEYRING_SESSION_READINESS_TESTS_GREEN`;
- `KEYRING_RUNTIME_LOCK_CHECK_TESTS_GREEN`;
- `SOURCE_VALIDATION_GREEN`.

The current lock-check regression explicitly verifies unlocked=false passes, locked=true fails, malformed output fails, and an invalid session-bus address fails.

## External-state boundary

No production candidate promotion or keyring mutation occurred after the lock-verifier correction.

The prior live authorization was consumed by the earlier failed-and-safely-rolled-back retry. A new explicit operator authorization is required before another Tower retry. The retry must reach `WORKSTATION_RUNTIME_GREEN` before legacy credential / migration-backup cleanup is accepted.
