# Research R2 — production-keyring-copy acceptance evidence

Research ID: `issue-keyring-live-migration-rollback-R2`
Status: `blocked`
Origin role: `other`
Origin subject: `review:MF-T01@35d1b9dd91287c2953c56824f91716a77c59afd7; evidence=implementation/workstreams/issue-keyring-live-migration-rollback/evidence/MF_T01_REVIEW_2_2026-09-20.md`
Return target: `execution_resolution:MF-T01@35d1b9dd91287c2953c56824f91716a77c59afd7`
Research question: `Can a disposable exact copy of the real Tower persistent keyring reproduce the pre-fix migration failure and then prove the corrected v2 migration preserves items, converges login/default aliases, and remains passwordless across a fresh isolated Secret Service/container-equivalent session without mutating live production state or exposing secret values?`
Return reconciliation: `pending`
Return reconciliation result: `none`

## Scope

Evidence-only investigation for the RED review gap. Do not mutate the live production keyring, clear/replace the live migration credential, rerun/promote the workstation updater, delete live keyring files, or expose secret values.

A temporary/disposable copy of the real persistent keyring may be created on the target host solely for isolated reproduction. Any temporary process/session must use an isolated HOME/runtime D-Bus context and must not point at the live persistent keyring directory.

## Required evidence

1. Establish an exact disposable copy derived from the current real Tower keyring directory while leaving the live directory byte-for-byte untouched.
2. Demonstrate the pre-fix failure class against the disposable production-derived state, using the pre-fix helper semantics or an equivalent faithful reproduction.
3. Demonstrate the corrected v2 migration against a fresh equivalent disposable copy:
   - all pre-existing keyring files/items preserved;
   - canonical login collection selected/migrated;
   - both `login` and `default` aliases converge;
   - empty-master-password state survives a fresh isolated Secret Service/session restart without the legacy migration credential.
4. Record non-secret metadata only (file hashes/counts/ownership, collection paths/labels/item counts, lock/alias state, exit status). Never print secret item values or credential bytes.
5. If the exact-copy experiment cannot be run safely with available host/runtime tooling, record the concrete blocker and smallest required remedy.

## Attempt 2026-09-20

Tower is online and ordinary non-secret remote command execution works. The target host still has both the known-working rollback image `sha256:ea264b43f32482b8edd9a6012f0c28a38bab4fd27f1db092ad6ed11d67d27852` and the failed candidate image `sha256:1e552c66f416b729c48c41fa7bea6fd43ef2125c9734d7682626ffe60762d758`, so an isolated reproduction would otherwise be technically possible.

The reviewed source subject was fetched into the Tower repository object store without changing the working tree.

The first command that attempted to create a temporary local copy derived from the live keyring directory was blocked by the OpenAI remote-execution safeguard before execution. No disposable keyring copy was created, no keyring bytes or secret values were read into chat, and no live production keyring/container state was changed.

A simpler non-secret Tower probe and Docker/image readback succeeded, which narrows the blocker specifically to the required operation involving the live keyring material rather than general Tower connectivity.

## Blocker

The exact acceptance-required production-keyring-copy experiment cannot be performed from this chat because the available remote execution surface blocks the operation before it can copy/use the live keyring material, even when the intended destination is an isolated temporary directory and the test would not expose secret values.

Do not bypass this safeguard by switching to a generic file-read/download path or by reading keyring bytes into model context.

### Smallest required remedy

Provide the missing production-copy evidence through a user-run/local trusted test on Tower (or another approved execution surface that is explicitly permitted to handle the keyring bytes locally without exposing them to chat). The evidence returned to the repository/chat should contain only non-secret metadata/results needed by the Required evidence section.

After that evidence exists, set this record to `Status: complete` and return to `execution_resolution:MF-T01@35d1b9dd91287c2953c56824f91716a77c59afd7`.

## Exit / return

When sufficient evidence is durable, keep `Status: complete` and return to `execution_resolution:MF-T01@35d1b9dd91287c2953c56824f91716a77c59afd7` for classification against the current Card/review state. Research does not itself change the reviewed subject or issue acceptance.
