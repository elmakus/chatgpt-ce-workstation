# M10-T09 — resume-side workspace reservation correction

Date: 2026-09-19
Card: `implementation/workstreams/feature-muse-worker-orchestration/cards/M10-T09.md`
Result: **GREEN**

## Exact subject

- Repository: `elmakus/codex_workflow`
- Candidate branch: `impl/m10-stateful-muse-lifecycle`
- Base review subject: `74a7588e5bab0a5b7a89549c4309486dc9e7ba4f`
- Corrected exact result subject: `3a5d98b3c21fc37a527c3aa8eb0ed771e6428acb`
- The candidate branch is exactly two corrective commits ahead of the reviewed base and changes only:
  - `codex_workflow/runtime/muse_sessions.py`
  - `scripts/test_muse_adapter.py`
- Remote blob readback matched the exact locally tested blobs:
  - `muse_sessions.py`: `e6f27899930463c60b39c3d03359a4cb3a54d842`
  - `test_muse_adapter.py`: `2a8d64b51b0dbd77693d2ae81c4608f393ad009a`

## Correction

The resume path now validates that the retained worker is in a resumable `ready|needs_probe` state and persists the session record as `reserved` while still holding the registry guard, before acquiring the per-session flock.

This closes the independently reproduced window where two different retained sessions bound to the same canonical workspace could both pass registry validation and later become `active` concurrently.

A deterministic regression now pauses one retained A1 resume after durable workspace reservation but before its session lock, then attempts A2 resume against the same workspace. A2 fails closed with `session_busy`; after release, A1 resumes normally.

## Verification

On a disposable checkout whose two modified blobs exactly match the final remote subject:

- `scripts/test_muse_adapter.py`: **28/28 GREEN**, including the new resume-side pre-lock reservation regression and the existing creator-reservation regression;
- `scripts/test_workflow_runtime.py`: **90/90 GREEN**;
- `scripts/test_muse_profile.py`: **7/7 GREEN**;
- Python compile validation: **GREEN**;
- `git diff --check`: **GREEN**;
- release package build: **GREEN**;
- package verify: **GREEN**;
- ZIP archive integrity test: **GREEN**.

The initial package-build attempt correctly rejected transient `__pycache__` generated by compile validation. Those disposable generated caches were removed and the clean-source package build/verify then passed; no project/source content was changed by that remediation.

## Boundary

No release/tag/main merge or production runtime promotion was performed. The installed production runtime remains outside T09 scope.

M10-T10 must now bind the required live Meta-backed lifecycle/interruption/isolation evidence to exact subject `3a5d98b3c21fc37a527c3aa8eb0ed771e6428acb` before the next REQUIRED milestone review is frozen.
