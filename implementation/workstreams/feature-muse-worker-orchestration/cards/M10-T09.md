# M10-T09 — Reserve resumed Muse workspace before session-lock acquisition

- Milestone: `M10`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#milestone-m10--add-stateful-muse-logical-worker-lifecycle-and-fail-closed-recovery`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R7-R9, R13-R15, R17 plus preserved R1/R2/R4/R6/R10-R12 regression boundaries
- Accepted decision: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-stateful-worker-codex_workflow-profile`
- Independent RED review: `implementation/workstreams/feature-muse-worker-orchestration/evidence/reviews/m10-reservation-race-corrected-stateful-muse-runtime-independent-review-2026-09-19.md`
- Reviewed baseline subject: `elmakus/codex_workflow@74a7588e5bab0a5b7a89549c4309486dc9e7ba4f`
- Prior live evidence: `implementation/workstreams/feature-muse-worker-orchestration/evidence/M10_T08_LIVE_RESERVATION_RACE_CORRECTION_REVALIDATION_2026-09-19.md`

### Must preserve

- Workspace/session acquisition remains fail-closed and process-safe.
- A resumed retained session must reserve its canonical workspace before the registry guard is released for session-lock acquisition.
- Two different retained logical sessions bound to the same workspace must not both cross the pre-lock window and become active concurrently.
- Existing new-worker `reserved` semantics, creator cleanup, per-session flock behavior, stale-`active` and `cleanup_unconfirmed` quarantine behavior remain intact.
- Normal ready/needs-probe resume, binding checks, explicit replacement identity, structured results/artifacts, Tester no-repair, lane isolation and non-`muse-max` profiles remain unchanged.
- Production `codex_workflow:main`, release metadata and installed runtime remain untouched.

## Dependencies

- M10-T08 done.
- M10 REQUIRED independent review RED on exact subject `74a7588e5bab0a5b7a89549c4309486dc9e7ba4f`.

## Outcome

A new immutable `codex_workflow` candidate closes the resume-side pre-lock workspace race by making retained-session reservation atomic before per-session lock acquisition.

## Scope

### Included

- Bounded `runtime/muse_sessions.py` correction for resume-side workspace reservation.
- Deterministic regression for two retained sessions sharing one workspace when one resume is paused before its session lock.
- Any minimal test-only support needed for that regression.
- Existing M10 lifecycle/profile/runtime regression verification.

### Excluded

- Project Workflow policy/state changes.
- New public recovery API or lifecycle redesign.
- Companion semantics.
- Release/version bump, publication, merge to `codex_workflow:main`, or production promotion.
- Meta-backed revalidation, which is M10-T10.

## Acceptance

- A resuming retained worker durably enters a workspace-reserving state before the registry guard is released.
- While that resume is paused before session-lock acquisition, resume of another retained logical worker bound to the same canonical workspace fails closed with `session_busy`.
- Releasing the first resume allows it to acquire its session lease and proceed normally.
- The existing new-worker reservation regression remains GREEN.
- Existing stale-active, cleanup-unconfirmed, unavailable/binding/replacement, same-session lease and confirmed-cleanup resume regressions remain GREEN.
- Full workflow-runtime, Muse-adapter and profile suites plus compile/package/archive verification and `git diff --check` are GREEN.
- Exact remote corrected head is recorded for M10-T10; no unauthorized main/release/production write occurs.

## Required tests / checks

- New deterministic resume-side pre-lock reservation interleaving regression.
- Existing creator pre-lock reservation regression.
- Existing same-session lease, stale-active persistence-failure and cleanup-unconfirmed regressions.
- Existing successful resume/binding/replacement/concurrency regressions.
- Full `scripts/test_workflow_runtime.py`.
- Full `scripts/test_muse_adapter.py`.
- Full `scripts/test_muse_profile.py`.
- Python compile, package validation/build/archive verification and `git diff --check`.
- Exact branch/base/head readback.

## External write/readback needs

Writes are limited to `elmakus/codex_workflow:impl/m10-stateful-muse-lifecycle` plus bounded workstation evidence/state. Read back exact remote head after correction. No production release/promotion is authorized.

## Independent review

`none` at Card level. The corrected exact M10 subject must complete M10-T10 live revalidation and then be frozen for the existing **REQUIRED milestone-level independent implementation review**.
