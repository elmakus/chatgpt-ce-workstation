# M10-T05 — Fail closed on stale active Muse session state after unreconciled adapter exit

- Milestone: `M10`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#milestone-m10--add-stateful-muse-logical-worker-lifecycle-and-fail-closed-recovery`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R7, R9, R13-R15, R17 plus preserved R1/R2/R4/R6/R10-R12 regression boundaries
- Accepted decision: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-stateful-worker-codex_workflow-profile`
- Independent RED review: `implementation/workstreams/feature-muse-worker-orchestration/evidence/reviews/m10-corrected-stateful-muse-runtime-independent-review-2026-09-19.md`
- Corrected baseline subject: `elmakus/codex_workflow@b67785486ba5e2e996b8c6feaf1a163816a49d47`
- Prior correction/live evidence: `implementation/workstreams/feature-muse-worker-orchestration/evidence/M10_T03_CLEANUP_CONFIRMATION_CORRECTION_2026-09-19.md`, `implementation/workstreams/feature-muse-worker-orchestration/evidence/M10_T04_LIVE_CORRECTED_STATEFUL_MUSE_REVALIDATION_2026-09-19.md`

### Must preserve

- Confirmed timeout/cancel cleanup still allows the existing exact-session probe/resume path.
- `cleanup_unconfirmed` remains a durable workspace quarantine and blocks both same-worker reuse and replacement in that workspace.
- A stale durable `active` record whose process-local lease is no longer held must not be treated as an ordinary resumable session or allow another logical worker to reuse the same workspace until explicit reconciliation.
- Normal `ready` session reuse, stable session versus per-turn invocation identity, binding, one-active-turn locking, explicit replacement identity, Tester no-repair, structured result/artifact isolation and lane isolation remain unchanged.
- `codex_workflow` remains caller-policy agnostic.
- Production `codex_workflow:main`, release metadata and installed `~/.codex/codex_workflow` remain untouched.

## Dependencies

- M10-T04 done.
- M10 REQUIRED independent review RED on exact subject `b67785486ba5e2e996b8c6feaf1a163816a49d47`, with the persistence-failure/stale-active defect classified as bounded L1/L2 implementation correction.

## Outcome

A new immutable `codex_workflow` candidate keeps the affected workspace fail-closed across adapter-process exit even when persisting `cleanup_unconfirmed` fails and leaves a stale durable `active` record.

## Scope

### Included

- Bounded session-acquisition/reconciliation correction for stale/unreconciled `active` records.
- Workspace-level fail-closed behavior preventing same-worker resume or replacement-worker reuse while that stale active state is unresolved.
- Deterministic regression simulating a released process-local lease with stale durable `active` state, corresponding to adapter exit after terminal-state persistence failure.
- Existing successful cleanup/resume, cleanup-unconfirmed quarantine, binding/locking, explicit replacement, concurrency and profile regressions.

### Excluded

- Project Workflow policy/state changes.
- New public session-recovery API or strategic lifecycle redesign.
- Companion semantics.
- Release/version bump, publication, merge to `codex_workflow:main`, or production promotion.
- Live Meta-backed revalidation, which is M10-T06.

## Acceptance

- A stale/unreconciled `active` session record cannot be resumed as a normal retained worker after its process-local lease is gone.
- The same stale `active` record prevents a different logical worker from starting in that canonical workspace until explicit reconciliation/retirement.
- Existing live/fixture successful cleanup path still transitions to `needs_probe` and can safely resume the exact retained session.
- Existing `cleanup_unconfirmed` quarantine remains fail-closed.
- Existing one-active-turn/session behavior remains fail-closed.
- Existing normal `ready` session reuse remains GREEN.
- Runtime suite, Muse adapter suite, profile suite, compile/package validation/build/archive verification and `git diff --check` are GREEN.
- The corrected exact remote branch head is recorded for M10-T06 and no unauthorized main/release/production write occurs.

## Required tests / checks

- Focused stale-`active` unlocked-session/workspace regression.
- Existing cleanup-unconfirmed quarantine regression.
- Existing successful timeout/cancellation descendant-cleanup and resume regressions.
- Existing session binding/busy/unavailable/replacement regressions.
- Full `scripts/test_workflow_runtime.py`.
- Full `scripts/test_muse_adapter.py`.
- Full `scripts/test_muse_profile.py`.
- Python compile, package validation/build/archive verification and `git diff --check`.
- Exact branch/base/head readback.

## External write/readback needs

Writes are limited to `elmakus/codex_workflow:impl/m10-stateful-muse-lifecycle` plus bounded workstation evidence/state. Read back the exact remote branch head after the corrective commit. No production release/promotion is authorized.

## Independent review

`none` at Card level. The corrected exact M10 subject must complete M10-T06 live revalidation and then be frozen for the existing **REQUIRED milestone-level independent implementation review**.
