# M11-T04 — Publish exact candidate with identity-preserving provenance

- Milestone: `M11`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m11--cut-validate-publish-and-promote-the-stateful-muse-release`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` — R1, R17, R18
- Accepted decisions: `docs/DECISIONS.md` — D21, D22
- Relevant OpenSpec: none
- Accepted dependency results: exact candidate review GREEN from M11-T02 and exact-candidate live validation GREEN from M11-T03.

### Must preserve

- Immediately refresh current `codex_workflow:main` and target state before publication.
- Publication is legal only when exact candidate remains fast-forwardable from current `main` and no behavioral conflict/drift exists.
- Advance `main` to the exact candidate with no merge/squash/rebase identity change.
- Normal VERSION-triggered owner release workflow must publish tag/assets from that same exact commit.
- Verify tag/release target, workflow success, package assets/checksums and exact source provenance.

### Must not / rationale that must travel

- Do not force-push or synthesize another release commit.
- Do not publish if target drift breaks exact fast-forward lineage.
- Do not mutate workstation production in this Card.
- Publication is a material external write and cannot start without explicit execution-time user authorization for this exact M11 publication action.

## Dependencies

- M11-T03 GREEN.
- Explicit user authorization for M11 publication/main advance.

## Outcome

`codex_workflow:main`, the new release tag and published assets resolve to the exact independently reviewed/live-validated candidate.

## Scope

### Included

- Final target refresh.
- Identity-preserving fast-forward of `main`.
- Normal release workflow.
- Tag/release/assets/checksum/workflow provenance readback.

### Excluded

- Any source change.
- Workstation production update.

## Acceptance

- Exact candidate SHA is the resulting `main` commit.
- Release/tag provenance resolves to the same SHA.
- Release workflow is GREEN.
- Expected ZIP and SHA256SUMS verify.
- No production workstation mutation occurred.

## Required tests / checks

- Fresh compare/ancestry immediately before write.
- Post-write `main` SHA readback.
- Release workflow status and artifact readback.
- Tag/release target and checksum verification.

## External write/readback needs

**Explicit user authorization required before execution.** Material external writes: advance `elmakus/codex_workflow:main` and publish its exact tag/release. Persist exact post-write readback.

## Independent review

`none`; source was independently reviewed before live validation and this Card must not change it.

## Contract overrides

None.
