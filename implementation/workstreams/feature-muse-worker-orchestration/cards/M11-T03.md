# M11-T03 — Live-validate the exact reviewed release candidate

- Milestone: `M11`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m11--cut-validate-publish-and-promote-the-stateful-muse-release`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` — R1, R3-R18
- Accepted decisions: `docs/DECISIONS.md` — D21, D22
- Relevant OpenSpec: none
- Accepted dependency results: M11-T02 exact candidate with REQUIRED independent review GREEN; M10 exhaustive live evidence.

### Must preserve

- Run against the exact independently GREEN candidate without source mutation.
- M10 exhaustive interrupted-turn/fail-closed evidence is reusable only because behavioral blobs remain unchanged.
- Re-prove bounded stateful behavior on the exact candidate: stable session reuse across two turns with distinct invocation identities; distinct Executor/Tester identities; lane isolation; clean process/artifact termination.
- Validation remains isolated and must not mutate the installed production runtime.

### Must not / rationale that must travel

- Do not treat fixture evidence as live candidate proof.
- Do not publish or update workstation production in this Card.
- Any behavioral incompatibility or required source correction invalidates the release-only path and requires a new implementation/review/revalidation subject.

## Dependencies

- M11-T02 complete only after REQUIRED independent review GREEN.

## Outcome

Bounded live Muse validation is GREEN on the exact reviewed candidate with secret-safe evidence and no production mutation.

## Scope

### Included

- Disposable/exact-candidate runtime validation using installed Muse.
- Same logical Muse worker reused across two bounded turns with distinct invocation IDs.
- Distinct Executor and Tester logical/session identities.
- One safe non-overlapping two-lane batch or equivalent accepted lane-isolation smoke.
- Process/artifact cleanup/readback and production non-mutation proof.

### Excluded

- Repeating exhaustive M10 fault injection when behavioral blobs are unchanged.
- Publication/main/tag/release.
- Workstation production update.

## Acceptance

- All bounded exact-candidate live checks are GREEN.
- Candidate source identity matches M11-T02 reviewed subject.
- No residual candidate process tree remains.
- Production runtime/version remains unchanged.
- Evidence is bounded and contains no raw secret/session material.

## Required tests / checks

- Exact subject/package identity before and after validation.
- Live two-turn session reuse with unique invocation IDs.
- Executor/Tester identity separation.
- Lane-isolation smoke.
- Residual-process and production-version/hash readback.

## External write/readback needs

Authorized isolated release-candidate validation only; disposable runtime/session/workspace artifacts plus durable project evidence. No production mutation.

## Independent review

`none`; the exact source candidate already requires GREEN independent review in M11-T02, and this Card adds live evidence without changing that subject.

## Contract overrides

None.
