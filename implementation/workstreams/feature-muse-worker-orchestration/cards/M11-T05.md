# M11-T05 — Promote exact published release and prove production acceptance

- Milestone: `M11`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m11--cut-validate-publish-and-promote-the-stateful-muse-release`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` — R1, R3-R18
- Accepted decisions: `docs/DECISIONS.md` — D21, D22
- Relevant OpenSpec: none
- Accepted dependency results:
  - M11-T04 exact published release provenance
  - M09 rollback baseline `v1.1.17-private.11` / `4081cde7...`

### Must preserve

- Install only the exact M11-T04 published release through the existing owner update path.
- Keep verified `.11` rollback package/provenance available until all production acceptance is GREEN.
- Active profile stays `muse-max`.
- Companion stays internal GPT-5.6 Luna XHigh; six non-Companion roles stay Muse Spark 1.3 Contributor/max.
- `plus`, `luna-xhigh`, `pro-x5` allocations/lifecycle remain unchanged; verify by authority/readback without switching the user's active profile.
- Prove installed stateful Muse session reuse with stable session identity and a new invocation identity.
- On installation/smoke failure, do not mark M11 complete; restore the prior verified release or equivalent verified transactional backup and read back version/profile/runtime state before another attempt.

### Must not / rationale that must travel

- Do not install from an unreviewed branch, moving ref or different release commit.
- Do not accept version-only readback without exact package/source provenance and stateful runtime smoke.
- Do not leave the workstation on a failed/partially verified candidate when rollback is required.
- Production mutation is a separate material live-write gate.

## Dependencies

- M11-T04 publication GREEN with exact provenance.
- Explicit user authorization for M11 workstation production promotion.

## Outcome

The workstation production runtime is the exact published M11 release and passes stateful `muse-max` plus profile-isolation acceptance, after which the old `.11` baseline no longer needs to remain the active rollback target.

## Scope

### Included

- Exact published-release update via owner path.
- Installed version/package/source provenance readback.
- Active-profile and role-allocation readback.
- One retained Muse logical session reused on a later bounded turn with distinct invocation identity.
- Other-profile authority regression without active-profile switching.
- Residual process cleanup.
- Rollback/readback if promotion or smoke fails.

### Excluded

- New runtime/source behavior changes.
- Project Workflow policy/state implementation in `codex_workflow`.

## Acceptance

- Installed package/source identity equals exact M11-T04 release candidate.
- Production active profile is `muse-max`.
- Companion and six Muse role allocations are correct.
- Installed runtime demonstrates stateful session reuse with a new invocation identity.
- `plus`, `luna-xhigh`, `pro-x5` remain unchanged.
- No residual candidate/validation processes remain.
- M11 can be marked GREEN only after all readbacks/smoke checks are GREEN.

## Required tests / checks

- Pre-update rollback readiness and current-production readback.
- Post-update exact release/package/source identity.
- Active `muse-max` role allocation.
- Installed two-turn stateful session smoke.
- Non-Muse profile allocation/lifecycle readback without switching active profile.
- Residual process check.
- Rollback and post-rollback readback on failure.

## External write/readback needs

**Explicit user authorization required before execution.** Material live write: update persistent workstation `~/.codex/codex_workflow` production runtime. Persist exact installation and smoke/readback evidence.

## Independent review

`none`; no source mutation is permitted after the exact release-candidate review. Any source/runtime correction routes to a new implementation/review subject.

## Contract overrides

None.
