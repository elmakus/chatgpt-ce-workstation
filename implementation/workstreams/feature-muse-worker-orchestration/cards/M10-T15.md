# M10-T15 — Enforce process-safe nested-workspace isolation

- Milestone: `M10`

> Stable Task Card contract. Mutable state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#milestone-m10--add-stateful-muse-logical-worker-lifecycle-and-fail-closed-recovery`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R7-R15, R17 plus R1/R2/R4/R6/R10-R12 regression boundaries
- Accepted decision: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-stateful-worker-codex_workflow-profile`
- RED review: `implementation/workstreams/feature-muse-worker-orchestration/evidence/reviews/m10-nested-workspace-isolation-independent-review-2026-09-19.md`
- Reviewed baseline: `elmakus/codex_workflow@ad62ffa515026714a42a610c0310b1f6e92f47bd`

### Must preserve

- Workspace identity remains canonicalized by the adapter before session acquisition.
- Process-safe registry/lease ordering and activation-persistence reconciliation remain intact.
- Same logical-worker resume, caller-scope binding, one-active-turn locking, stale-active/quarantine fail-closed behavior and explicit replacement semantics remain intact.
- Managed batch overlap validation remains unchanged or stronger.
- No Project Workflow state semantics enter `codex_workflow`.
- Production release/promotion remains out of scope.

## Outcome

The durable Muse session-acquisition boundary rejects equal or nested active/reserved/quarantined workspace conflicts across independent adapter callers, not only inside one managed batch helper.

## Included scope

- Minimal `runtime/muse_sessions.py` workspace-conflict correction.
- Deterministic exact-subject regressions for parent-active -> child-attempt and child-active -> parent-attempt outside `execute_workers_concurrently(...)`.
- Full runtime/profile/adapter/package regression.

## Excluded scope

- Live Meta lifecycle validation; T16 owns it.
- Release, tag, main merge or production promotion.
- Broader scheduler or policy changes.

## Acceptance

- With a parent workspace held by an unreconciled/active logical session, nested child acquisition fails closed with `session_busy`.
- With a nested child workspace held, parent acquisition fails closed with `session_busy`.
- Non-overlapping workspaces remain independently acquirable.
- Existing M10 reservation, activation, resume, lock, quarantine and normalization regressions remain GREEN.
- Full exact-subject runtime/profile/adapter/package verification is GREEN.

## Required tests / checks

- New direct session-acquisition regressions in both nesting directions.
- `scripts/test_muse_adapter.py`.
- `scripts/test_workflow_runtime.py`.
- `scripts/test_muse_profile.py`.
- Python compile validation and `git diff --check`.
- Release package build, verify and ZIP integrity.

## External write/readback needs

Source writes are limited to `elmakus/codex_workflow:impl/m10-stateful-muse-lifecycle` plus bounded project evidence/state. Production is untouched.

## Independent review

`none` at Card level. T16 must revalidate the corrected exact subject live and freeze the next REQUIRED M10 milestone review attempt.
