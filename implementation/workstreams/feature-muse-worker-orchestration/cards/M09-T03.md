# M09-T03 — Close the deferred Luna XHigh Companion final acceptance gate

- Milestone: `M09`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m09--add-bounded-lane-concurrency-and-promote-the-complete-runtime` (approved R5)
- Requirements: `requirements/MUSE_MAX_RUNTIME.md#r3--companion-is-persistent-luna-xhigh`
- Accepted decisions: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-codex_workflow-profile`
- Relevant OpenSpec: none
- Accepted dependency results: `M09-T05`, `M09-T06`, `M09-T07`, `M09-T08`, `planning/reviews/R5.md`

### Must preserve

- The Companion remains exactly one persistent internal Codex GPT-5.6 Luna worker at XHigh reasoning per workflow session.
- No substitute model, lower reasoning level, Muse-routed Companion, or quota/runtime rejection can satisfy the gate.
- Under approved R5 sequencing, this Card is a hard final M09/project-completion gate, not a pre-publication/pre-promotion gate.
- Execution Prep has now bound the exact published/promoted production subject: `elmakus/codex_workflow@4081cde7d4f71bcc2083c62cf69b7e3b845eefb8`, release `v1.1.17-private.11`, with production promotion evidence `implementation/workstreams/feature-muse-worker-orchestration/evidence/M09_T08_PRODUCTION_PROMOTION_2026-09-18.md`.
- A Companion defect on the promoted release keeps M09 non-terminal and returns through normal correction/review/release handling.

### Must not / rationale that must travel

- Do not block release-candidate preparation, its independent review, exact-candidate live two-lane validation, publication, or production promotion solely because Luna XHigh quota is unavailable.
- Do not execute this Card against source checkpoint `f660376...` as a substitute for the exact promoted release.

## Dependencies

- `M09-T05` exact R5 release candidate done with REQUIRED independent review GREEN.
- `M09-T06` exact-subject live two-lane GREEN.
- `M09-T07` exact release publication GREEN.
- `M09-T08` exact production promotion GREEN for `elmakus/codex_workflow@4081cde7d4f71bcc2083c62cf69b7e3b845eefb8`.

## Outcome

Before M09/project completion, demonstrate in one actual Codex Main `muse-max` workflow session on the exact promoted release that exactly one internal GPT-5.6 Luna XHigh Companion is created and that the same Companion is reused later in that session.

## Scope

### Included

- Use only the bound exact promoted `codex_workflow` release commit `4081cde7d4f71bcc2083c62cf69b7e3b845eefb8`.
- In an actual Codex Main execution surface exposing the internal worker lifecycle, activate `muse-max`.
- Create one internal Companion allocated to GPT-5.6 Luna with XHigh reasoning.
- Reuse that same Companion later in the same workflow session.
- Capture bounded durable evidence of exact release identity, Companion identity/model/effort, and same-session reuse.
- Preserve all six non-Companion `muse-max` roles as Muse-backed leaf invocations.

### Excluded

- Any substitute Companion model, lower reasoning level, or Muse-routed Companion.
- Treating quota/runtime rejection as GREEN.
- Reimplementation of already-reviewed Muse runtime behavior except through a separately authorized corrective path if this final proof exposes a defect.

## Acceptance

- Evidence is bound to the exact promoted release commit.
- One actual Codex Main workflow session creates one GPT-5.6 Luna XHigh Companion.
- The same Companion identity is reused later in that same session.
- Companion remains internal Codex and is not routed through Muse.
- A quota/runtime rejection remains an open blocker and is not accepted as GREEN.
- No substitute model/harness is counted as a pass.

## Required tests / checks

- exact promoted release readback;
- active `muse-max` allocation readback;
- Companion creation identity/model/effort proof;
- same-session reuse proof;
- confirmation that no substitute model/harness was used.

## External write/readback needs

M09 live operations were explicitly authorized by the user on 2026-09-18. The exact promoted release is now bound and production-promoted. The Card remains blocked while the required Luna XHigh Companion cannot be exercised in an actual Codex Main session; a quota/runtime rejection is not GREEN.

## Independent review

`none` — this is the final live acceptance proof for the exact already-reviewed-and-promoted release; any defect found here routes through the normal corrective review path.

## Contract overrides

None.
