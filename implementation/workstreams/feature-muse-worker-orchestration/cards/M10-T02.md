# M10-T02 — Validate exact stateful Muse candidate live and freeze review subject

- Milestone: `M10`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m10--add-stateful-muse-logical-worker-lifecycle-and-fail-closed-recovery`
- Independent plan review: `planning/reviews/R6.md` (GREEN)
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R7-R15, R17 plus R1/R2/R4/R6/R10-R12 regression boundaries
- Accepted decision: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-stateful-worker-codex_workflow-profile`
- Research evidence/gate: `research/MUSE_SESSION_RESUME_LIFECYCLE_2026-09-19.md`
- Accepted dependency result: exact GREEN result/source commit from M10-T01
- Production regression baseline: `implementation/workstreams/feature-muse-worker-orchestration/evidence/M09_T08_PRODUCTION_PROMOTION_2026-09-18.md`

### Must preserve

- All M10-T01 runtime/session/role/binding/fail-closed invariants.
- Live validation must use the actual candidate adapter on the exact committed M10 source subject; no manual bypass may satisfy the lifecycle/fault-injection gate.
- A1 and B1 remain distinct sessions. B1 does not receive Executor trajectory and never performs production repair.
- B1 recheck of S2 is a full verification against current authority/workspace state; prior RED is not passing evidence.
- Interrupted-turn handling must prove process-tree cleanup, inspect partial workspace effects, probe exact session resumability, and then prove either safe same-session continuation or explicit new-worker recovery.
- No silent logical identity substitution and no cross-lane session reuse.
- Raw Muse session/trajectory material remains private and secret-safe; committed evidence is bounded.
- Production `~/.codex/codex_workflow` remains on `v1.1.17-private.11` and is not overwritten by the candidate.
- M09-T03 Companion blocker remains separate and is neither satisfied nor bypassed by this Card.

### Must not / rationale that must travel

- Do not count fixture-only evidence, a direct Muse CLI bypass, or a non-Meta provider as satisfying the interrupted-turn acceptance gate.
- Do not publish, tag, merge to `codex_workflow:main`, or promote the candidate to production.
- If safe lifecycle requires a strategic `muse serve`/MSP architecture change or contradicts the approved Definition, stop and route back to Project Definition instead of widening the Card.

## Dependencies

- M10-T01 GREEN with one exact immutable `codex_workflow` source commit.

## Outcome

The exact M10 stateful source candidate has live Meta-backed evidence for the full A1/B1 reuse lifecycle, interrupted-turn reconciliation/recovery and two-lane session isolation, with production runtime untouched. The exact same source subject is then ready to freeze for REQUIRED independent milestone review.

## Scope

### Included

- Isolated candidate runtime/workspaces/session registry derived from the exact M10-T01 source subject.
- Live lifecycle: A1 implement S1 -> B1 full review S1 RED -> A1 resume repair S2 -> B1 resume full recheck S2 GREEN.
- Live interrupted-turn fault injection through candidate adapter cancellation/timeout process control.
- Process-tree absence verification, partial-workspace readback and exact-session probe.
- Safe same-session continuation or explicit A2/B2 fallback demonstration.
- Two-lane live regression with separate Executor/Tester session namespaces per lane and no artifact/session collision.
- Bounded secret-safe evidence persisted in the workstation project repository.
- Exact source-subject and production-runtime readback at completion.

### Excluded

- Any stateful source mutation after the exact live subject is selected; if a bounded implementation defect is found, this Card records the failure and normal JIT corrective work must produce a new exact subject and rerun the applicable live gate.
- Companion invocation/acceptance.
- Release/publication/promotion.

## Acceptance

- Same A1 session is reused for repair and same independent B1 session is reused for full S2 recheck.
- A1 and B1 are distinct and their per-turn invocation/artifact identities are unique.
- Live fault injection terminates the full active Muse process tree and records partial workspace effects.
- Exact session probe results in either safe same-session continuation or normalized fail-closed recovery followed by an explicitly new logical/session identity.
- Runtime never silently reuses an old logical label for a newly minted session.
- Two isolated lanes use disjoint Executor/Tester sessions and private artifacts with no cross-lane reuse/collision.
- Structured results/artifact isolation/retention remain valid under resumed and interrupted turns.
- Committed evidence contains no raw trajectory/session secrets or credentials.
- Exact `codex_workflow` source commit used for all live evidence is unchanged from M10-T01.
- Production installed runtime/version/readback remains `v1.1.17-private.11`.
- On GREEN, M10 milestone review can be frozen as REQUIRED against that exact source commit with this live evidence.

## Required tests / checks

- Exact source commit/readback before every live phase.
- Full four-turn A1/B1 live cycle on Meta-backed Muse.
- Actual adapter timeout/cancel/fault-injection path.
- Descendant process-tree absence check.
- Exact session existence/health/resume probe.
- Explicit fallback path when the exercised branch is non-resumable, or a separate bounded negative-path proof sufficient to establish fail-closed replacement semantics when the live interrupted session is resumable.
- Two-lane live session/artifact isolation regression.
- Final production runtime/version/readback confirming no mutation.
- Evidence secret scan/boundedness check.

## Optional execution hints

- Priority: HIGH
- Complexity: HIGH
- Phase: isolated live validation / review freeze

## External write/readback needs

User authority already permits the R6-requested isolated real-Muse lifecycle/fault-injection validation. Writes are limited to disposable candidate runtime/workspaces/private session artifacts plus bounded project evidence/state. Every material live write must receive persisted-state/process/runtime readback. Production `~/.codex/codex_workflow` is excluded.

## Independent review

`none` at Card level. On GREEN this Card must activate the Master Plan's **REQUIRED milestone-level independent implementation review** for the exact immutable `codex_workflow` source subject used by the live evidence.
