# M05-T01 — Capture live Muse CLI/runtime contract

- Milestone: `M05`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m05--capture-the-live-muse-runtime-contract`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R12, R16; evidence inputs for R6, R10, R11, R13, R14
- Accepted decisions: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-codex_workflow-profile`
- Relevant OpenSpec: none
- Accepted dependency results:
  - completed M03 workstation Muse installation baseline
  - `research/MUSE_MAX_RUNTIME_AUDIT_2026-09-18.md`

### Must preserve

- This Card is evidence capture, not an architecture redesign.
- Exact Muse CLI argv/event fields must come from the actually installed workstation build.
- Muse login/session/auth state remains under persistent `/home/codex`; no credentials/session secrets may be committed.
- Raw sensitive session material is not durable project evidence.
- Workstation deployment continues to use tracked Compose/source as authority.
- Do not introduce workstation-side worker orchestration code in this Card.
- Do not modify `plus`, `luna-xhigh`, `pro-x5`, or `codex_workflow` implementation here.

### Must not / rationale that must travel

- Do not freeze parser or CLI behavior from public docs alone; this Card exists specifically to bind implementation to the installed version.
- Do not treat a successful process exit as proof of the final production result protocol.
- Do not capture or commit auth/session tokens, cookies, browser state, or secret-bearing raw logs.
- Do not perform live Unraid build/recreate/login/runtime actions before explicit user authorization.

## Dependencies

- M03 complete.
- Approved `requirements/MUSE_MAX_RUNTIME.md` and `planning/MASTER_PLAN.md`.
- Explicit user authorization before live Unraid operations.

## Outcome

Produce durable, secret-safe evidence describing the exact installed Muse Code headless runtime contract needed by later `codex_workflow` adapter work.

## Scope

### Included

- Refresh/read back the then-current workstation deployment through its supported path as required for live validation.
- Capture `muse --version`, relevant `muse --help`, and `muse exec --help`.
- Verify Muse login under persistent `/home/codex` and persistence through any required restart/recreate.
- Run bounded disposable-repository probes for:
  - machine-readable read-only/headless execution;
  - workspace-write execution;
  - controlled failure;
  - stdout/stderr behavior;
  - sandbox/write boundaries;
  - network/tool availability relevant to Investigator;
  - process/subprocess behavior relevant to later cancellation design.
- Capture exact observed terminal/lifecycle record shape and exit semantics.
- Persist a concise evidence artifact in the workstation repository with secrets removed.

### Excluded

- Implementing or rewriting `codex_workflow/runtime/muse_worker.py`.
- Changing `compute_profiles.py`.
- Implementing normalized result schemas, timeout/cancellation, or concurrency.
- Production release/publication of `codex_workflow`.
- Generic delegated-worker changes to Project Workflow.

## Acceptance

- Exact installed Muse version is recorded.
- Exact supported headless/machine-readable invocation surface needed by M07 is recorded.
- At least one successful machine-readable run is understood to terminal completion.
- One controlled failure has understood process/terminal/stderr behavior.
- Workspace-write and relevant sandbox/network boundaries are observed rather than assumed.
- Persistent auth behavior is verified at the level required by the normal workstation lifecycle.
- Evidence contains no auth/session secret.
- Later M07 can bind parser/argv behavior without guessing.

## Required tests / checks

- Read back exact command help/version output from the live workstation.
- Successful disposable read-only/headless run.
- Successful disposable workspace-write run.
- Controlled failure run.
- Check stdout versus stderr separation.
- Check resulting workspace state after write run.
- Check persisted evidence for accidental secret material before commit.
- Read back committed evidence from GitHub.

## External write/readback needs

Live workstation operations on Unraid plus a durable evidence write to `elmakus/chatgpt-ce-workstation:feat/muse-worker-orchestration`.

**Authorization gate:** explicit user approval is required before live Unraid build/recreate/login/runtime actions.

## Independent review

`none` — this Card captures implementation evidence and does not itself change production workflow/runtime semantics. The resulting evidence becomes authority for later reviewed implementation work.

## Contract overrides

None.
