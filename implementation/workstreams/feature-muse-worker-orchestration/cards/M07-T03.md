# M07-T03 — Correct live Muse exec argv dispatch

- Milestone: `M07`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m07--build-the-production-muse-processresult-adapter`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R6, R7, R12–R14
- Accepted decisions: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-codex_workflow-profile`
- Relevant OpenSpec: none
- Accepted dependency results:
  - `elmakus/codex_workflow@ab9601d9a3e587168b4c3182b3be3dffd7cc12bd`
  - `implementation/workstreams/feature-muse-worker-orchestration/evidence/M07_LIVE_ADAPTER_SMOKE_2026-09-18.md#first-read-only-attempt`

### Must preserve

- Keep all M07-T01 normalized-result, artifact, timeout/cancel, profile-authority, retention and secret-safety behavior unchanged.
- Bind argv to the actual installed Muse Code 1.3.0 parser: the headless subcommand must be selected as `muse exec` before exec-specific flags.
- Do not hard-code model/reasoning/profile allocation.
- A CLI parser/argv failure before a Muse terminal event must not be mislabeled as authentication merely because help text contains words such as `login`.
- Preserve unchanged behavior for `plus`, `luna-xhigh`, and `pro-x5`.

## Dependencies

- M07-T01 done.
- First authorized M07-T02 live attempt captured the argv/parser defect.

## Outcome

The adapter invokes Muse through the actual `muse exec [OPTIONS]` parser and classifies pre-run CLI usage/argv failures as adapter/internal failures rather than auth failures.

## Scope

### Included

- Correct `build_command` argv ordering.
- Correct failure classification for deterministic CLI parser/usage errors.
- Update focused/regression expectations and active documentation only if required by the corrected exact command.
- Deterministic tests and GitHub readback.

### Excluded

- New runtime features.
- Live smoke itself; M07-T02 resumes after this Card is GREEN.
- M08/M09 orchestration, concurrency or publication.

## Acceptance

- Generated command begins with `muse exec` and keeps M05-supported flags on the exec invocation.
- The exact live failure text observed in M07-T02 classifies as `adapter_internal`, not `auth_runtime`.
- M07 adapter suite, mixed-profile suite and full runtime regression are GREEN.
- Python compile and `git diff --check` are GREEN.
- Exact correction commit is pushed and verified by GitHub readback.

## Required tests / checks

- Focused adapter tests including argv order and observed CLI-usage classification.
- Existing `scripts.test_muse_profile`.
- Full `scripts/test_workflow_runtime.py`.
- Python compile check.
- `git diff --check`.

## External write/readback needs

Repository writes only to `elmakus/codex_workflow:impl/m07-muse-process-result-adapter`; verify pushed exact commit by GitHub readback.

## Independent review

none — M09 owns independent review of the complete production subject.

## Contract overrides

After GREEN, return to M07-T02 and repeat the authorized live read-only/write smoke against the corrected exact subject.
