# M11-T02 — Verify exact candidate and freeze REQUIRED review subject

- Milestone: `M11`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m11--cut-validate-publish-and-promote-the-stateful-muse-release`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` — R1, R3-R7, R10-R18
- Accepted decisions: `docs/DECISIONS.md` — D21, D22
- Relevant OpenSpec: none
- Accepted dependency results: M11-T01 exact release-candidate subject; M10 final independent GREEN/live evidence.

### Must preserve

- Verification is against the exact unchanged M11-T01 candidate.
- Exhaustive M10 evidence may be reused only when candidate behavioral blobs are unchanged.
- Exact candidate must preserve `plus`, `luna-xhigh`, `pro-x5` allocations/lifecycle and the accepted M10 stateful `muse-max` behavior.
- Candidate provenance must remain suitable for identity-preserving fast-forward publication.
- REQUIRED independent review is on this exact immutable candidate.

### Must not / rationale that must travel

- Do not repair the candidate while treating it as the same review subject.
- Any source change after verification begins must return to the owning corrective route and freeze a new exact subject.
- Do not live-validate, publish, or mutate production before REQUIRED review GREEN.

## Dependencies

- M11-T01 complete with exact candidate SHA and release-only diff evidence.

## Outcome

The exact release candidate passes complete deterministic/package regression coverage and is frozen in the Task Board as a REQUIRED independent review subject.

## Scope

### Included

- Complete workflow/runtime, Muse adapter/session and profile regression suites.
- Compilation/static validation, diff check, package build, archive verification and checksum verification.
- Exact candidate-vs-M10 diff audit.
- Profile-allocation regression for `muse-max`, `plus`, `luna-xhigh`, `pro-x5`.
- Freeze exact review subject/evidence and set Task Board review state to `pending`.

### Excluded

- Independent verdict by the implementing chat.
- Live exact-candidate Muse validation.
- Publication or production mutation.

## Acceptance

- Complete exact-candidate regression/package checks are GREEN.
- Candidate behavior blobs are unchanged from accepted M10 except permitted release-only synchronization.
- No Project Workflow state semantics appear in `codex_workflow`.
- Task Board records REQUIRED review as `pending` against the exact candidate SHA with concise evidence.
- The implementing chat stops at the fresh independent-review boundary.

## Required tests / checks

- `scripts/test_muse_adapter.py`.
- `scripts/test_workflow_runtime.py`.
- `scripts/test_muse_profile.py`.
- Python source compilation or repository-equivalent compile check.
- Package validation/build/verify, ZIP integrity and checksum verification.
- `git diff --check`.
- Exact diff/readback against `cf4c01f...`.

## External write/readback needs

Project evidence/Task Board updates only. No `codex_workflow:main`, tag/release or workstation production mutation.

## Independent review

`REQUIRED` — the release candidate is the exact source subject that may later be live-validated, published and installed. The chat that cut or changed it cannot issue the verdict.

## Contract overrides

None.
