# M07-T02 — Validate production Muse adapter live on workstation

- Milestone: `M07`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m07--build-the-production-muse-processresult-adapter`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R6, R7, R10–R14, R16
- Accepted decisions: `docs/DECISIONS.md` D5, D8–D10, D21
- Relevant OpenSpec: none
- Accepted dependency results:
  - `implementation/workstreams/feature-muse-worker-orchestration/evidence/M05_LIVE_MUSE_RUNTIME_2026-09-18.md`
  - M07-T01 exact completed implementation checkpoint

### Must preserve

- Use the actual installed Muse Code runtime and persistent account state without reading or recording credential contents.
- Use the M07-T01 adapter as implemented; do not bypass it with ad-hoc raw Muse commands except bounded diagnostics when a failure requires classification.
- Keep the disposable live workspace outside production project state and clean it after evidence capture.
- Raw run artifacts stay under persistent user runtime data, not project Git/Task Board/Main output.
- Current workstation Docker remains the outer isolation boundary; nested bubblewrap is known unavailable from M05.
- A live failure does not silently redefine accepted adapter semantics; persist evidence and route bounded corrections back through normal execution.

## Dependencies

- M07-T01 done.

## Outcome

The exact M07 adapter checkpoint is proven against the installed Muse Code build with one read-only and one disposable write run, including normalized result/raw-artifact readback and no residual worker process.

## Scope

### Included

- Read-only live adapter smoke using an authorized Muse-backed role.
- Disposable workspace-write live adapter smoke using an authorized executor role.
- Readback of normalized result, raw-artifact existence/permissions, expected workspace mutation, and process cleanup.
- Secret-safe evidence recording in the workstation project.
- Bounded correction only if the live finding remains inside the accepted M07 contract.

### Excluded

- Production release/publication.
- M08 multi-role orchestration/independent tester pilot.
- M09 concurrency.
- Reading credential/session file contents.

## Acceptance

- Exact installed Muse build completes one read-only adapter run and returns one valid normalized completed result.
- Exact installed Muse build completes one disposable write run; requested workspace mutation is correct and normalized result remains bounded.
- JSONL/stderr artifacts exist under the configured runtime artifact location and are referenced but not copied into normal output.
- No credential content appears in committed evidence.
- No residual Muse or child process from the probes remains.
- Any disposable workspace is cleaned after verification.

## Required tests / checks

- Exact adapter checkpoint readback before live invocation.
- Live read-only smoke.
- Live disposable write smoke.
- Artifact/path/permission readback.
- Residual process check.
- Cleanup readback.

## External write/readback needs

**Explicit live-operation authorization required before starting this Card.**

Targets:
- live workstation / Unraid Muse runtime;
- persistent `/home/codex` runtime artifact state;
- disposable workspace under the canonical project bind.

Read back all material live effects and persist only secret-safe evidence.

## Independent review

none — M09 owns independent review of the complete production subject.

## Contract overrides

Execution must stop at the authorization gate until the user explicitly authorizes M07 live operations.
