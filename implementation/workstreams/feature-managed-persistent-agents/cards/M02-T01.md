# M02-T01 — Prepare exact reviewed deployment candidate

- Milestone: `M02`

> Stable Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan: `planning/MANAGED_PERSISTENT_AGENTS_MASTER_PLAN.md#milestone-m02--production-activation-and-live-verification`
- Requirements: `requirements/MANAGED_PERSISTENT_AGENTS.md#R1-R12`
- Accepted decisions: `docs/DECISIONS.md#D8`, `#D10`, `#D12`, `#D23`
- Accepted predecessor: M01 checkpoint `00c32fa9f34229e2c227352f4891a7a64f119d35` with GREEN independent Card review.

## Must preserve

- The deployable workstation source must remain behaviorally identical to the independently reviewed M01 subject.
- Preparation must not rewrite the running workstation's persistent `~/.codex/AGENTS.md` and must not recreate/replace the production workstation container.
- Current foreign/user content evidence must be bounded: hashes/marker counts/stat only; do not dump unrelated AGENTS content.
- Build from the exact reviewed detached worktree and do not use `scripts/update.sh` or another upstream-refresh path for this feature activation.
- Preserve the current Docker isolation boundary and normal Compose deployment definition.

## Outcome

A replacement workstation image is built from the exact reviewed M01 source, with current production baseline and rollback/deployment inputs recorded, while the running container and persistent AGENTS file remain unchanged.

## Included

- Verify the exact detached worktree exists, is clean and resolves to M01 reviewed subject.
- Capture bounded current live AGENTS baseline: full hash/size, exact known legacy-prefix hash, workstation/codex_workflow marker counts, codex_workflow suffix hash and ownership/mode.
- Capture current running container/image/health and Compose identity.
- Run exact-subject source validation.
- Build through the tracked `scripts/build.sh` path from the exact reviewed worktree without an explicit upstream refresh.
- Record resulting candidate image ID.
- Verify the running container identity/image and persistent AGENTS hash are unchanged after the build.
- Persist a concrete rollback/live-deployment capsule for the next Card.

## Excluded

- No production persistent AGENTS write or backup creation.
- No `docker compose up --force-recreate`, restart, deployment or migration.
- No manual edit of the live AGENTS file.
- No upstream-refresh update run.
- No source behavior change.

## Acceptance

- Exact source worktree is clean at `00c32fa9f34229e2c227352f4891a7a64f119d35`.
- Live baseline proves the current file is the accepted unmarked legacy prefix followed by one `codex_workflow` region, using bounded hashes/counts only.
- Source validation is GREEN on the exact reviewed worktree.
- Candidate image build succeeds from that worktree without explicit upstream refresh.
- Candidate image ID is recorded.
- Running production container remains healthy on its pre-build image/identity.
- Production AGENTS SHA remains unchanged.
- Durable evidence records the exact commands/IDs/checks needed by the authorized live Card.

## Required checks

- `git status --porcelain` + exact `git rev-parse HEAD` on detached worktree.
- `bash scripts/validate-source.sh`.
- `bash scripts/build.sh`.
- Docker image/container readback.
- Bounded AGENTS hash/marker/stat readback before and after build.

## External write/readback

Docker image build/tag mutation only. No running-container or persistent-home mutation.

## Independent review

`REQUIRED` — this Card is the last reversible deployment-readiness checkpoint before the next Card performs a persistent shared-user-configuration migration. The independent review must verify exact reviewed-source identity, candidate image identity, bounded live baseline, rollback procedure and no production mutation before M02-T02 may become executable.

## Contract overrides

None.
