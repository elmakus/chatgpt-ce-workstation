# M00-T04 — Restore Codex-LB key helper to packaged v6 launcher

- Milestone: `M00`

## Authority slice

- Master Plan / milestone contract: `planning/SMART_UPSTREAM_UPDATES_MASTER_PLAN.md#milestone-m00--codex-web-gpt-fork-release-readiness`
- Requirements: `requirements/SMART_UPSTREAM_UPDATES.md`
- Accepted decisions: `docs/DECISIONS.md#D10`, `#D11`, `#D25`
- Relevant OpenSpec: none
- Accepted dependency results: published `elmakus/codex-chatgpt-web@v6.1.0-private.1`, reviewed subject `b39499c391744eaa378b157822e18b9218590b60`, and live Tower candidate-build failure recorded in the exact Intake.

### Must preserve

- Codex-LB routing/auth isolation and persistent key-helper behavior required by Workstation.
- Muse/CLIProxyAPI and ordinary native Codex behavior from the accepted v6.1 fork subject.
- Canonical downstream version lineage `v6.1.0-private.N`; an already-published immutable release is never rewritten.
- AppImage release checksum/provenance and image-managed update policy.
- Workstation installer remains fail-closed when a required packaged helper is absent.
- No production Workstation mutation is part of this Card; deployment resumes only after corrected release acceptance/publication.

### Must not / rationale that must travel

Do not weaken or delete the Workstation key-helper presence check merely to make the build pass. The defect is that the release artifact silently omitted a source-owned required asset.

## Dependencies

- Accepted M00 v6.1 synchronization/review/publication result.
- Live Tower pre-promotion failure proving `v6.1.0-private.1` lacks the helper in the packaged AppImage.

## Outcome

The next canonical v6.1 private release packages `set-codex-lb-key.sh` again and has regression coverage that fails on the final Linux AppImage if the required helper is omitted.

## Scope

### Included

- Restore `launcher/assets/set-codex-lb-key.sh` to launcher package inclusion and ASAR unpacking.
- Extend packaging-contract coverage for the required helper.
- Extend Linux packaged-launcher smoke to inspect the produced AppImage and verify the required helper is physically present at the bounded unpacked asset path.
- Advance canonical v6.1 private release metadata to the next legal revision and keep root/launcher/lockfile version metadata coherent.
- Run focused packaging tests plus applicable launcher/full verification, Linux package/AppImage smoke and ABI checks on the exact subject.

### Excluded

- Changes to Codex-LB routing/auth semantics.
- Relaxing Workstation installer validation.
- Unrelated upstream-v6 cleanup or refactors.
- Production Workstation deployment before independent review and corrected release publication.

## Acceptance

- The launcher build manifest includes `assets/set-codex-lb-key.sh` in both `files` and `asarUnpack`.
- The packaged Linux AppImage contains `resources/app.asar.unpacked/assets/set-codex-lb-key.sh`.
- Existing bounded Linux runner remains packaged.
- Canonical release version is the next valid `v6.1.0-private.N` revision without mutating `private.1`.
- Focused packaging-contract tests, full applicable verify/typecheck/build, Linux AppImage package smoke and ABI smoke are GREEN on the exact subject.
- No Workstation production mutation occurs during this Card.

## Required tests / checks

- `launcher/tests/packaging-contract.test.cjs`
- `bun run verify`
- `bun run app:package`
- `bun run app:smoke` on Linux
- Linux AppImage ABI smoke used by the fork release workflow
- exact artifact extraction/readback proving the helper path exists

## External write/readback needs

Related-repository branch/PR writes in `elmakus/codex-chatgpt-web`; no live production write. Publication is a later Close action after independent review.

## Independent review

`RECOMMENDED` — this changes the contents of the release artifact consumed by Workstation and corrects a live pre-promotion compatibility failure.

## Contract overrides

None.
