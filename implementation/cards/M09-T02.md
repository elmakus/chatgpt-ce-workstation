# M09-T02 — Validate reviewed M09 candidate live and close the pre-publication gate

## ID / milestone

- Card: `M09-T02`
- Milestone: `M09`
- Executor: normal ChatGPT under `chatgpt_only`

## Dependencies

- `M09-T01` done with independent GREEN.
- Exact reviewed candidate: `elmakus/codex_workflow@f6603767115cf7f31ef7d8c3cb3a419a7f430aca`.
- Independent review evidence: `implementation/reviews/m09-muse-lane-concurrency-independent-review-2026-09-18.md`.

## Authority

- `planning/MASTER_PLAN.md#milestone-m09--add-bounded-lane-concurrency-and-promote-the-complete-runtime`
- `requirements/MUSE_MAX_RUNTIME.md#r1--scope-is-only-muse-max`
- `requirements/MUSE_MAX_RUNTIME.md#r2--codex-main-remains-the-orchestrator`
- `requirements/MUSE_MAX_RUNTIME.md#r3--companion-is-persistent-luna-xhigh`
- `requirements/MUSE_MAX_RUNTIME.md#r7--muse-workers-are-leaf-one-shot-invocations`
- `requirements/MUSE_MAX_RUNTIME.md#r8--separate-executor-and-tester`
- `requirements/MUSE_MAX_RUNTIME.md#r11--raw-eventlog-isolation`
- `requirements/MUSE_MAX_RUNTIME.md#r13--timeout-and-process-tree-cancellation`
- `requirements/MUSE_MAX_RUNTIME.md#r15--project-workflow-owns-lane-parallelism`
- `requirements/MUSE_MAX_RUNTIME.md#r16--workstation-boundary`
- `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-codex_workflow-profile`
- `project-handoffs/M08_HANDOFF.md`
- `implementation/evidence/M09_SOURCE_CANDIDATE_2026-09-18.md`
- `implementation/reviews/m09-muse-lane-concurrency-independent-review-2026-09-18.md`
- `elmakus/codex_workflow@f6603767115cf7f31ef7d8c3cb3a419a7f430aca`

## Outcome

Using the exact independently reviewed M09 candidate without publishing or promoting it, close the two remaining live pre-publication evidence gaps: demonstrate safe managed two-lane Muse execution on the workstation and demonstrate one real GPT-5.6 Luna XHigh Companion created and reused within the same actual Codex Main workflow session.

## Included scope

- Verify the exact reviewed `codex_workflow` candidate SHA before any live test.
- Use only a reversible/isolated source-validation path for the reviewed candidate; do not publish a release or promote production in this Card.
- Activate/verify `muse-max` against that exact candidate using the existing supported source/runtime mechanism.
- Run one safe managed two-lane Muse smoke using caller-authorized, isolated, non-overlapping disposable workspaces.
- Verify both lanes can overlap in time, return distinct normalized results/run artifacts and leave no shared workspace/log collision.
- Verify no residual Muse worker/process tree remains and clean disposable workspaces.
- In an actual Codex Main workflow session on the same exact candidate, create exactly one internal Companion allocated to GPT-5.6 Luna with XHigh reasoning and reuse that same Companion later in the same session.
- Preserve six Muse-backed roles as leaf Muse invocations; the Companion must remain internal Codex and must not be substituted with another model/harness.
- Capture concise durable live evidence without raw secrets or full worker trajectories.

## Excluded scope

- Merge of PR #6 or any other merge to `codex_workflow/main`.
- Version bump, tag, GitHub Release or package publication.
- Production workstation promotion/update to a released M09 version.
- Permanent workstation architecture/configuration changes unrelated to the reversible candidate validation path.
- Any substitute Companion model or Muse-routed Companion.
- Project Workflow scheduler/task-database changes.

## Acceptance

- Exact candidate SHA is read back as `f6603767115cf7f31ef7d8c3cb3a419a7f430aca` for the live validation path.
- Managed two-lane Muse smoke overlaps safely on isolated non-overlapping workspaces and both lanes finish with distinct normalized results and artifact/run IDs.
- No lane corrupts or silently cancels the other; no residual Muse process tree or disposable workspace remains after cleanup.
- Active `muse-max` still allocates the six Muse roles to Muse Contributor/max and Companion to internal GPT-5.6 Luna XHigh.
- One actual Codex Main workflow session creates one GPT-5.6 Luna XHigh Companion and later reuses that same Companion in-session.
- A quota/runtime rejection of the required Companion is recorded as a blocker and is **not** accepted as GREEN.
- No substitute model/harness is counted as a pass.
- No release/publication/production promotion occurs in this Card.
- Durable evidence records exact subject, commands/actions, readbacks, cleanup and any limitations.

## Required tests / checks

- exact reviewed SHA/source readback before live execution;
- active `muse-max` allocation readback;
- live two-lane managed Muse smoke on isolated disposable workspaces;
- distinct run/artifact IDs and bounded normalized results;
- process-tree and disposable-workspace cleanup readback;
- actual Codex Main Companion creation identity/model/effort proof;
- same-session Companion reuse proof;
- final no-publication/no-promotion readback.

## External write/readback needs

This Card performs live workstation operations and requires explicit user authorization before any live mutation/test begins.

The Companion portion additionally requires an actual Codex Main execution surface that exposes the internal Companion lifecycle. Normal ChatGPT/terminal-only access must not be treated as equivalent proof.

## Independent review

`none` — M09-T01 already independently reviewed the exact immutable source candidate. This Card is live acceptance/readback evidence for that same subject.

## JIT continuation trigger

After this Card is GREEN, return to M09 Execution Prep and create the bounded publication Card for the exact reviewed-and-live-validated subject. Publication remains forbidden until this Card is GREEN.
