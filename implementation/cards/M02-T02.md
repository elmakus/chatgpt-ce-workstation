# M02-T02 — Prepare next workstation build for Muse Code live validation

- Milestone: `M02`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: user-approved M02 recorded in `implementation/TASK_BOARD.yaml`
- Requirements: next workstation build must be test-ready for Muse Code without merging Muse support to baseline before live validation
- Accepted decisions: `docs/DECISIONS.md` D8, D10, D11, D15, D16
- Muse plan: `docs/MUSE_CODE_PLAN.md`
- Research: `docs/MUSE_CODE_RESEARCH_2026-09-17.md`
- Accepted dependency results: M02-T01 published release

### Must preserve

- Muse Code remains isolated on `feat/muse-code` until first live Unraid validation succeeds.
- Muse application binaries are image-owned under `/opt/muse-code`; runtime wrapper is `/usr/local/bin/muse`.
- Persistent auth/config lives under the existing full `/home/codex` bind; no redundant nested bind.
- Runtime Muse auto-update stays disabled.
- Build/install stays non-interactive; runtime login is not stored in Git/image.
- Empty `CODEX_CHATGPT_WEB_VERSION` means workstation resolves the fork's current latest release at build time.
- No live Unraid deployment/login is part of this Card.

## Dependencies

- M02-T01.

## Outcome

Leave `feat/muse-code` source in a state where the next build can install the newly published Codex Web GPT release and image-managed Muse Code, then proceed directly to the documented live Muse validation sequence.

## Scope

### Included

- Refresh/audit the exact workstation source seams used by the next build.
- Confirm or correct latest-release resolution for Codex Web GPT.
- Confirm or correct immutable Muse installation/wrapper/runtime verification wiring.
- Ensure source validation covers Muse Code's required image-owned executable/wrapper assumptions.
- Record exact live-validation commands/gates if existing plan/runbook is insufficient.

### Excluded

- Performing the Unraid build/recreate.
- Muse account login.
- CLIProxyAPI/Meta OAuth runtime setup.
- Merging Muse support into workstation `main`.

## Acceptance

- Next build on `feat/muse-code`, with `CODEX_CHATGPT_WEB_VERSION` unset/empty, resolves the fork's latest release rather than a hard-coded 5.0.9.
- Dockerfile installs Muse Code under `/opt/muse-code`.
- `/usr/local/bin/muse` disables runtime auto-update and launches the image-owned binary.
- Runtime/source validation asserts Muse availability without requiring login during build.
- Persistent full home remains the auth/config persistence boundary.
- Live validation remains a separate explicit next step after build.

## Required tests / checks

- Static source readback.
- Existing `scripts/validate-source.sh` checks relevant to Muse/install/update wiring.
- No live-host claims without actual Unraid evidence.

## External write/readback needs

none; repository-only source preparation.

## Independent review

OPTIONAL — no live secrets/deployment and no new routing/security behavior unless Refresh Gate discovers a material change.
