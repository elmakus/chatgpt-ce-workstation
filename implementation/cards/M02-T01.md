# M02-T01 — Publish codex-chatgpt-web 5.0.10

- Milestone: `M02`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: user-approved M02 recorded in `implementation/TASK_BOARD.yaml`
- Requirements: publish the already-reviewed M01 Muse-routing implementation as the next installable workstation release
- Accepted decisions: `docs/DECISIONS.md#d11--codex-web-gpt-included-our-fork-is-the-default-package-source`
- Relevant OpenSpec: none
- Accepted dependency results: `project-handoffs/M01_HANDOFF.md`

### Must preserve

- Release must contain the exact M01 routing behavior already merged to `elmakus/codex-chatgpt-web:main`.
- Do not change routing/auth/security behavior merely to publish.
- Release must use the existing fork publication path (`.github/workflows/fork-linux-release.yml`) on a `release/v*` branch; do not add a new release mechanism.
- Workstation release discovery remains checksum-verified and uses the fork's `releases/latest` when `CODEX_CHATGPT_WEB_VERSION` is empty.
- No Unraid/live workstation mutation is authorized by this Card.

## Dependencies

- M01 complete.

## Outcome

Publish `v5.0.10` of `elmakus/codex-chatgpt-web` so its Linux AppImage contains the M01 Muse routing implementation and becomes the fork's latest release.

## Scope

### Included

- Synchronize repository/launcher/runtime version metadata to `5.0.10`.
- Create a bounded release commit synchronizing version metadata on `release/v5.0.10` from the reviewed fork `main`.
- Let the existing Fork Linux Release workflow run its verify/package/ABI/package-smoke gates and publish `v5.0.10`.
- After GREEN publication, fast-forward fork `main` to the same version-only release commit so default-branch metadata stays coherent.
- Verify the published release/assets/checksums and final `main` readback.

### Excluded

- Further routing changes.
- Workstation deployment/rebuild.
- CLIProxyAPI runtime configuration.
- Muse Code live login/testing.

## Acceptance

- Version metadata is synchronized at 5.0.10 and `bun run check-version`/full PR CI is GREEN.
- Fork Linux Release workflow completes GREEN from `release/v5.0.10`.
- Published tag/release `v5.0.10` points at the intended version-only release commit containing M01.
- Fork `main` is fast-forwarded to that same commit after publication.
- GitHub latest release is `v5.0.10` and includes Linux x64 AppImage plus checksums.
- Published AppImage/checksum readback is consistent.

## Required tests / checks

- Existing `fork-linux-release.yml` verification/package/ABI/package-smoke gates.
- GitHub release readback.

## External write/readback needs

GitHub release branch, publication, and post-publication fast-forward of fork `main`; read back branch/release/tag target, workflow conclusion, final `main`, assets and checksums.

## Independent review

OPTIONAL — bounded release/version metadata only; M01 implementation already received REQUIRED integrated independent review.
