# M10-T11 — Reconcile retained reservation after session-lock conflict

- Milestone: `M10`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#milestone-m10--add-stateful-muse-logical-worker-lifecycle-and-fail-closed-recovery`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R7-R9, R13-R15, R17 plus preserved R1/R2/R4/R6/R10-R12 regression boundaries
- Accepted decision: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-stateful-worker-codex_workflow-profile`
- Independent RED review: `implementation/reviews/m10-resume-terminal-lock-race-independent-review-2026-09-19.md`
- Reviewed baseline subject: `elmakus/codex_workflow@3a5d98b3c21fc37a527c3aa8eb0ed771e6428acb`
- Prior live evidence: `implementation/evidence/M10_T10_LIVE_RESUME_RESERVATION_CORRECTION_REVALIDATION_2026-09-19.md`

### Must preserve

- Terminal session state remains durably persisted before the finishing invocation releases its per-session flock.
- Retained-session resume remains atomically workspace-reserved before the registry guard is released for session-lock acquisition.
- A transient session-lock conflict after that reservation must fail closed without leaving an ownerless durable `reserved` state.
- Existing creator reservation, resume reservation, per-session flock, stale-`active`, `cleanup_unconfirmed`, binding, explicit replacement, structured-result/artifact, Tester no-repair, lane-isolation and non-`muse-max` behavior remain unchanged.
- Production `codex_workflow:main`, release metadata and installed runtime remain untouched.

## Dependencies

- M10-T10 done.
- M10 REQUIRED independent review RED on exact subject `3a5d98b3c21fc37a527c3aa8eb0ed771e6428acb`.

## Outcome

A new immutable `codex_workflow` candidate safely reconciles the resume-side `reserved` transition when non-blocking per-session lock acquisition loses the terminal-state-before-unlock race.

## Scope

### Included

- Bounded `runtime/muse_sessions.py` correction for retained-session reservation reconciliation after lock conflict.
- Deterministic regression for the exact terminal-state-persisted / flock-still-held interleaving.
- Any minimal test-only support needed for that regression.
- Existing M10 lifecycle/profile/runtime regression verification.

### Excluded

- Project Workflow policy/state changes.
- New public recovery API or lifecycle redesign.
- Companion semantics.
- Release/version bump, publication, merge to `codex_workflow:main`, or production promotion.
- Meta-backed revalidation, which is M10-T12.

## Acceptance

- A finishing invocation may persist `ready` before releasing its flock without allowing a concurrent resume attempt to strand the record in `reserved`.
- The overlapping resume fails with `session_busy` while the prior flock is held.
- After that failure, the retained record is durably restored to its prior resumable state when reconciliation persistence succeeds.
- After the prior invocation releases its flock, a later resume succeeds normally using the same retained session.
- Existing creator/resume reservation, same-session lease, stale-active, cleanup-unconfirmed, unavailable/binding/replacement and concurrency regressions remain GREEN.
- Full workflow-runtime, Muse-adapter and profile suites plus compile/package/archive verification and `git diff --check` are GREEN.
- Exact remote corrected head is recorded for M10-T12; no unauthorized main/release/production write occurs.

## Required tests / checks

- New deterministic terminal-state-before-flock-release interleaving regression.
- Existing resume-side and creator pre-lock reservation regressions.
- Existing same-session lease, stale-active persistence-failure and cleanup-unconfirmed regressions.
- Existing successful resume/binding/replacement/concurrency regressions.
- Full `scripts/test_workflow_runtime.py`.
- Full `scripts/test_muse_adapter.py`.
- Full `scripts/test_muse_profile.py`.
- Python compile, package validation/build/archive verification and `git diff --check`.
- Exact branch/base/head readback.

## External write/readback needs

Writes are limited to `elmakus/codex_workflow:impl/m10-stateful-muse-lifecycle` plus bounded workstation evidence/state. Read back exact remote head after correction. No production release/promotion is authorized.

## Independent review

`none` at Card level. The corrected exact M10 subject must complete M10-T12 live revalidation and then be frozen for the existing **REQUIRED milestone-level independent implementation review**.
