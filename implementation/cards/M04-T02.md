# M04-T02 — Fix Muse installer temporary-directory traversal

- Milestone: `M04`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Milestone contract: `implementation/cards/M04-T01.md`
- Runtime evidence: M04-T01 live build failure at Muse installer step on Unraid
- Relevant source: `scripts/build/install-muse-code.sh`
- Relevant OpenSpec: none

### Must preserve

- Muse installer is downloaded, SHA-256 is recorded and optional expected SHA-256 verification remains intact.
- Installer execution remains under the unprivileged `codex` user.
- Build HOME remains isolated under the temporary build directory.
- Muse remains installed under `/opt/muse-code/bin`.
- Runtime login/auth remains outside the image.

### Must not / rationale that must travel

- Do not solve the failure by running the vendor installer as root.
- Do not weaken permissions globally on `/tmp`.
- Do not disable checksum handling.
- Do not alter Muse version/update/auth policy beyond the minimum required traversal fix.

## Dependencies

- M03; live failure evidence from M04-T01

## Outcome

The temporary Muse build directory is traversable by the `codex` user that runs the vendor installer, eliminating the observed `Permission denied` while preserving the existing privilege and persistence model.

## Scope

### Included

- Correct ownership/mode of the per-build `mktemp` directory before switching to `codex`.
- Source validation / ShellCheck / Dockerfile static checks through repository CI.
- Merge the bounded correction to `main` after GREEN CI.

### Excluded

- Muse login.
- Worker protocol/model/sandbox behavior.
- Any unrelated workstation changes.

## Acceptance

- The helper makes its temporary parent directory accessible to `codex` without making the installer run as root.
- Existing source validation is GREEN.
- Existing ShellCheck is GREEN.
- Existing Dockerfile static check is GREEN.
- Secret scan is GREEN.
- Correction is merged to `main`.

## Required tests / checks

- Repository CI on PR.
- Subsequent M04-T01 live rebuild must pass beyond the prior installer permission failure.

## External write/readback needs

GitHub branch/PR/merge with exact merged-state readback.

## Independent review

none — bounded permission correction directly explained by exact live failure evidence.

## Contract overrides

none
