# M03-T01 — Merge Muse Code candidate into main for live validation

- Milestone: `M03`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- User decision: merge PR #1 now so the next Unraid build can be performed from workstation `main`; runtime fixes may follow after live validation.
- Accepted decision: `docs/DECISIONS.md#d19--merge-muse-code-candidate-before-live-validation`
- Muse integration plan: `docs/MUSE_CODE_PLAN.md`
- Predecessor evidence: `project-handoffs/M02_HANDOFF.md`
- PR: `elmakus/chatgpt-ce-workstation#1`

### Must preserve

- Merge only the current source-ready Muse candidate already validated by PR CI.
- Preserve the existing workstation baseline and the Codex Web GPT v5.0.10/latest-release behavior.
- Do not claim Muse runtime/live behavior is GREEN merely because source CI is GREEN.
- Keep live Muse validation as a required follow-up activity after merge.
- No Unraid build/recreate/login is part of this Card.

### Must not / rationale that must travel

- Do not add new Muse orchestration behavior in the merge Card.
- Do not silently treat `muse exec` flags/output protocol as frozen before live help/behavior inspection.
- Do not perform unrelated runtime/configuration changes.

## Dependencies

- M02 complete.

## Outcome

Merge PR #1 into workstation `main` so the next build may be performed directly from `main`, while retaining live Muse validation as subsequent work.

## Scope

### Included

- Persist the superseding merge-before-live-validation decision.
- Update the PR description/readiness state.
- Verify current PR head and CI.
- Merge PR #1 to `main`.
- Read back merged PR and `main` head.
- Reconcile Task Board/handoff to the actual merge result.

### Excluded

- Unraid build/recreate.
- Muse login.
- Muse orchestrator adapter implementation.
- Live worker/model/effort/cancellation/sandbox validation.

## Acceptance

- PR #1 current head is GREEN in repository CI before merge.
- PR #1 is merged into `main`.
- `main` readback contains the Muse Code candidate.
- Durable project state records that live validation is still pending.

## Required tests / checks

- Existing PR CI at exact pre-merge head.
- GitHub merge/readback verification.

## External write/readback needs

GitHub PR #1 readiness/merge and workstation `main` readback, explicitly authorized by the user on 2026-09-18.

## Independent review

none — publication of already source-validated repository state; live runtime behavior remains explicitly unaccepted pending validation.
