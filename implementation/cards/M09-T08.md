# M09-T08 — Promote published Muse-max release to workstation production runtime

- Milestone: `M09`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m09--add-bounded-lane-concurrency-and-promote-the-complete-runtime` (approved R5)
- Plan review: `planning/reviews/R5.md`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md#r1--scope-is-only-muse-max`, `#r15--project-workflow-owns-lane-parallelism`, `#r16--workstation-boundary`
- Accepted decisions: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-codex_workflow-profile`
- Relevant OpenSpec: none
- Accepted dependency results:
  - `M09-T05` exact release candidate + REQUIRED independent review GREEN
  - `M09-T06` exact-subject live two-lane GREEN
  - `M09-T07` publication GREEN
  - `implementation/evidence/M09_R5_PUBLICATION_2026-09-18.md`
- Exact production release subject: `elmakus/codex_workflow@4081cde7d4f71bcc2083c62cf69b7e3b845eefb8`, tag `v1.1.17-private.11`, ZIP SHA-256 `e274b3a23c49c785631f0ec6e645cfd4623c2e3388b65bac1d44f414b718e3c7`

### Must preserve

- Production promotion must consume the published owner release whose tag and asset provenance resolve to exact commit `4081cde7...`; do not substitute an unreviewed checkout or a different release.
- Workstation owns Muse CLI availability, persistent `/home/codex`, Muse authentication and runtime verification; `codex_workflow` owns role routing/process/result semantics.
- The existing persistent user home and unrelated `~/.codex` content, Codex settings, workers, skills and user AGENTS content must be preserved according to the release lifecycle contract.
- The selected production profile after promotion must be exactly `muse-max`: Companion `gpt-5.6-luna/xhigh/codex`; six remaining roles `muse-spark-1.3-contributor/max/muse-code`.
- Production smoke must use fresh separate Muse Executor and Tester invocations; Tester is grounded in accepted verification authority + resulting workspace state, not executor transcript/raw artifacts.
- Two-lane smoke must use isolated non-overlapping disposable workspaces and the managed concurrency helper; Project Workflow retains lane/workspace authority.
- Raw Muse JSONL/stderr remain private runtime artifacts outside project Git and normal Main-facing context.
- No Luna XHigh Companion invocation is required by this Card. `M09-T03` remains the final M09/project-completion gate.
- M09 live workstation operations were explicitly authorized by the user on 2026-09-18.

### Must not / rationale that must travel

- Do not rebuild/recreate the workstation image merely to install `codex_workflow`; the production workflow runtime is persistent user state under `/home/codex/.codex`.
- Do not mutate `elmakus/codex_workflow` source, release/tag identity, or workstation application/image source as part of promotion.
- Do not count an isolated M09 pre-publication runtime as production installation.
- Do not treat a quota/runtime rejection or another model as the M09-T03 Companion proof.
- Do not modify the tracked `chatgpt-ce-workstation` project merely to obtain a bootstrap target when a disposable bootstrap/smoke project suffices.

## Dependencies

- `M09-T07` done with publication GREEN for exact `4081cde7d4f71bcc2083c62cf69b7e3b845eefb8`.

## Outcome

Install/promote the exact published `v1.1.17-private.11` workflow into the workstation's persistent production Codex home, activate `muse-max`, and prove the available production Muse path with an Executor → fresh Tester smoke plus one safe managed two-lane run, without consuming the deferred Luna XHigh Companion gate.

## Scope

### Included

- Refresh the published tag/release/assets and workstation runtime state immediately before mutation.
- Verify the exact published ZIP against `SHA256SUMS` and the accepted SHA-256.
- If the shared production runtime is absent, use the exact release package's supported initial bootstrap path against a disposable project under the canonical project root; if an installed shared runtime already exists, use the supported owner-release update path instead.
- Preserve unrelated persistent user/runtime state and use the lifecycle transaction/backup behavior supplied by the release.
- Complete any required bootstrap documentation action on the disposable bootstrap project before considering initial installation complete; use the active supported role harness rather than silently doing Archivist work in Main.
- Set/read back production compute profile `muse-max`.
- Verify exact installed version `1.1.17-private.11`, Muse CLI/auth availability without reading credential contents, and the full role allocation.
- Run one disposable production Executor task and a separate fresh Tester verification against resulting state.
- Run one safe two-lane managed Muse validation with isolated disposable workspaces and verify both lanes, run identities, process cleanup and workspace isolation.
- Verify raw artifacts stay private and no production-smoke process subtree remains.
- Remove disposable bootstrap/smoke workspaces after acceptance while preserving the production runtime and bounded private runtime artifacts needed for evidence.
- Persist secret-safe promotion/readback evidence in the workstation project repository.

### Excluded

- Workstation image rebuild/recreate or changes to `Dockerfile`/`compose.yaml`.
- Source changes in `elmakus/codex_workflow`.
- Installing the workflow wrapper into unrelated user projects.
- Changing non-`muse-max` profile semantics.
- Luna XHigh Companion creation/reuse acceptance.
- Deleting unrelated user `~/.codex` state or Muse credentials.

## Acceptance

- Published release readback still binds `v1.1.17-private.11` and its verified ZIP to exact commit `4081cde7...`.
- Production `~/.codex/codex_workflow/operate/VERSION` reads exactly `1.1.17-private.11` after the supported bootstrap/update transaction.
- Production `muse-max` profile readback maps Companion to Luna XHigh/internal Codex and the other six roles to Muse Contributor/max.
- Muse Code remains available and authenticated sufficiently for the production worker smokes; no credential contents are recorded.
- A Muse Executor completes a bounded disposable task, and a fresh separate Tester returns GREEN from workspace/contract evidence without executor transcript/raw artifacts.
- Two authorized isolated lanes are observed/verified through the managed concurrency path and complete without cross-workspace or raw-log collision.
- No smoke process subtree remains and disposable workspaces are cleaned up.
- Persistent user home and unrelated runtime/configuration state are preserved.
- Durable secret-safe evidence records release identity, install/readback, profile mapping and smoke results.
- No Companion pass is claimed; `M09-T03` remains non-terminal until its exact live proof is possible.

## Required tests / checks

- exact published tag/release/asset provenance readback;
- ZIP + `SHA256SUMS` verification against accepted SHA-256;
- pre-mutation production runtime/version/profile snapshot;
- supported bootstrap/update transaction result + backup/readback;
- exact production VERSION and `profile --json` readback;
- Muse CLI version/help/auth-usable worker probe without credential disclosure;
- Executor → fresh Tester production smoke;
- managed two-lane concurrency smoke with distinct workspaces/run IDs and process cleanup;
- private run-artifact permission/isolation readback;
- final production runtime/version/profile readback;
- disposable path cleanup.

## External write/readback needs

Writes to the authorized live workstation persistent user runtime under `/home/codex/.codex` and to disposable bootstrap/smoke workspaces under `/home/codex/Documents/ChatGPT`. Every material write must be followed by persisted-state readback. M09 live operations were explicitly authorized by the user on 2026-09-18.

## Independent review

`none` — this Card deploys and live-validates the exact immutable release subject that already received REQUIRED independent source review and fresh exact-subject live validation. Any defect requiring a source/runtime correction creates a new subject and must route through the normal corrective review/release path.

## Contract overrides

None.
