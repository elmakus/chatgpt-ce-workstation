# M09-T05 — Cut R5 synchronized next-version release candidate

- Milestone: `M09`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#milestone-m09--add-bounded-lane-concurrency-and-promote-the-complete-runtime` (approved R5)
- Requirements: `requirements/MUSE_MAX_RUNTIME.md#r1--scope-is-only-muse-max`, `requirements/MUSE_MAX_RUNTIME.md#r16--workstation-boundary`
- Accepted decisions: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-codex_workflow-profile`
- Relevant OpenSpec: none
- Accepted dependency results: `elmakus/codex_workflow@f6603767115cf7f31ef7d8c3cb3a419a7f430aca`, `implementation/reviews/m09-muse-lane-concurrency-independent-review-2026-09-18.md`, `implementation/evidence/M09_LIVE_TWO_LANE_2026-09-18.md`, `planning/reviews/R5.md`
- Scope-correction evidence: `implementation/blockers/M09_T04_RELEASE_VERSION_TEST_SYNC_2026-09-18.md`
- Release contract: `elmakus/codex_workflow@f6603767115cf7f31ef7d8c3cb3a419a7f430aca:RELEASING.md`, `.github/workflows/release.yml`, and `scripts/test_workflow_runtime.py`

### Must preserve

- `f6603767115cf7f31ef7d8c3cb3a419a7f430aca` remains the accepted source implementation checkpoint.
- The release candidate must be exactly one direct child of that checkpoint.
- Candidate changes are limited to synchronized release-version material: `codex_workflow/operate/VERSION`, the version marker in `codex_workflow/operate/user_AGENTS.md`, README release-version text, `RELEASING.md`, and exactly four version-coupled literals in `scripts/test_workflow_runtime.py`.
- The four test literals are only: current version, previous version, `NEXT_PACKAGE_VERSION`, and update-fixture source version. They may move from the current release values to the selected next-release values only.
- No test logic, coverage, runtime semantics, Muse behavior, compute-profile allocation, role contract, release workflow logic, or workstation source may change.
- Immediately before choosing the version, re-read current tag/release state and select the next unique private version; `1.1.17-private.11` is only the expected value while `v1.1.17-private.10` remains the latest published/tagged private release.
- The exact candidate commit must pass complete regression/package verification and then become a REQUIRED fresh independent-review subject before any live revalidation or publication.
- R3/R5 Companion sequencing is unchanged: Luna XHigh quota does not block this Card, but M09/project completion remains blocked until the final live Companion proof on the exact promoted release.

### Must not / rationale that must travel

- Do not reuse `1.1.17-private.10`; that published tag/release already points to `f2b1811853a2c1da5a5af4bb735c84c3111a44d6`.
- Do not change any version-independent assertion, fixture behavior, test structure, runtime/source behavior, Muse orchestration code, profile allocation, role TOML, or release workflow.
- Do not advance `codex_workflow` `main`, create a tag/release, publish assets, update the workstation runtime, or perform live Muse validation in this Card.
- Do not merge/squash/rebase the candidate onto a different parent; exact subject identity is part of the release contract.

## Dependencies

- `M09-T01` done with independent GREEN at exact source checkpoint `f660376...`.
- `M09-T02` live two-lane GREEN on exact source checkpoint `f660376...`.
- Approved Master Plan R5 and independent plan review GREEN.
- Historical `M09-T04` is superseded; its blocker evidence is retained for provenance.

## Outcome

Create and push one exact next-version `codex_workflow` release-candidate commit directly on top of `f660376...`, containing only the R5-authorized release-version synchronization, with full deterministic regression/package verification GREEN and no publication/promotion side effects.

## Scope

### Included

- Re-read current published/tag state and choose the next unique private version.
- Create/use an isolated release-candidate branch directly from `f660376...`.
- Synchronize exactly:
  - `codex_workflow/operate/VERSION`;
  - version marker in `codex_workflow/operate/user_AGENTS.md`;
  - README release-version text;
  - `RELEASING.md` version/examples/release text;
  - exactly four version literals in `scripts/test_workflow_runtime.py`: current, previous, next package, update-fixture source.
- Run the complete deterministic regression, compile, validation, package-build and archive-verification checks on the exact resulting commit.
- Push the exact candidate branch and persist its immutable SHA/evidence into Task Board/project evidence.

### Excluded

- Any runtime/source-semantic or test-semantic change outside literal release-version synchronization.
- Changes to Muse profile allocation, adapter/concurrency implementation, role TOMLs, release workflow logic, or workstation runtime/source.
- `main` advancement, GitHub release/tag creation, publication, workstation update, live two-lane revalidation, or Companion validation.

## Acceptance

- Candidate parent is exactly `f6603767115cf7f31ef7d8c3cb3a419a7f430aca`.
- Diff from `f660376...` contains only the four release metadata files plus `scripts/test_workflow_runtime.py`.
- The test-file diff contains only the four authorized version literal substitutions and no other line change.
- Selected version is unique at execution-time readback and all required version markers/text are synchronized.
- Full workflow runtime, Muse adapter, Muse profile, compile/package validation/build/archive checks are GREEN on the exact candidate.
- Candidate branch is pushed and exact SHA is durably recorded.
- No `main`, tag, release, published asset, workstation runtime, or live validation side effect occurs.
- Task Board records the exact candidate as `review_subject` with `review_state: pending` after implementation.

## Required tests / checks

- current tag/release readback immediately before version selection;
- `git rev-parse <candidate>^` equals exact `f660376...`;
- exact changed-path/diff readback against `f660376...`;
- exact four-line diff readback for `scripts/test_workflow_runtime.py`;
- synchronized VERSION / `user_AGENTS.md` / README / `RELEASING.md` checks used by the release workflow;
- `python3 -B scripts/test_workflow_runtime.py -v`;
- `python3 -B scripts/test_muse_adapter.py -v`;
- `python3 -B scripts/test_muse_profile.py -v`;
- Python compile check matching repository CI;
- package validation, package build, archive verification, and `git diff --check`.

## External write/readback needs

Repository write only: push the isolated release-candidate branch in `elmakus/codex_workflow`, then read back the exact remote branch SHA. No release/publication/live-workstation authorization is consumed by this Card.

## Independent review

`REQUIRED` — approved R5 requires fresh independent review GREEN of the exact release-version-synchronized candidate before live two-lane validation or publication/promotion.

## Contract overrides

None.
