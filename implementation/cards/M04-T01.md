# M04-T01 — Rebuild current main and establish live Muse baseline

- Milestone: `M04`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan / milestone contract: `docs/MUSE_CODE_PLAN.md#live-validation-gates-after-merge`
- Requirements: `docs/MUSE_CODE_PLAN.md#authentication`, `docs/IMPLEMENTATION_PLAN.md#updates-after-successful-migration`
- Accepted decisions: `project-handoffs/M03_HANDOFF.md#remaining-gate`
- Relevant OpenSpec: none
- Accepted dependency results: M03 GREEN at `elmakus/chatgpt-ce-workstation@17111247fd28a07de15e5a94908bd3d63858184b`; current main source baseline `153a3bb337671fd6e742664710c5cc8e4d5c9a7a`

### Must preserve

- Rebuild from the current workstation `main` using repository-provided update/build/runtime verification.
- Preserve persistent `/home/codex` and direct project bind semantics.
- Muse remains image-owned under `/opt/muse-code`; auth/config remains persistent under `/home/codex`.
- Runtime Muse auto-update remains disabled by the workstation wrapper.
- Capture the resolved live Muse version and require `WORKSTATION_RUNTIME_GREEN`.
- Do not introduce Muse API/PAYG credentials or secrets into Git/image.

### Must not / rationale that must travel

- Do not treat source-only CI as proof of the Unraid runtime.
- Do not change Muse worker/orchestrator protocol during this rebuild; later live validation will establish that contract.
- Do not bypass repository scripts with an ad-hoc Docker recreation unless a concrete runtime defect requires bounded remediation.

## Dependencies

- M03

## Outcome

The target Unraid workstation is rebuilt from current `main`, the replacement container is healthy, repository runtime verification is GREEN, and the exact installed Muse version is captured for subsequent live Muse validation.

## Scope

### Included

- Fast-forward the Unraid checkout to current `main`.
- Confirm the checkout is clean and on `main`.
- Run `scripts/update.sh`.
- Capture update/build output relevant to the resolved Muse version.
- Require `WORKSTATION_RUNTIME_GREEN` from runtime verification.
- Read back the resulting Git HEAD and Muse version.

### Excluded

- Muse account login.
- Persistence/recreate authentication test beyond the rebuild itself.
- Bounded Muse worker task.
- Model/reasoning-effort validation.
- Timeout/process-tree cancellation validation.
- Sandbox classification.
- Orchestrator worker protocol or nested fan-out changes.

## Acceptance

- Unraid checkout is on `main`, clean, and fast-forwarded to the current remote main.
- `bash scripts/update.sh` completes successfully.
- Runtime verification prints `WORKSTATION_RUNTIME_GREEN`.
- `muse --version` succeeds in the running workstation and the exact version is recorded.
- Resulting live container remains healthy with expected persistent mounts.

## Required tests / checks

- `git status --short --branch`
- `git pull --ff-only`
- `bash scripts/update.sh`
- repository `scripts/verify-runtime.sh` as invoked by update
- post-update Git HEAD readback
- post-update in-container `muse --version`

## External write/readback needs

Target: live Unraid workstation container/image. User authorization for this rebuild was given in the active session on 2026-09-18. Required readback: container health/runtime verifier, exact Git HEAD, and exact Muse version.

## Independent review

none — this Card performs an already-approved operational rebuild and baseline readback without changing repository source or architecture.

## Contract overrides

none
