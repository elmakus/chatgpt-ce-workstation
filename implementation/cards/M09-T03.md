# M09-T03 — Close the deferred Luna XHigh Companion pre-publication gate

## ID / milestone

- Card: `M09-T03`
- Milestone: `M09`
- Executor: normal ChatGPT under `chatgpt_only`

## Dependencies

- `M09-T01` done with independent GREEN.
- `M09-T02` GREEN live two-lane Muse smoke.
- Exact reviewed candidate: `elmakus/codex_workflow@f6603767115cf7f31ef7d8c3cb3a419a7f430aca`.

## Authority

- `planning/MASTER_PLAN.md#milestone-m09--add-bounded-lane-concurrency-and-promote-the-complete-runtime`
- `requirements/MUSE_MAX_RUNTIME.md#r3--companion-is-persistent-luna-xhigh`
- `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-codex_workflow-profile`
- `project-handoffs/M08_HANDOFF.md`
- `implementation/reviews/m09-muse-lane-concurrency-independent-review-2026-09-18.md`
- `elmakus/codex_workflow@f6603767115cf7f31ef7d8c3cb3a419a7f430aca`

## Outcome

Before any M09 publication/release or production promotion, demonstrate in one actual Codex Main `muse-max` workflow session that exactly one internal GPT-5.6 Luna XHigh Companion is created and that the same Companion is reused later in that session.

## Included scope

- Use the exact independently reviewed M09 candidate.
- In an actual Codex Main execution surface exposing the internal worker lifecycle, activate `muse-max`.
- Create one internal Companion allocated to GPT-5.6 Luna with XHigh reasoning.
- Reuse that same Companion later in the same workflow session.
- Capture bounded durable evidence of identity/model/effort and same-session reuse.
- Preserve all six non-Companion `muse-max` roles as Muse-backed leaf invocations.

## Excluded scope

- Any substitute Companion model, lower reasoning level or Muse-routed Companion.
- Publication, release or production promotion before this Card is GREEN.
- Reimplementation of the already-reviewed M09 source candidate.

## Acceptance

- One actual Codex Main workflow session creates one GPT-5.6 Luna XHigh Companion.
- The same Companion identity is reused later in that same session.
- Companion remains internal Codex and is not routed through Muse.
- A quota/runtime rejection is recorded as an open blocker and is not accepted as GREEN.
- No substitute model/harness is counted as a pass.

## Required tests / checks

- active `muse-max` allocation readback;
- Companion creation identity/model/effort proof;
- same-session reuse proof;
- confirmation that no substitute model/harness was used.

## External write/readback needs

M09 live operations were explicitly authorized by the user on 2026-09-18.

The user explicitly requested this Companion check be deferred until Luna XHigh quota is available again.

## Independent review

`none` — this is the mandatory live acceptance gate for the exact independently reviewed candidate.

## JIT continuation trigger

After this Card is GREEN, return to M09 Execution Prep and create the bounded publication/release Card for the exact reviewed-and-live-validated subject.
