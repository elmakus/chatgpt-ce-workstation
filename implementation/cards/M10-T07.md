# M10-T07 — Fail closed during pre-lock Muse session reservation

- Milestone: `M10`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#milestone-m10--add-stateful-muse-logical-worker-lifecycle-and-fail-closed-recovery`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R7-R9, R13-R15, R17 plus preserved R1/R2/R4/R6/R10-R12 regression boundaries
- Accepted decision: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-stateful-worker-codex_workflow-profile`
- Independent RED review: `implementation/reviews/m10-stale-active-corrected-stateful-muse-runtime-independent-review-2026-09-19.md`
- Reviewed baseline subject: `elmakus/codex_workflow@7f6f51fe9faf318fa90ce9414e6024c673ad9226`
- Prior live evidence: `implementation/evidence/M10_T06_LIVE_STALE_ACTIVE_CORRECTION_REVALIDATION_2026-09-19.md`

### Must preserve

- One logical Muse worker/session has at most one active turn.
- An in-flight new-worker reservation cannot be adopted or bypassed by another process before the creator owns the session lease.
- A losing creator must not delete a mapping that another process has advanced beyond its own reservation.
- Normal `ready` resume, explicit replacement identity, stale-`active` and `cleanup_unconfirmed` fail-closed behavior, binding checks and confirmed-cleanup `needs_probe` resume remain unchanged.
- Caller-policy agnosticism, Tester no-repair, lane isolation, structured result/artifact isolation and non-`muse-max` profile behavior remain unchanged.
- Production `codex_workflow:main`, release metadata and installed runtime remain untouched.

## Dependencies

- M10-T06 done.
- M10 REQUIRED independent review RED on exact subject `7f6f51fe9faf318fa90ce9414e6024c673ad9226`.

## Outcome

A new immutable `codex_workflow` candidate makes the `reserved -> session flock -> active` transition process-safe and closes the independently reproduced startup race.

## Scope

### Included

- Bounded session-registry/lease correction for in-flight `reserved` mappings.
- Defensive creator cleanup that removes only a still creator-owned reservation.
- Deterministic concurrency regression reproducing the reviewed startup interleaving.
- Existing session lifecycle/profile/runtime regressions.

### Excluded

- Project Workflow policy/state changes.
- New public session-recovery API or lifecycle redesign.
- Companion semantics.
- Release/version bump, publication, merge to `codex_workflow:main`, or production promotion.
- Meta-backed revalidation, which is M10-T08.

## Acceptance

- While a creator is paused after durable reservation but before session-lock acquisition, same-worker resume fails closed with `session_busy`.
- A different replacement worker cannot start in the same canonical workspace while that reservation is unresolved.
- The original creator can continue after the contention probe and owns the only session lease.
- A creator cleanup path never deletes a record whose state is no longer its own `reserved` mapping.
- Existing normal ready-session resume remains GREEN.
- Existing stale-`active`, `cleanup_unconfirmed`, binding, busy, unavailable, explicit replacement and confirmed-cleanup resume regressions remain GREEN.
- Full workflow-runtime, Muse-adapter and profile suites plus compile/package/archive verification and `git diff --check` are GREEN.
- Exact remote corrected head is recorded for M10-T08; no unauthorized main/release/production write occurs.

## Required tests / checks

- Deterministic pre-lock reservation interleaving regression.
- Existing overlapping same-worker/session regression.
- Existing stale-active persistence-failure and cleanup-unconfirmed regressions.
- Existing successful resume/binding/replacement/concurrency regressions.
- Full `scripts/test_workflow_runtime.py`.
- Full `scripts/test_muse_adapter.py`.
- Full `scripts/test_muse_profile.py`.
- Python compile, package validation/build/archive verification and `git diff --check`.
- Exact branch/base/head readback.

## External write/readback needs

Writes are limited to `elmakus/codex_workflow:impl/m10-stateful-muse-lifecycle` plus bounded workstation evidence/state. Read back exact remote head after correction. No production release/promotion is authorized.

## Independent review

`none` at Card level. The corrected exact M10 subject must complete M10-T08 live revalidation and then be frozen for the existing **REQUIRED milestone-level independent implementation review**.
