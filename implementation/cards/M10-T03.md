# M10-T03 — Fail closed on unconfirmed Muse process-tree cleanup

- Milestone: `M10`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in `implementation/TASK_BOARD.yaml`.

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#milestone-m10--add-stateful-muse-logical-worker-lifecycle-and-fail-closed-recovery`
- Requirements: `requirements/MUSE_MAX_RUNTIME.md` R7, R9, R12-R15, R17 plus existing regression boundaries
- Accepted decision: `docs/DECISIONS.md#d21--muse-max-is-a-mixed-harness-stateful-worker-codex_workflow-profile`
- Independent RED review: `implementation/reviews/m10-stateful-muse-runtime-independent-review-2026-09-19.md`
- Corrected baseline subject: `elmakus/codex_workflow@b225fad5ffa0497566ff9179ef684968a3d9be16`
- Prior implementation/live evidence: `implementation/evidence/M10_T01_STATEFUL_MUSE_RUNTIME_2026-09-19.md`, `implementation/evidence/M10_T02_LIVE_STATEFUL_MUSE_VALIDATION_2026-09-19.md`

### Must preserve

- Interrupted Muse turns release/reconcile their logical-session lease only after cleanup of the captured Muse process tree is positively confirmed.
- If cleanup cannot be confirmed after bounded graceful/hard termination, fail closed: do not report safe resumability and prevent unsafe reuse of the affected session/workspace until explicitly reconciled.
- Stable session versus per-turn invocation identity, binding, one-active-turn locking, explicit replacement identity, Tester no-repair, structured result/artifact isolation and lane isolation remain unchanged.
- `codex_workflow` remains caller-policy agnostic.
- Production `codex_workflow:main`, release metadata and installed `~/.codex/codex_workflow` remain untouched.

## Dependencies

- M10-T02 done.
- M10 REQUIRED independent review RED on exact subject `b225fad5ffa0497566ff9179ef684968a3d9be16`, with this defect classified as bounded L1/L2 implementation correction.

## Outcome

A new immutable `codex_workflow` candidate closes the R13 cleanup-confirmation gap and fails closed when post-termination process-tree absence cannot be established.

## Scope

### Included

- Bounded process-tree termination/confirmation correction in the Muse adapter.
- Private session/workspace quarantine or equivalent fail-closed mechanism when cleanup remains unconfirmed.
- Deterministic regression proving the unconfirmed-cleanup path cannot be treated as safely reusable.
- Existing timeout/cancel successful-cleanup behavior and all stateful/profile regressions.

### Excluded

- Project Workflow policy/state changes.
- Companion semantics.
- Release/version bump, publication, merge to `codex_workflow:main`, or production promotion.
- Live Meta-backed revalidation, which is M10-T04.

## Acceptance

- After SIGTERM/SIGKILL escalation, the adapter performs an explicit bounded confirmation that the captured process tree is gone.
- Confirmed cleanup preserves existing timeout/cancel normalization and later exact-session probe/resume behavior.
- Unconfirmed cleanup yields a normalized fail-closed result and does not expose the affected logical session/workspace as safe for another Muse turn.
- A deterministic regression exercises the unconfirmed-cleanup path and proves subsequent unsafe reuse is rejected.
- Existing successful timeout/cancel cleanup tests remain GREEN.
- Runtime 90-test surface (or its corrected count), Muse adapter suite, profile suite, compile/package validation/build/archive verification and `git diff --check` are GREEN.
- The corrected exact branch head is recorded for M10-T04 and no unauthorized main/release/production write occurs.

## Required tests / checks

- Focused process-tree confirmation/quarantine regression.
- Existing timeout and cancellation descendant-cleanup regressions.
- Existing session resume/binding/busy/unavailable replacement regressions.
- Full `scripts/test_workflow_runtime.py`.
- Full `scripts/test_muse_adapter.py`.
- Full `scripts/test_muse_profile.py`.
- Python compile, package validation/build/archive verification and `git diff --check`.
- Exact branch/base/head readback.

## External write/readback needs

Writes are limited to `elmakus/codex_workflow:impl/m10-stateful-muse-lifecycle` plus bounded workstation evidence/state. Read back the exact remote branch head after the corrective commit. No production release/promotion is authorized.

## Independent review

`none` at Card level. The corrected exact M10 subject must complete M10-T04 live revalidation and then be frozen for the existing **REQUIRED milestone-level independent implementation review**.
